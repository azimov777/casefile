"""MCP-сервер для агентов.

Инструменты зовут те же сценарии из `services`, что и HTTP-слой, и ничего не знают про
`api`: дублирование логики между интерфейсами — то, что проект запрещает в первую
очередь. Сборка сервера — `app/mcp/server.py`, запуск — `python -m app.mcp`.

Через MCP агент проходит весь цикл задачи, не касаясь REST: берёт задачу, ведёт дело,
задаёт вопросы, декомпозирует, проверяет и закрывает. Дисциплину этого цикла сервер
раздаёт сам — полем `instructions` при подключении и метадатой каждого инструмента.
"""
