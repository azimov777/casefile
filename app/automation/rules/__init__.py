"""Правила автоматики, поставляемые с проектом.

Модуль сам ничего не импортирует: список модулей правил лежит в
`app/automation/registry.py` (`RULE_MODULES`), и загружает их `load_rules()`. Импорты
здесь означали бы два места, где перечислены правила, и второе однажды отстало бы.
"""
