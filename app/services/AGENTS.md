# app/services

Сценарии приложения: оркестрация домена, границы транзакций, запись в журнал изменений и outbox.
Один сценарий — одна функция, вызываемая и из REST, и из MCP; дублировать логику в интерфейсах нельзя.

Пока пуст: наполняется задачами 02 и далее (`docs/tasks`).

## Файлы
- `__init__.py` — маркер пакета
