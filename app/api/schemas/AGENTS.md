# app/api/schemas

Схемы Pydantic для HTTP-слоя: что приходит в запросе и что уходит в ответе. Из них же
собирается OpenAPI, поэтому примеры и описания полей пишутся здесь, а не в роутерах.

## Файлы
- `__init__.py` — маркер пакета
- `accounts.py` — учётные записи: чтение без хеша, заведение, ответ с паролем один раз, частичное изменение, сброс и смена пароля
- `authors.py` — автор действия: род и подпись; одна схема на все ответы, где есть автор
- `bootstrap.py` — первый экран: участник за токеном, его учётная запись, сам токен, очереди установки, число открытых вопросов
- `common.py` — оболочки `data`/`meta`, формат ошибки, `unset_field()` для схем `PATCH`
- `entries.py` — записи дела: размеченное по `type` объединение чтения и записи, модели нагрузки, строка описи
- `health.py` — ответ проверки здоровья
- `installation.py` — сведения установки: публичный адрес MCP
- `links.py` — связь со стороны задачи: вид, задача на другом конце со статусом, постановка связи
- `participants.py` — участники: чтение, регистрация, частичное обновление
- `queues.py` — очереди: чтение, создание, частичное обновление без ключа
- `search.py` — отбор задач: параметры запроса и структурный фильтр, задача в выдаче с необязательными полями
- `session.py` — вход: тело (почта и пароль) и живой сеанс — токен вкладки, срок, учётная запись
- `tasks.py` — задачи: чтение, пакет преемника с признаками, создание без статуса, частичное обновление, переход
- `tokens.py` — токены: чтение без секрета со сроком у токена сеанса, выпуск, ответ с секретом, токен текущего запроса (идентификатор и набор)
