# app/domain

Доменные модели и бизнес-правила на чистом Python: ключи задач, категории статусов, проверки
инвариантов. Не импортирует ни `api`, ни `db`, ни `mcp` — домен должен тестироваться без базы.

Пока пуст: наполняется задачами 03 и далее (`docs/tasks`).

## Файлы
- `__init__.py` — маркер пакета
