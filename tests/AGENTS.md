# tests

Тесты идут на реальной PostgreSQL из compose-контура: проект опирается на JSONB и специфичные
индексы, на SQLite они не воспроизводятся. Запуск — `docker compose run --rm test`.

Набор покрывает фундамент (участники, токены, проекты, автор), ядро задачи (переходы,
правки по статусам, версия), дело (записи агента, обязательная сводка, вердикты, вопросы,
пакет преемника), связи (виды, циклы, блокировка входа в работу, дети при закрытии, родословная закрытой задачи), замечания человека и их разбор, отбор
задач (язык запросов, структурный фильтр, вычисляемые признаки, курсор), ленту журнала
(хвост по `seq`, долгое ожидание на оповещениях, гонка ста параллельных подшивок, поток SSE
с продолжением), идемпотентность создающих вызовов (повтор, конфликт отпечатков, срок
жизни ключа, гонка одновременных повторов) и MCP: каждый инструмент через настоящий клиент
SDK, состав `tools/list` по наборам токена, `instructions` и метадата инструментов. Отдельно —
выдача доступа: первый токен человеку и ключ набора `main`, который установка кладёт в
файл своему интерфейсу. Отдельным слоем —
надёжность на двух транзакциях: гонка перехода со связью, гонка создающих вызовов в MCP и
командной строке, прекращение долгого опроса ленты после ухода клиента. Сверху лежат
сквозные сценарии: полный цикл задачи через REST и тот же цикл через MCP, дающие одну и ту
же опись дела, и сценарий ожидания ответа на блокирующий вопрос. Отдельно от кода
лежат проверки поставляемых артефактов и самих контуров запуска: закоммиченная схема
против кода, `.env.example` против `Settings`, одинаковое окружение приложения в
дев- и прод-контуре, скрипт, которым ветка задачи уходит в `main`, и скрипт проверки
подписи вклада — включая узкое исключение для Dependabot.

## Папки
- `data/` — файлы, которые неудобно хранить в коде теста: вендорная копия официальной схемы
  `server.json` реестра MCP (`mcp-server-schema-2025-12-11.json`, `test_server_json.py`) — схема
  датирована и неизменна, поэтому копия не протухает, а тест остаётся зелёным без сети

## Файлы
- `conftest.py` — общие фикстуры и помощники: тестовая база с миграциями, сессия с откатом, клиенты REST и MCP, вызов инструмента, токены наборов, проект, задача
- `test_accounts.py` — учётные записи: заведение с паролем один раз, отказы с причиной, право администратора и его отсутствие у агента и набора `task`, отключение с отзывом токенов, последний администратор, почта, сброс и смена пароля, двое в одной команде, команды `account-*`
- `test_admin_account.py` — администратор, которого установка заводит сама: при подъёме, `init` и шаге агента, у владельца до учётных записей, перенос прежнего `TRACKER_PASSWORD_HASH` без перетирания и отказ испорченному
- `test_agent_token.py` — токен агента от установки: набор `main` участнику `agent` в файле с правами `0600`, независимость от порядка с `local-token`, неизменность при повторе, новый токен и отзыв старого при потере файла, ключ интерфейса не трогается при переиздании
- `test_api_contract.py` — сплошная проверка контракта: оболочка у всех маршрутов, токен у всех, кроме объявленных в `TOKEN_EXEMPT`, справочник ошибок, полнота документации
- `test_archive.py` — перенос установки: выгрузка без исключённых таблиц и сеансов, право только администратора, приём в пустую установку с прежними делами, авторами и временем, доступы после переезда, архив старой ревизии до head, длинное описание очереди архива v0.3 — записью в дело проекта, отказы — установка с проектами, архив новее, испорченный архив — без следа
- `test_archive_visibility.py` — архивный проект скрыт по умолчанию (TRK-160): список проектов и `bootstrap` без `include_archived`, поиск без названного проекта, ключа или родителя, «входящая» и счётчик вопросов; лента и чтение по ключу — без скрытия; REST и MCP
- `test_artifacts.py` — поставляемые артефакты: закоммиченный `openapi.json` против кода, коды в отказах MCP, поля `Settings` против `.env.example`
- `test_assignee_takes_work.py` — вход в `in_progress` только исполнителю (TRK-123) через REST и MCP настоящими токенами: без исполнителя, чужой, совпавшее имя без учёта регистра, метка временного агента; отказ не трогает дело
- `test_auth.py` — аутентификация: наборы, метка временного агента, отозванный и просроченный токен, отметка использования
- `test_auto_update.py` — автообновление установки: канал `stable` и час по умолчанию, публикация канала на тег, compose-файл в образе выпуска, ревизия обновлятора против его определения, сценарий `updater` и `updater-renew` против подставного `docker` — проверка без выпуска, новый выпуск, файл выпуска, нет сети, выключатель, интервал
- `test_bootstrap.py` — первый экран: участник, его учётная запись, токен запроса с набором и идентификатором из списка токенов (именные `task` и `main`, общий агентский), проекты, счётчик вопросов
- `test_case_api.py` — эндпоинты дела: обзорные проверки задачи 23 через HTTP, полный цикл до `done`
- `test_case_index_facts.py` — факты в описи: сплошная сверка форм с `EntryType` в обоих
  контрактах, состав по типам, отсутствие свободного текста, независимость размера описи
  от длины разделов, цена пакета в запросах
- `test_case_service.py` — сценарии дела: подшивка записей, проверки по базе, пакет преемника, вопросы
- `test_compose.py` — контуры запуска: один и тот же набор переменных у приложения в дев- и прод-контуре, объявленное чтение `.env`, ключ интерфейса в каталоге, видимом хосту, владелец записанного файла, названный контуром, публичный адрес MCP в общем окружении контура, запрет монтировать что-либо внутрь каталога, приехавшего с хоста, один адрес публикации `CASEFILE_BIND` у обоих портов и режим пароля интерфейса из той же подстановки, что у API, `X-Real-IP` — только от службы `ui`, которая его перезаписывает
- `test_check_dco_script.py` — подпись вклада: базовый зелёный/красный путь, исключение для коммита Dependabot по `PR_AUTHOR_LOGIN`, самозванство именем и адресом бота без подтверждения GitHub, узость исключения внутри одного PR
- `test_check_edit.py` — правка одной обзорной проверки: точечная замена текста против списка целиком, пометка вердикта, оставшегося от прежней формулировки
- `test_demo.py` — демо-данные: все статусы, все типы записей, все виды связей, метка временного агента, идемпотентность повтора
- `test_domain_case.py` — домен дела без базы: форма нагрузки по типам, ссылки, выведенные заголовки
- `test_domain_foundation.py` — домен без базы: имена, ключи проектов, метки, наборы, структура автора
- `test_domain_journal.py` — домен ленты без базы: ключи задач и потолки, разбор `Last-Event-ID`, слияние двух позиций
- `test_domain_links.py` — домен связей без базы: пары видов, канонизация направления, имя стороны
- `test_domain_search.py` — домен отбора без базы: грамматика языка, позиция ошибки, потолки, склейка фильтров
- `test_domain_tasks.py` — домен задачи без базы: ключ, таблица переходов, шаг назад, проверки перехода, форма полей
- `test_end_to_end.py` — сквозные сценарии: цикл задачи через REST и через MCP с одинаковой описью, ожидание ответа на блокирующий вопрос
- `test_entry_numbering.py` — гонка за номером записи: двадцать одновременных записей в одну задачу без дыр
- `test_errors.py` — единый формат ошибки: доменное исключение, ошибка схемы, несуществующий маршрут
- `test_field_changed_journal.py` — правка обвязки доходит до ленты: запись `field_changed` и её путь до потребителя
- `test_health.py` — smoke-тест `/health` и проверка того, что фикстура доходит до настоящей БД
- `test_integrity_conflicts.py` — гонка создающих вызовов: проигравший получает доменный отказ в MCP и в командной строке, а не ошибку драйвера
- `test_idempotency.py` — идемпотентность через HTTP: повтор, конфликт отпечатков, срок жизни, все создающие маршруты
- `test_idempotency_race.py` — одновременные повторы одного ключа на настоящих сессиях: одна задача, откат освобождает ключ
- `test_installation.py` — сведения установки: адрес MCP по умолчанию и заданный, пустая настройка из compose, отказ адресу с паролем, один ответ любому токену
- `test_installers.py` — установщики `install.sh` и `install.ps1` текстом: проверка режима Docker Desktop есть в обоих, ветвится на `windows`, называет переключение на Linux-контейнеры одной по смыслу фразой и стоит до скачивания образов; синтаксис `install.sh`
- `test_journal_api.py` — лента через HTTP: хвост по номеру, фильтры, страницы и курсор, потолок ожидания
- `test_journal_disconnect.py` — ушедший клиент прекращает долгий опрос ленты; отключение изображается каналом ASGI
- `test_journal_stream.py` — поток SSE: форма кадра, продолжение по `Last-Event-ID`, потолок соединений; кадры читаются прямо из ASGI
- `test_journal_wait.py` — ожидание на настоящих коммитах: пробуждение оповещением, несколько дел одним ожиданием, сигнал не раньше строки, сто параллельных подшивок
- `test_last_entry_at.py` — признак `last_entry_at`: служебные записи его не двигают, и он не подменяет `updated_at`
- `test_links_api.py` — эндпоинты связей: обзорные проверки задачи 24 через HTTP, отказы и записи в делах
- `test_links_service.py` — сценарии связей: две стороны одной строки, записи в обоих делах, циклы, `blocked`
- `test_local_token.py` — ключ интерфейса от установки: исходы команды `local-token`, набор `main`, права файла, побайтная неизменность при повторе, отсутствие секрета в выводе, отзыв одноимённого предшественника, замена годного ключа `task` с отзывом его самого
- `test_mcp_instructions.py` — `instructions` сервера: не длиннее 2048 символов UTF-16 в ответе `initialize`, текст своего файла, каждое правило раскладки TRK-141 на месте
- `test_mcp_journal.py` — ожидание ленты из MCP: ключи задач, пустой ответ по таймауту, пробуждение оповещением из чужого процесса
- `test_mcp_metadata.py` — метадата в `tools/list`: без кириллицы, без повелительных форм, оценок и «because», без фраз, повторённых у двух инструментов, описание не длиннее 2048 единиц UTF-16, примеры только формата, коды конфликта, смысл пустого `next_cursor`, ожидание ответа — `waiting`
- `test_mcp_server.py` — каркас MCP: `initialize`, разбор токена и метки из заголовков, проверка здоровья
- `test_mcp_stdio.py` — транспорт stdio: токен процесса вместо заголовка, тот же состав, аннотации и подпись, что по HTTP; настоящий процесс `--stdio` — отказ без токена и только JSON-RPC в stdout
- `test_mcp_tool_files.py` — раскладка инструментов: файл `<группа>/<имя>.py` объявляет свой инструмент, реестр группы называет каждый файл, порядок реестров — порядок `tools/list`
- `test_mcp_tools.py` — инструменты через клиент SDK: состав по наборам, пакет преемника против REST, дело, связи, реестры
- `test_merge_script.py` — скрипт слияния: бит запуска, синтаксис, строка-доказательство и команды прогона (набор, линтер, `pnpm check`) в скрипте и в документах, порядок пересборки перед прогоном, красные линтер/набор/`pnpm check` останавливают слияние, `pnpm check` идёт только когда сама ветка трогает `ui/`
- `test_migrations.py` — миграции на данных: задача из `review` переезжает в `in_progress` записью от трекера, откат и повтор; длинное описание проекта переезжает записью «Описание до v0.4.0» с номером после последнего в деле
- `test_mutation_races.py` — гонка перехода со связью: закрытого родителя с открытым ребёнком и работы поверх блокера не бывает
- `test_notes.py` — заметки `docs/notes/`: четыре поля записи и их порядок, указатель «Где:» в живой
  файл и символ, имена из кода во всём тексте записи против кода проекта, разбор токена на образцах
- `test_openapi.py` — пригодность схемы к генерации клиента: идентификаторы, модели ответов
- `test_own_tokens.py` — свои токены людей: два человека видят и отзывают только свои, чужим именем не выпустить, агент с `main` не выпускает, агент в MCP до отзыва и после, администратор видит и отзывает все, отключение гасит агентов человека, токен отключённого не пускает
- `test_participants_api.py` — эндпоинты участников: оболочка, доступ по набору, канонизация имени
- `test_password_login.py` — вход по почте и паролю: токен сеанса в куке и в ответе, подпись этим токеном, чтение сеанса и срок, выход с отзывом токена, сеанс переживает перезапуск, отключённая учётная запись, окна попыток на адрес, почту и установку с `Retry-After` и попытками в полёте, пустышка незаведённой почты, подделанные заголовки адреса, `X-Real-IP` от своего nginx, доверенный прокси по имени, `/64` для IPv6, предел памяти счётчиков
- `test_passwords.py` — хеш пароля без базы: стоимость scrypt, строка без `$` для `.env`, разбор и отказы без самой строки, длина пароля
- `test_participants_service.py` — сценарии участников: регистрация, конфликт имени, правка описания
- `test_project_archive.py` — архив проекта: причина в обе стороны, записи `archived`/`restored` в деле и ленте, отказ `project_archived` на каждое изменение и раньше правил сценария, чтение, `unlink` из архива, восстановление, REST и MCP, наборы
- `test_project_attributes.py` — атрибуты проекта: заведение без причины, изменение и снятие с причиной, имя без учёта регистра, пределы и отказы, история отбором записей дела по имени, REST и MCP набором `task`
- `test_project_case.py` — дело проекта: номер внутри проекта и общий `seq`, типы задачи отклоняются, `created` и `field_changed` карточки, ссылка `TRK#7` из обоих дел, лента с отбором `project`, REST `/projects/{key}/entries` и инструменты MCP
- `test_project_description.py` — описание проекта до 320 знаков: предел в знаках на кириллице, отказ `project_description_too_long` в сценарии, REST и MCP, описание в карточке задачи и его отсутствие в строке поиска
- `test_project_numbering.py` — гонка за номером задачи: пятьдесят одновременных выдач на своих сессиях
- `test_projects_api.py` — эндпоинты проектов: доступ по набору, неизменяемый ключ, частичное обновление
- `test_projects_service.py` — сценарии проектов: создание, правка, чтение, выдача номеров
- `test_questions_api.py` — эндпоинт вопросов: открытость, адресат, признак `blocking`, фильтр по проекту
- `test_readme_tools.py` — README для каталогов MCP: раздел `## Tools` против множества зарегистрированных инструментов, JSON-блок `mcpServers` разбирается и указывает на адрес и заголовок авторизации
- `test_remarks.py` — замечание человека и его разбор: пакет одним вызовом в REST и MCP, исход значением, отбор «разобрано, но работа не закрыта», закрытая задача и признак
- `test_response_envelope.py` — форма ответа: одиночный ресурс в `data`, коллекция с `meta`
- `test_search_api.py` — эндпоинт списка задач: обзорные проверки задачи 25 через HTTP, выбор полей, отказы с позицией, страницы с номерами (общее число выдачи, адрес страницы смещением, цена вставки между запросами)
- `test_search_service.py` — сценарий отбора: два входа с одним результатом, признаки против карточки, курсор при вставке, подсчёт выдачи по просьбе и адрес страницы смещением
- `test_server_json.py` — карточка `server.json` для официального реестра MCP: соответствие вендорной схеме `2025-12-11`, имя пространства, адрес репозитория, отсутствие `packages`/`remotes`, форма версии-плейсхолдера
- `test_shutdown.py` — остановка процесса: поток ленты кончается по сигналу, а начатая запись дописывается
- `test_setup.py` — первичная инициализация: пустая установка размыкается, работающая не трогается
- `test_task_move.py` — перенос задачи в другой проект: новый номер и возврат прежнего ключа, прежний ключ в чтении, записях, связях, `refs`, поиске и `remarks_in_work`, отказы, REST и MCP, одновременные переносы на настоящих сессиях
- `test_tasks_api.py` — эндпоинты задач: обзорные проверки задачи 22 через HTTP, неизменяемость записей по схеме
- `test_tasks_service.py` — сценарии задач: создание, правки по статусам, переходы, версия, служебные записи
- `test_tokens_api.py` — эндпоинты токенов: секрет один раз, отзыв, общий агентский токен через HTTP
- `test_tokens_service.py` — сценарии токенов: выпуск именного и общего, список, идемпотентный отзыв
- `test_transactions.py` — граница транзакции: коммит, откат, падение самого коммита, конфликт целостности
- `test_wakeup.py` — слушатель журнала на настоящем соединении: `close()` не пишет предупреждение о потере связи, обрыв снаружи (`pg_terminate_backend`) — пишет
- `test_watch_journal_sample.py` — временный образец сторожа `scripts/watch-journal.sh` (TRK-77): бит запуска, синтаксис, отбор по отслеживаемой задаче, продолжение по `seq`, переживание отказа установки
