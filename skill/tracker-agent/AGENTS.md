# skill/tracker-agent

Скил дисциплины работы с трекером. Единственный исходный текст: из него берутся промпт
MCP `tracker-discipline` (весь файл) и поле `instructions` при инициализации сервера
(раздел «Кратко»).

## Файлы
- `SKILL.md` — текст скила с frontmatter `name` и `description`; раздел «Кратко» идёт первым и служит выжимкой: ход дисциплины, снятый из описаний инструментов, обязан иметь в нём строку
