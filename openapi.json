{
  "openapi": "3.1.0",
  "info": {
    "title": "Tracker API",
    "version": "0.1.0"
  },
  "paths": {
    "/health": {
      "get": {
        "tags": [
          "service"
        ],
        "summary": "Health check",
        "description": "Отвечает `ok`, только если соединение с БД действительно живое.",
        "operationId": "health",
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HealthResponse"
                }
              }
            }
          },
          "503": {
            "description": "Database is unavailable",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/bootstrap": {
      "get": {
        "tags": [
          "bootstrap"
        ],
        "summary": "Read the first screen",
        "description": "Текущий участник, очереди установки и число открытых вопросов к нему.\n\nРовно то, что нужно интерфейсу до первой отрисовки, и ничего сверх этого: списки\nзадач и вопросов приходят своими запросами, уже с фильтрами, которые выбрал человек.\n\nУ общего агентского токена участника нет: `participant` приходит `null`, а\n`open_questions` — ноль, потому что временного агента нельзя адресовать вопросом\n(`docs/CONCEPT.md`, 3.6). Очереди в этом случае отдаются те же самые.",
        "operationId": "read_bootstrap",
        "security": [
          {
            "ApiToken": []
          }
        ],
        "parameters": [
          {
            "name": "X-Actor-Label",
            "in": "header",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Signature of a temporary agent, latin snake_case. Required with a shared agent token (one issued without a participant), ignored with a participant token",
              "examples": [
                "nightly_agent"
              ],
              "title": "X-Actor-Label"
            },
            "description": "Signature of a temporary agent, latin snake_case. Required with a shared agent token (one issued without a participant), ignored with a participant token"
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DataResponse_BootstrapRead_"
                }
              }
            }
          },
          "401": {
            "description": "Token is missing, unknown or revoked",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "403": {
            "description": "Action is not allowed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "404": {
            "description": "Object not found",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "409": {
            "description": "State conflict",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "422": {
            "description": "Request validation failed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "500": {
            "description": "Unexpected error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/participants": {
      "get": {
        "tags": [
          "participants"
        ],
        "summary": "List participants",
        "description": "Все участники установки: люди и постоянные агенты.\n\nОдин список без разделения по роду: адресовать вопрос можно и человеку, и агенту, и\nинтерфейсу нужен общий выбор. Временных агентов здесь нет и быть не может — они не\nрегистрируются, и адресовать их нельзя.",
        "operationId": "list_participants",
        "security": [
          {
            "ApiToken": []
          }
        ],
        "parameters": [
          {
            "name": "limit",
            "in": "query",
            "required": false,
            "schema": {
              "type": "integer",
              "maximum": 200,
              "minimum": 1,
              "description": "Page size",
              "default": 50,
              "title": "Limit"
            },
            "description": "Page size"
          },
          {
            "name": "cursor",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Cursor from `meta.next_cursor` of a previous page",
              "title": "Cursor"
            },
            "description": "Cursor from `meta.next_cursor` of a previous page"
          },
          {
            "name": "X-Actor-Label",
            "in": "header",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Signature of a temporary agent, latin snake_case. Required with a shared agent token (one issued without a participant), ignored with a participant token",
              "examples": [
                "nightly_agent"
              ],
              "title": "X-Actor-Label"
            },
            "description": "Signature of a temporary agent, latin snake_case. Required with a shared agent token (one issued without a participant), ignored with a participant token"
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CollectionResponse_ParticipantRead_"
                }
              }
            }
          },
          "401": {
            "description": "Token is missing, unknown or revoked",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "403": {
            "description": "Action is not allowed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "404": {
            "description": "Object not found",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "409": {
            "description": "State conflict",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "422": {
            "description": "Request validation failed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "500": {
            "description": "Unexpected error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      },
      "post": {
        "tags": [
          "participants"
        ],
        "summary": "Register a participant",
        "description": "Заводит человека или постоянного агента. Токен ему выпускается отдельным запросом.\n\nТребует набора `main`. Имя уникально без учёта регистра и дальше неизменяемо: оно\nстоит подписью в записях дела, и переименование порвало бы эти подписи задним числом.\nПовтор с тем же `Idempotency-Key` отвечает первым участником, а не `409\nparticipant_name_taken`.",
        "operationId": "register_participant",
        "security": [
          {
            "ApiToken": []
          }
        ],
        "parameters": [
          {
            "name": "X-Actor-Label",
            "in": "header",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Signature of a temporary agent, latin snake_case. Required with a shared agent token (one issued without a participant), ignored with a participant token",
              "examples": [
                "nightly_agent"
              ],
              "title": "X-Actor-Label"
            },
            "description": "Signature of a temporary agent, latin snake_case. Required with a shared agent token (one issued without a participant), ignored with a participant token"
          },
          {
            "name": "Idempotency-Key",
            "in": "header",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Makes this creating call safe to repeat. A retry with the same key and the same request answers with the first response instead of creating a second object; the same key with a different request answers 409 idempotency_key_reused. Keys are paired with the token, are at most 255 characters long and are forgotten after 24 hours",
              "examples": [
                "6b1f0c34-9b2e-4b0a-9a5f-3f1d6c8e0a11"
              ],
              "title": "Idempotency-Key"
            },
            "description": "Makes this creating call safe to repeat. A retry with the same key and the same request answers with the first response instead of creating a second object; the same key with a different request answers 409 idempotency_key_reused. Keys are paired with the token, are at most 255 characters long and are forgotten after 24 hours"
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ParticipantCreate"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DataResponse_ParticipantRead_"
                }
              }
            }
          },
          "401": {
            "description": "Token is missing, unknown or revoked",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "403": {
            "description": "Action is not allowed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "404": {
            "description": "Object not found",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "409": {
            "description": "State conflict",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "422": {
            "description": "Request validation failed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "500": {
            "description": "Unexpected error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/participants/{participant_name}": {
      "get": {
        "tags": [
          "participants"
        ],
        "summary": "Read a participant",
        "description": "Карточка участника по имени. Адресация мягкая: `Alice` находит `alice`.",
        "operationId": "read_participant",
        "security": [
          {
            "ApiToken": []
          }
        ],
        "parameters": [
          {
            "name": "participant_name",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "description": "Participant name; matching ignores case",
              "examples": [
                "release_bot"
              ],
              "title": "Participant Name"
            },
            "description": "Participant name; matching ignores case"
          },
          {
            "name": "X-Actor-Label",
            "in": "header",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Signature of a temporary agent, latin snake_case. Required with a shared agent token (one issued without a participant), ignored with a participant token",
              "examples": [
                "nightly_agent"
              ],
              "title": "X-Actor-Label"
            },
            "description": "Signature of a temporary agent, latin snake_case. Required with a shared agent token (one issued without a participant), ignored with a participant token"
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DataResponse_ParticipantRead_"
                }
              }
            }
          },
          "401": {
            "description": "Token is missing, unknown or revoked",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "403": {
            "description": "Action is not allowed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "404": {
            "description": "Object not found",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "409": {
            "description": "State conflict",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "422": {
            "description": "Request validation failed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "500": {
            "description": "Unexpected error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      },
      "patch": {
        "tags": [
          "participants"
        ],
        "summary": "Update a participant",
        "description": "Меняет описание участника; имя и род неизменяемы. Требует набора `main`.",
        "operationId": "update_participant",
        "security": [
          {
            "ApiToken": []
          }
        ],
        "parameters": [
          {
            "name": "participant_name",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "description": "Participant name; matching ignores case",
              "examples": [
                "release_bot"
              ],
              "title": "Participant Name"
            },
            "description": "Participant name; matching ignores case"
          },
          {
            "name": "X-Actor-Label",
            "in": "header",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Signature of a temporary agent, latin snake_case. Required with a shared agent token (one issued without a participant), ignored with a participant token",
              "examples": [
                "nightly_agent"
              ],
              "title": "X-Actor-Label"
            },
            "description": "Signature of a temporary agent, latin snake_case. Required with a shared agent token (one issued without a participant), ignored with a participant token"
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ParticipantUpdate"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DataResponse_ParticipantRead_"
                }
              }
            }
          },
          "401": {
            "description": "Token is missing, unknown or revoked",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "403": {
            "description": "Action is not allowed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "404": {
            "description": "Object not found",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "409": {
            "description": "State conflict",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "422": {
            "description": "Request validation failed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "500": {
            "description": "Unexpected error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/tokens": {
      "get": {
        "tags": [
          "tokens"
        ],
        "summary": "List tokens",
        "description": "Все токены установки, включая отозванные: у отозванного заполнено `revoked_at`.\n\nСекрета в списке нет — в базе лежит только хеш, и восстановить значение неоткуда.",
        "operationId": "list_tokens",
        "security": [
          {
            "ApiToken": []
          }
        ],
        "parameters": [
          {
            "name": "limit",
            "in": "query",
            "required": false,
            "schema": {
              "type": "integer",
              "maximum": 200,
              "minimum": 1,
              "description": "Page size",
              "default": 50,
              "title": "Limit"
            },
            "description": "Page size"
          },
          {
            "name": "cursor",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Cursor from `meta.next_cursor` of a previous page",
              "title": "Cursor"
            },
            "description": "Cursor from `meta.next_cursor` of a previous page"
          },
          {
            "name": "X-Actor-Label",
            "in": "header",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Signature of a temporary agent, latin snake_case. Required with a shared agent token (one issued without a participant), ignored with a participant token",
              "examples": [
                "nightly_agent"
              ],
              "title": "X-Actor-Label"
            },
            "description": "Signature of a temporary agent, latin snake_case. Required with a shared agent token (one issued without a participant), ignored with a participant token"
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CollectionResponse_TokenRead_"
                }
              }
            }
          },
          "401": {
            "description": "Token is missing, unknown or revoked",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "403": {
            "description": "Action is not allowed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "404": {
            "description": "Object not found",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "409": {
            "description": "State conflict",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "422": {
            "description": "Request validation failed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "500": {
            "description": "Unexpected error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      },
      "post": {
        "tags": [
          "tokens"
        ],
        "summary": "Issue a token",
        "description": "Единственный ответ, содержащий секрет токена: второго способа узнать его нет.\n\nТребует набора `main`. Без `participant` выпускается общий агентский токен: запрос с\nним обязан нести заголовок `X-Actor-Label`, иначе действие некому приписать.\n\nЕдинственное исключение — повтор с тем же `Idempotency-Key`: он отвечает **тем же**\nсекретом, потому что ответ первого выпуска сохранён целиком. Иначе повтор запроса,\nоборвавшегося по сети, оставлял бы действующий токен, которого никто не видел.\nСутки спустя ключ забыт вместе с ответом, и секрета не остаётся нигде.",
        "operationId": "issue_token",
        "security": [
          {
            "ApiToken": []
          }
        ],
        "parameters": [
          {
            "name": "X-Actor-Label",
            "in": "header",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Signature of a temporary agent, latin snake_case. Required with a shared agent token (one issued without a participant), ignored with a participant token",
              "examples": [
                "nightly_agent"
              ],
              "title": "X-Actor-Label"
            },
            "description": "Signature of a temporary agent, latin snake_case. Required with a shared agent token (one issued without a participant), ignored with a participant token"
          },
          {
            "name": "Idempotency-Key",
            "in": "header",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Makes this creating call safe to repeat. A retry with the same key and the same request answers with the first response instead of creating a second object; the same key with a different request answers 409 idempotency_key_reused. Keys are paired with the token, are at most 255 characters long and are forgotten after 24 hours",
              "examples": [
                "6b1f0c34-9b2e-4b0a-9a5f-3f1d6c8e0a11"
              ],
              "title": "Idempotency-Key"
            },
            "description": "Makes this creating call safe to repeat. A retry with the same key and the same request answers with the first response instead of creating a second object; the same key with a different request answers 409 idempotency_key_reused. Keys are paired with the token, are at most 255 characters long and are forgotten after 24 hours"
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/TokenCreate"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DataResponse_TokenIssued_"
                }
              }
            }
          },
          "401": {
            "description": "Token is missing, unknown or revoked",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "403": {
            "description": "Action is not allowed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "404": {
            "description": "Object not found",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "409": {
            "description": "State conflict",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "422": {
            "description": "Request validation failed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "500": {
            "description": "Unexpected error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/tokens/{token_id}": {
      "delete": {
        "tags": [
          "tokens"
        ],
        "summary": "Revoke a token",
        "description": "Отзыв идемпотентен: повторный запрос отвечает так же и ничего не меняет.\n\nТребует набора `main`. Запись токена остаётся в базе с проставленным `revoked_at` —\nпо ней видно, чем ходили раньше. Наружу это выглядит удалением, поэтому `DELETE`\nи `204`.",
        "operationId": "revoke_token",
        "security": [
          {
            "ApiToken": []
          }
        ],
        "parameters": [
          {
            "name": "token_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid",
              "description": "Identifier of the token to revoke",
              "title": "Token Id"
            },
            "description": "Identifier of the token to revoke"
          },
          {
            "name": "X-Actor-Label",
            "in": "header",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Signature of a temporary agent, latin snake_case. Required with a shared agent token (one issued without a participant), ignored with a participant token",
              "examples": [
                "nightly_agent"
              ],
              "title": "X-Actor-Label"
            },
            "description": "Signature of a temporary agent, latin snake_case. Required with a shared agent token (one issued without a participant), ignored with a participant token"
          }
        ],
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "401": {
            "description": "Token is missing, unknown or revoked",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "403": {
            "description": "Action is not allowed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "404": {
            "description": "Object not found",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "409": {
            "description": "State conflict",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "422": {
            "description": "Request validation failed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "500": {
            "description": "Unexpected error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/queues": {
      "get": {
        "tags": [
          "queues"
        ],
        "summary": "List queues",
        "description": "Все очереди установки. Единственный уровень группировки: над ними ничего нет.",
        "operationId": "list_queues",
        "security": [
          {
            "ApiToken": []
          }
        ],
        "parameters": [
          {
            "name": "limit",
            "in": "query",
            "required": false,
            "schema": {
              "type": "integer",
              "maximum": 200,
              "minimum": 1,
              "description": "Page size",
              "default": 50,
              "title": "Limit"
            },
            "description": "Page size"
          },
          {
            "name": "cursor",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Cursor from `meta.next_cursor` of a previous page",
              "title": "Cursor"
            },
            "description": "Cursor from `meta.next_cursor` of a previous page"
          },
          {
            "name": "X-Actor-Label",
            "in": "header",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Signature of a temporary agent, latin snake_case. Required with a shared agent token (one issued without a participant), ignored with a participant token",
              "examples": [
                "nightly_agent"
              ],
              "title": "X-Actor-Label"
            },
            "description": "Signature of a temporary agent, latin snake_case. Required with a shared agent token (one issued without a participant), ignored with a participant token"
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CollectionResponse_QueueRead_"
                }
              }
            }
          },
          "401": {
            "description": "Token is missing, unknown or revoked",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "403": {
            "description": "Action is not allowed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "404": {
            "description": "Object not found",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "409": {
            "description": "State conflict",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "422": {
            "description": "Request validation failed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "500": {
            "description": "Unexpected error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      },
      "post": {
        "tags": [
          "queues"
        ],
        "summary": "Create a queue",
        "description": "Заводит очередь. Требует набора `main`.\n\nКлюч уникален без учёта регистра, хранится в верхнем и дальше неизменяем: он идёт\nв ключ каждой задачи очереди. Повтор с тем же `Idempotency-Key` отвечает первой\nочередью, а не `409 queue_key_taken`.",
        "operationId": "create_queue",
        "security": [
          {
            "ApiToken": []
          }
        ],
        "parameters": [
          {
            "name": "X-Actor-Label",
            "in": "header",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Signature of a temporary agent, latin snake_case. Required with a shared agent token (one issued without a participant), ignored with a participant token",
              "examples": [
                "nightly_agent"
              ],
              "title": "X-Actor-Label"
            },
            "description": "Signature of a temporary agent, latin snake_case. Required with a shared agent token (one issued without a participant), ignored with a participant token"
          },
          {
            "name": "Idempotency-Key",
            "in": "header",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Makes this creating call safe to repeat. A retry with the same key and the same request answers with the first response instead of creating a second object; the same key with a different request answers 409 idempotency_key_reused. Keys are paired with the token, are at most 255 characters long and are forgotten after 24 hours",
              "examples": [
                "6b1f0c34-9b2e-4b0a-9a5f-3f1d6c8e0a11"
              ],
              "title": "Idempotency-Key"
            },
            "description": "Makes this creating call safe to repeat. A retry with the same key and the same request answers with the first response instead of creating a second object; the same key with a different request answers 409 idempotency_key_reused. Keys are paired with the token, are at most 255 characters long and are forgotten after 24 hours"
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/QueueCreate"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DataResponse_QueueRead_"
                }
              }
            }
          },
          "401": {
            "description": "Token is missing, unknown or revoked",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "403": {
            "description": "Action is not allowed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "404": {
            "description": "Object not found",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "409": {
            "description": "State conflict",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "422": {
            "description": "Request validation failed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "500": {
            "description": "Unexpected error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/queues/{queue_key}": {
      "get": {
        "tags": [
          "queues"
        ],
        "summary": "Read a queue",
        "description": "Карточка очереди вместе с описанием — общим контекстом всех её задач.\n\nАгент запрашивает её отдельно: в карточке задачи лежат только ключ и название, а\nописание бывает длинным, и таскать его в каждом ответе значило бы тратить контекст.",
        "operationId": "read_queue",
        "security": [
          {
            "ApiToken": []
          }
        ],
        "parameters": [
          {
            "name": "queue_key",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "description": "Queue key; matching ignores case",
              "examples": [
                "TRK"
              ],
              "title": "Queue Key"
            },
            "description": "Queue key; matching ignores case"
          },
          {
            "name": "X-Actor-Label",
            "in": "header",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Signature of a temporary agent, latin snake_case. Required with a shared agent token (one issued without a participant), ignored with a participant token",
              "examples": [
                "nightly_agent"
              ],
              "title": "X-Actor-Label"
            },
            "description": "Signature of a temporary agent, latin snake_case. Required with a shared agent token (one issued without a participant), ignored with a participant token"
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DataResponse_QueueRead_"
                }
              }
            }
          },
          "401": {
            "description": "Token is missing, unknown or revoked",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "403": {
            "description": "Action is not allowed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "404": {
            "description": "Object not found",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "409": {
            "description": "State conflict",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "422": {
            "description": "Request validation failed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "500": {
            "description": "Unexpected error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      },
      "patch": {
        "tags": [
          "queues"
        ],
        "summary": "Update a queue",
        "description": "Меняет название и описание; ключ неизменяем. Требует набора `main`.\n\nПоле `key` в теле — ошибка `422`, а не молчаливый пропуск: клиент должен узнать,\nчто переименования не произошло, из ответа, а не из следующего чтения.",
        "operationId": "update_queue",
        "security": [
          {
            "ApiToken": []
          }
        ],
        "parameters": [
          {
            "name": "queue_key",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "description": "Queue key; matching ignores case",
              "examples": [
                "TRK"
              ],
              "title": "Queue Key"
            },
            "description": "Queue key; matching ignores case"
          },
          {
            "name": "X-Actor-Label",
            "in": "header",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Signature of a temporary agent, latin snake_case. Required with a shared agent token (one issued without a participant), ignored with a participant token",
              "examples": [
                "nightly_agent"
              ],
              "title": "X-Actor-Label"
            },
            "description": "Signature of a temporary agent, latin snake_case. Required with a shared agent token (one issued without a participant), ignored with a participant token"
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/QueueUpdate"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DataResponse_QueueRead_"
                }
              }
            }
          },
          "401": {
            "description": "Token is missing, unknown or revoked",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "403": {
            "description": "Action is not allowed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "404": {
            "description": "Object not found",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "409": {
            "description": "State conflict",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "422": {
            "description": "Request validation failed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "500": {
            "description": "Unexpected error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/tasks": {
      "post": {
        "tags": [
          "tasks"
        ],
        "summary": "Create a task",
        "description": "Заводит задачу в `backlog`. Ключ выдаёт счётчик очереди, статус не принимается.\n\nВ деле сразу появляется запись `created` с автором из токена. Разделы можно\nоставить пустыми и дописать в `backlog`; перед переходом в `open` четыре раздела\nдолжны быть заполнены, а `checks` — содержать хотя бы одну проверку.\n\nПовтор с тем же `Idempotency-Key` и тем же телом отвечает первой задачей, а не\nзаводит вторую: номер очереди на этом не тратится.",
        "operationId": "create_task",
        "security": [
          {
            "ApiToken": []
          }
        ],
        "parameters": [
          {
            "name": "X-Actor-Label",
            "in": "header",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Signature of a temporary agent, latin snake_case. Required with a shared agent token (one issued without a participant), ignored with a participant token",
              "examples": [
                "nightly_agent"
              ],
              "title": "X-Actor-Label"
            },
            "description": "Signature of a temporary agent, latin snake_case. Required with a shared agent token (one issued without a participant), ignored with a participant token"
          },
          {
            "name": "Idempotency-Key",
            "in": "header",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Makes this creating call safe to repeat. A retry with the same key and the same request answers with the first response instead of creating a second object; the same key with a different request answers 409 idempotency_key_reused. Keys are paired with the token, are at most 255 characters long and are forgotten after 24 hours",
              "examples": [
                "6b1f0c34-9b2e-4b0a-9a5f-3f1d6c8e0a11"
              ],
              "title": "Idempotency-Key"
            },
            "description": "Makes this creating call safe to repeat. A retry with the same key and the same request answers with the first response instead of creating a second object; the same key with a different request answers 409 idempotency_key_reused. Keys are paired with the token, are at most 255 characters long and are forgotten after 24 hours"
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/TaskCreate"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DataResponse_TaskRead_"
                }
              }
            }
          },
          "401": {
            "description": "Token is missing, unknown or revoked",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "403": {
            "description": "Action is not allowed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "404": {
            "description": "Object not found",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "409": {
            "description": "State conflict",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "422": {
            "description": "Request validation failed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "500": {
            "description": "Unexpected error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      },
      "get": {
        "tags": [
          "tasks"
        ],
        "summary": "List and search tasks",
        "description": "Задачи по строке запроса, по структурному фильтру или по обоим сразу.\n\nОба входа сводятся к одному отбору и на одинаковых условиях дают одинаковый\nрезультат в одинаковом порядке: `?query=queue: TRK and status: open` и\n`?queue=TRK&status=open` — это буквально один путь исполнения. Условия из разных\nисточников складываются по `and`.\n\nЗапрос без условий — законный: это «все задачи» по ключу, и отдельного способа\nсказать то же самое заводить незачем. Ошибка разбора приходит `422\ninvalid_search_query` с позицией символа; незнакомое имя поля — `422\nsearch_field_unknown` со списком допустимых в `details.allowed`.\n\nОтбирать можно и по вычисляемым признакам (`blocked`, `open_questions`,\n`open_blocking_questions`, `open_remarks`): колонок под них нет, они считаются из\nсвязей и дела прямо в запросе. Запрос кандидатов назначателя — одна строка:\n`queue: TRK and status: open and blocked: false and open_blocking_questions: 0`.\nЕсть и поле отбора без признака — `remarks_in_work`: «замечание приняли в работу, а\nназванная задача ещё не закрыта».\n\nТе же признаки приходят **в каждой строке** объектом `features` — тем самым, что\nв пакете преемника: значок «заблокирована» и счётчик вопросов рисуются из списка,\nбез запроса на каждую строку.\n\n`fields` оставляет в ответе только перечисленные поля плюс `key`: полная задача с\nпятью разделами съедает контекст агента, которому нужен столбец ключей. Признаки\nвыбираются целиком именем `features`; отдельный признак именем поля выдачи не\nвыбирается — `blocked` остаётся именем условия отбора.\n\n**Страницами.** `meta.total` — сколько задач нашлось по отбору, а не сколько их на\nстранице: из него и `limit` собирается «страница 3 из 7, всего 98». Это\nединственная коллекция API, которая его считает, и считает всегда — вторым запросом\nпо тому же отбору.\n\nСтраницу адресует либо `cursor` из `meta.next_cursor` предыдущей страницы, либо\n`offset` — номер первой строки от начала выдачи: страница N размера L начинается с\n`(N - 1) * L`. Вместе они не принимаются (`422 cursor_with_offset`): это два разных\nадреса одной страницы, и выбрать за клиента значило бы отдать не ту.\n\nЦена смещения названа честно: база читает и выбрасывает пропускаемые строки, а\nстраница сдвигается, если между двумя запросами задачу завели или подняли наверх\n(`sort=-updated_at`) — строка на границе покажется дважды или пропадёт. Курсор от\nэтого свободен, поэтому обход **всей** выдачи (и агенты через MCP) идёт им.\nСмещение за концом выдачи — законный запрос: пустая страница, `has_more: false` и\nпрежний `total`.",
        "operationId": "list_tasks",
        "security": [
          {
            "ApiToken": []
          }
        ],
        "parameters": [
          {
            "name": "query",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string",
                  "maxLength": 4096
                },
                {
                  "type": "null"
                }
              ],
              "description": "Query language string, for example `queue: TRK and status: open and blocked: false and open_blocking_questions: 0`. Fields: `assignee`, `blocked`, `last_entry_at`, `open_blocking_questions`, `open_questions`, `open_remarks`, `parent`, `priority`, `queue`, `remarks_in_work`, `status`, `text`. Operators: `=`, `!=`, `>`, `>=`, `<`, `<=`, `~` (contains), `!~`, `in`, `not in`; `empty()` matches tasks with no value in the field. The operator goes **after** the colon — `status: in open, in_progress`, not `status in (open, in_progress)`: parentheses group conditions, not values. Without an operator a condition means equality, and several comma-separated values already mean set membership. Combine with `and`, `or` and parentheses. Values with spaces or a leading language word go in quotes. Examples: `queue: TRK and status: open and blocked: false`; `status: in open, in_progress`; `priority: >= high and text: ~ ключ`; `assignee: empty() or open_questions: > 0`. A parse error answers 422 with the position of the offending character and, where the right shape follows from it, with that shape in `details.hint`",
              "examples": [
                "queue: TRK and status: open and blocked: false"
              ],
              "title": "Query"
            },
            "description": "Query language string, for example `queue: TRK and status: open and blocked: false and open_blocking_questions: 0`. Fields: `assignee`, `blocked`, `last_entry_at`, `open_blocking_questions`, `open_questions`, `open_remarks`, `parent`, `priority`, `queue`, `remarks_in_work`, `status`, `text`. Operators: `=`, `!=`, `>`, `>=`, `<`, `<=`, `~` (contains), `!~`, `in`, `not in`; `empty()` matches tasks with no value in the field. The operator goes **after** the colon — `status: in open, in_progress`, not `status in (open, in_progress)`: parentheses group conditions, not values. Without an operator a condition means equality, and several comma-separated values already mean set membership. Combine with `and`, `or` and parentheses. Values with spaces or a leading language word go in quotes. Examples: `queue: TRK and status: open and blocked: false`; `status: in open, in_progress`; `priority: >= high and text: ~ ключ`; `assignee: empty() or open_questions: > 0`. A parse error answers 422 with the position of the offending character and, where the right shape follows from it, with that shape in `details.hint`"
          },
          {
            "name": "sort",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "array",
                  "items": {
                    "type": "string"
                  },
                  "maxItems": 3
                },
                {
                  "type": "null"
                }
              ],
              "description": "Sort keys, most significant first. A leading `-` sorts descending: `-updated_at`. Sortable: `key`, `last_entry_at`, `priority`, `updated_at`. `key` orders by queue and task number, so `TRK-10` follows `TRK-2`. The result is always tie-broken by task id, so paging stays stable while tasks are being created",
              "examples": [
                [
                  "-updated_at"
                ]
              ],
              "title": "Sort"
            },
            "description": "Sort keys, most significant first. A leading `-` sorts descending: `-updated_at`. Sortable: `key`, `last_entry_at`, `priority`, `updated_at`. `key` orders by queue and task number, so `TRK-10` follows `TRK-2`. The result is always tie-broken by task id, so paging stays stable while tasks are being created"
          },
          {
            "name": "fields",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                },
                {
                  "type": "null"
                }
              ],
              "description": "Fields to return, to keep the answer small: `assignee`, `checks`, `constraints`, `context`, `created_at`, `created_by`, `description`, `features`, `goal`, `id`, `key`, `output`, `priority`, `queue`, `status`, `title`, `updated_at`, `version`. Omit for the whole task, computed features included. The task key is always included. `features` is picked as a whole and brings `blocked`, `open_questions`, `open_blocking_questions`, `open_remarks`, `last_summary_at`, `last_entry_at`; a single feature is not a field of the answer, and asking for one answers 422 `search_field_unknown` with the selectable names",
              "examples": [
                [
                  "title",
                  "status"
                ]
              ],
              "title": "Fields"
            },
            "description": "Fields to return, to keep the answer small: `assignee`, `checks`, `constraints`, `context`, `created_at`, `created_by`, `description`, `features`, `goal`, `id`, `key`, `output`, `priority`, `queue`, `status`, `title`, `updated_at`, `version`. Omit for the whole task, computed features included. The task key is always included. `features` is picked as a whole and brings `blocked`, `open_questions`, `open_blocking_questions`, `open_remarks`, `last_summary_at`, `last_entry_at`; a single feature is not a field of the answer, and asking for one answers 422 `search_field_unknown` with the selectable names"
          },
          {
            "name": "limit",
            "in": "query",
            "required": false,
            "schema": {
              "type": "integer",
              "maximum": 200,
              "minimum": 1,
              "description": "Page size",
              "default": 50,
              "title": "Limit"
            },
            "description": "Page size"
          },
          {
            "name": "cursor",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Cursor from `meta.next_cursor` of a previous page",
              "title": "Cursor"
            },
            "description": "Cursor from `meta.next_cursor` of a previous page"
          },
          {
            "name": "offset",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 0
                },
                {
                  "type": "null"
                }
              ],
              "description": "Rows to skip before the page, an alternative address to `cursor`: page N of size L starts at `(N - 1) * L`. Sending both is refused (`cursor_with_offset`)",
              "examples": [
                100
              ],
              "title": "Offset"
            },
            "description": "Rows to skip before the page, an alternative address to `cursor`: page N of size L starts at `(N - 1) * L`. Sending both is refused (`cursor_with_offset`)"
          },
          {
            "name": "queue",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "array",
                  "items": {
                    "type": "string"
                  },
                  "maxItems": 100
                },
                {
                  "type": "null"
                }
              ],
              "description": "Queue keys; matching ignores case",
              "examples": [
                [
                  "TRK"
                ]
              ],
              "title": "Queue"
            },
            "description": "Queue keys; matching ignores case"
          },
          {
            "name": "parent",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "array",
                  "items": {
                    "type": "string"
                  },
                  "maxItems": 100
                },
                {
                  "type": "null"
                }
              ],
              "description": "Parent task keys: the answer holds their direct children, one level deep. `empty()` finds tasks with no parent — the top level of a queue. An unknown key answers 422 instead of an empty page: emptiness here reads as «no children» and would hide the typo",
              "examples": [
                [
                  "TRK-7"
                ]
              ],
              "title": "Parent"
            },
            "description": "Parent task keys: the answer holds their direct children, one level deep. `empty()` finds tasks with no parent — the top level of a queue. An unknown key answers 422 instead of an empty page: emptiness here reads as «no children» and would hide the typo"
          },
          {
            "name": "status",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/TaskStatus"
                  }
                },
                {
                  "type": "null"
                }
              ],
              "description": "Task statuses",
              "examples": [
                [
                  "open"
                ]
              ],
              "title": "Status"
            },
            "description": "Task statuses"
          },
          {
            "name": "assignee",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "array",
                  "items": {
                    "type": "string"
                  },
                  "maxItems": 100
                },
                {
                  "type": "null"
                }
              ],
              "description": "Assignee names, matched exactly; `empty()` finds unassigned tasks",
              "examples": [
                [
                  "release_bot"
                ]
              ],
              "title": "Assignee"
            },
            "description": "Assignee names, matched exactly; `empty()` finds unassigned tasks"
          },
          {
            "name": "priority",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/TaskPriority"
                  }
                },
                {
                  "type": "null"
                }
              ],
              "description": "Task priorities",
              "examples": [
                [
                  "high"
                ]
              ],
              "title": "Priority"
            },
            "description": "Task priorities"
          },
          {
            "name": "blocked",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "boolean"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Whether the task has a `blocked_by` link to a task that is neither `done` nor `cancelled`. Computed from links, not stored",
              "title": "Blocked"
            },
            "description": "Whether the task has a `blocked_by` link to a task that is neither `done` nor `cancelled`. Computed from links, not stored"
          },
          {
            "name": "open_questions",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 0
                },
                {
                  "type": "null"
                }
              ],
              "description": "Exact number of questions with no answer. Use the query language for ranges: `open_questions: > 0`",
              "title": "Open Questions"
            },
            "description": "Exact number of questions with no answer. Use the query language for ranges: `open_questions: > 0`"
          },
          {
            "name": "open_blocking_questions",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 0
                },
                {
                  "type": "null"
                }
              ],
              "description": "Of those, the ones marked `blocking`; `0` means nothing is in the way",
              "title": "Open Blocking Questions"
            },
            "description": "Of those, the ones marked `blocking`; `0` means nothing is in the way"
          },
          {
            "name": "open_remarks",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 0
                },
                {
                  "type": "null"
                }
              ],
              "description": "Exact number of remarks with no resolution. Use the query language for ranges: `open_remarks: > 0`",
              "title": "Open Remarks"
            },
            "description": "Exact number of remarks with no resolution. Use the query language for ranges: `open_remarks: > 0`"
          },
          {
            "name": "remarks_in_work",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 0
                },
                {
                  "type": "null"
                }
              ],
              "description": "Remarks resolved as `accepted` whose continuation task is still open: reviewed, but the work is not finished",
              "title": "Remarks In Work"
            },
            "description": "Remarks resolved as `accepted` whose continuation task is still open: reviewed, but the work is not finished"
          },
          {
            "name": "text",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string",
                  "minLength": 1
                },
                {
                  "type": "null"
                }
              ],
              "description": "Substring of the title or the description, matched case-insensitively",
              "examples": [
                "выдача ключей"
              ],
              "title": "Text"
            },
            "description": "Substring of the title or the description, matched case-insensitively"
          },
          {
            "name": "X-Actor-Label",
            "in": "header",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Signature of a temporary agent, latin snake_case. Required with a shared agent token (one issued without a participant), ignored with a participant token",
              "examples": [
                "nightly_agent"
              ],
              "title": "X-Actor-Label"
            },
            "description": "Signature of a temporary agent, latin snake_case. Required with a shared agent token (one issued without a participant), ignored with a participant token"
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CollectionResponse_TaskSearchRead_"
                }
              }
            }
          },
          "401": {
            "description": "Token is missing, unknown or revoked",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "403": {
            "description": "Action is not allowed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "404": {
            "description": "Object not found",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "409": {
            "description": "State conflict",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "422": {
            "description": "Request validation failed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "500": {
            "description": "Unexpected error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/tasks/{task_key}": {
      "get": {
        "tags": [
          "tasks"
        ],
        "summary": "Read a task",
        "description": "Пакет преемника: всё, что нужно агенту с чистым контекстом, одним вызовом.\n\nКарточка, связи с обеих сторон со статусом задачи на другой стороне, вычисляемые\nпризнаки, последняя сводка целиком, открытые вопросы и неразобранные замечания\nцеликом, опись дела и переходы по таблице. Тела остальных записей читаются отдельно\nв `GET /tasks/{key}/entries`.\nПереходы перечислены по таблице; валидации (заполненные разделы, сводка, вердикты,\nблокеры, дети) проверяются в момент перехода, а не при чтении.",
        "operationId": "read_task",
        "security": [
          {
            "ApiToken": []
          }
        ],
        "parameters": [
          {
            "name": "task_key",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "description": "Task key `QUEUE-number`; matching ignores case",
              "examples": [
                "TRK-42"
              ],
              "title": "Task Key"
            },
            "description": "Task key `QUEUE-number`; matching ignores case"
          },
          {
            "name": "X-Actor-Label",
            "in": "header",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Signature of a temporary agent, latin snake_case. Required with a shared agent token (one issued without a participant), ignored with a participant token",
              "examples": [
                "nightly_agent"
              ],
              "title": "X-Actor-Label"
            },
            "description": "Signature of a temporary agent, latin snake_case. Required with a shared agent token (one issued without a participant), ignored with a participant token"
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DataResponse_TaskPackageRead_"
                }
              }
            }
          },
          "401": {
            "description": "Token is missing, unknown or revoked",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "403": {
            "description": "Action is not allowed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "404": {
            "description": "Object not found",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "409": {
            "description": "State conflict",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "422": {
            "description": "Request validation failed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "500": {
            "description": "Unexpected error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      },
      "patch": {
        "tags": [
          "tasks"
        ],
        "summary": "Update a task",
        "description": "Меняет только переданные поля.\n\nНазвание, описание и пять разделов — только в `backlog` (иначе `409\ntask_field_locked`); исполнитель и приоритет — в любом незакрытом статусе; в\n`done` и `cancelled` не меняется ничего (`409 task_closed`). Каждое изменение\nподшивает запись: раздел — `section_changed`, исполнитель — `assignee_changed`,\nприоритет — `field_changed`. Поля без записи не бывает: изменение, не\nоставившее записи, не доходит до ленты (`CONCEPT.md`, 4.1). `version` — не поле\nзадачи, а условие: устаревшая версия отвечает `409 version_conflict`.\n\nПроверку правят двумя способами: `check` меняет текст одной на месте, `checks`\nзаменяет список целиком и годится, когда меняется сам состав. Вместе они не\nпринимаются — это два разных ответа на один вопрос.",
        "operationId": "update_task",
        "security": [
          {
            "ApiToken": []
          }
        ],
        "parameters": [
          {
            "name": "task_key",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "description": "Task key `QUEUE-number`; matching ignores case",
              "examples": [
                "TRK-42"
              ],
              "title": "Task Key"
            },
            "description": "Task key `QUEUE-number`; matching ignores case"
          },
          {
            "name": "X-Actor-Label",
            "in": "header",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Signature of a temporary agent, latin snake_case. Required with a shared agent token (one issued without a participant), ignored with a participant token",
              "examples": [
                "nightly_agent"
              ],
              "title": "X-Actor-Label"
            },
            "description": "Signature of a temporary agent, latin snake_case. Required with a shared agent token (one issued without a participant), ignored with a participant token"
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/TaskUpdate"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DataResponse_TaskRead_"
                }
              }
            }
          },
          "401": {
            "description": "Token is missing, unknown or revoked",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "403": {
            "description": "Action is not allowed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "404": {
            "description": "Object not found",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "409": {
            "description": "State conflict",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "422": {
            "description": "Request validation failed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "500": {
            "description": "Unexpected error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/tasks/{task_key}/transition": {
      "post": {
        "tags": [
          "tasks"
        ],
        "summary": "Move a task to another status",
        "description": "Переводит задачу по таблице переходов.\n\nПереход не из таблицы — `409 transition_not_allowed` с допустимыми целями в\n`details.allowed`. Шаг назад по цепочке и отмена требуют `reason` (`422\ntransition_reason_required`); `backlog → open` требует заполненных разделов (`422\ntask_sections_incomplete`). Выход из `in_progress` требует сводки, подшитой после\nпоследнего входа в него (`409 summary_required`); `in_progress → done` —\nположительного последнего вердикта по каждой проверке, подшитого после последнего\nвхода в `in_progress` (`409 checks_not_passed`, незасчитанные проверки в\n`details.checks` парами `check_no` и `reason`). Вход в\n`in_progress` отклоняется при открытом блокере (`409 task_blocked`, их ключи в\n`details.blockers`), закрытие — и `done`, и `cancelled` — при детях не в `done` и\nне в `cancelled` (`409 task_has_unclosed_children`, ключи в `details.children`).\nПереход подшивает `status_changed` с `from`, `to` и `reason`.\n\nЦель `done` не принимается: закрытие подшивает вердикты и сводку и переводит задачу\nодной транзакцией, и у него свой маршрут — `409 closing_not_a_transition`.",
        "operationId": "transition_task",
        "security": [
          {
            "ApiToken": []
          }
        ],
        "parameters": [
          {
            "name": "task_key",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "description": "Task key `QUEUE-number`; matching ignores case",
              "examples": [
                "TRK-42"
              ],
              "title": "Task Key"
            },
            "description": "Task key `QUEUE-number`; matching ignores case"
          },
          {
            "name": "X-Actor-Label",
            "in": "header",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Signature of a temporary agent, latin snake_case. Required with a shared agent token (one issued without a participant), ignored with a participant token",
              "examples": [
                "nightly_agent"
              ],
              "title": "X-Actor-Label"
            },
            "description": "Signature of a temporary agent, latin snake_case. Required with a shared agent token (one issued without a participant), ignored with a participant token"
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/TaskTransition"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DataResponse_TaskRead_"
                }
              }
            }
          },
          "401": {
            "description": "Token is missing, unknown or revoked",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "403": {
            "description": "Action is not allowed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "404": {
            "description": "Object not found",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "409": {
            "description": "State conflict",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "422": {
            "description": "Request validation failed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "500": {
            "description": "Unexpected error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/tasks/{task_key}/close": {
      "post": {
        "tags": [
          "tasks"
        ],
        "summary": "Close a task",
        "description": "Подшивает записи, вердикты и финальную сводку и переводит задачу в `done` — всё\nодним запросом и одной транзакцией.\n\nЕдинственный путь в `done`: у перехода эта цель отвечает `409\nclosing_not_a_transition`. Частичного закрытия не бывает — отказ на любой части не\nоставляет в деле ни одной записи и статуса не меняет.\n\nПорядок подшивки: присланные записи, вердикты, сводка. Требования выхода прежние и\nпроверяются после подшивки: положительный последний вердикт по каждой проверке\nсреди подшитых после последнего входа в `in_progress` (`409 checks_not_passed`),\nзакрытые дети (`409 task_has_unclosed_children`), задача в `in_progress` (`409\ntransition_not_allowed`). Вердикты этого запроса засчитываются наравне с подшитыми\nраньше по ходу работы, поэтому список может быть пуст.\n\nПовтор с тем же `Idempotency-Key` отвечает первым результатом и второго закрытия не\nзаводит. В ответе — карточка задачи; подшитые записи читаются `GET\n/tasks/{task_key}/entries`.",
        "operationId": "close_task",
        "security": [
          {
            "ApiToken": []
          }
        ],
        "parameters": [
          {
            "name": "task_key",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "description": "Task key `QUEUE-number`; matching ignores case",
              "examples": [
                "TRK-42"
              ],
              "title": "Task Key"
            },
            "description": "Task key `QUEUE-number`; matching ignores case"
          },
          {
            "name": "X-Actor-Label",
            "in": "header",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Signature of a temporary agent, latin snake_case. Required with a shared agent token (one issued without a participant), ignored with a participant token",
              "examples": [
                "nightly_agent"
              ],
              "title": "X-Actor-Label"
            },
            "description": "Signature of a temporary agent, latin snake_case. Required with a shared agent token (one issued without a participant), ignored with a participant token"
          },
          {
            "name": "Idempotency-Key",
            "in": "header",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Makes this creating call safe to repeat. A retry with the same key and the same request answers with the first response instead of creating a second object; the same key with a different request answers 409 idempotency_key_reused. Keys are paired with the token, are at most 255 characters long and are forgotten after 24 hours",
              "examples": [
                "6b1f0c34-9b2e-4b0a-9a5f-3f1d6c8e0a11"
              ],
              "title": "Idempotency-Key"
            },
            "description": "Makes this creating call safe to repeat. A retry with the same key and the same request answers with the first response instead of creating a second object; the same key with a different request answers 409 idempotency_key_reused. Keys are paired with the token, are at most 255 characters long and are forgotten after 24 hours"
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/TaskClosing"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DataResponse_TaskRead_"
                }
              }
            }
          },
          "401": {
            "description": "Token is missing, unknown or revoked",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "403": {
            "description": "Action is not allowed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "404": {
            "description": "Object not found",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "409": {
            "description": "State conflict",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "422": {
            "description": "Request validation failed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "500": {
            "description": "Unexpected error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/tasks/{task_key}/entries": {
      "post": {
        "tags": [
          "tasks"
        ],
        "summary": "Append a case entry",
        "description": "Подшивает запись агента: сводку, решение, попытку, находку, артефакт, вопрос,\nответ, вердикт или заметку.\n\nФорма нагрузки зависит от типа: тело запроса — размеченное по `type` объединение.\nЗаголовок принимается только там, где его нечем вывести: у `summary` он равен\nпервой строке `done`, у `answer` и `verdict` собирается из нагрузки.\nСлужебные типы (`status_changed`, `created`, ...) подшивает сам трекер, и в запросе\nони не принимаются. Замечания к форме приходят разом в `422 entry_fields_invalid`,\nсписком `details.fields`. Записи неизменяемы, а в закрытую задачу подшиваются.\n\nПовтор с тем же `Idempotency-Key` отвечает первой записью: агент, упавший до ответа,\nне подшивает вторую копию своей сводки.",
        "operationId": "create_task_entry",
        "security": [
          {
            "ApiToken": []
          }
        ],
        "parameters": [
          {
            "name": "task_key",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "description": "Task key `QUEUE-number`; matching ignores case",
              "examples": [
                "TRK-42"
              ],
              "title": "Task Key"
            },
            "description": "Task key `QUEUE-number`; matching ignores case"
          },
          {
            "name": "X-Actor-Label",
            "in": "header",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Signature of a temporary agent, latin snake_case. Required with a shared agent token (one issued without a participant), ignored with a participant token",
              "examples": [
                "nightly_agent"
              ],
              "title": "X-Actor-Label"
            },
            "description": "Signature of a temporary agent, latin snake_case. Required with a shared agent token (one issued without a participant), ignored with a participant token"
          },
          {
            "name": "Idempotency-Key",
            "in": "header",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Makes this creating call safe to repeat. A retry with the same key and the same request answers with the first response instead of creating a second object; the same key with a different request answers 409 idempotency_key_reused. Keys are paired with the token, are at most 255 characters long and are forgotten after 24 hours",
              "examples": [
                "6b1f0c34-9b2e-4b0a-9a5f-3f1d6c8e0a11"
              ],
              "title": "Idempotency-Key"
            },
            "description": "Makes this creating call safe to repeat. A retry with the same key and the same request answers with the first response instead of creating a second object; the same key with a different request answers 409 idempotency_key_reused. Keys are paired with the token, are at most 255 characters long and are forgotten after 24 hours"
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "oneOf": [
                  {
                    "$ref": "#/components/schemas/PlainEntryCreate"
                  },
                  {
                    "$ref": "#/components/schemas/SummaryEntryCreate"
                  },
                  {
                    "$ref": "#/components/schemas/QuestionEntryCreate"
                  },
                  {
                    "$ref": "#/components/schemas/AnswerEntryCreate"
                  },
                  {
                    "$ref": "#/components/schemas/VerdictEntryCreate"
                  },
                  {
                    "$ref": "#/components/schemas/RemarkEntryCreate"
                  },
                  {
                    "$ref": "#/components/schemas/ResolutionEntryCreate"
                  }
                ],
                "discriminator": {
                  "propertyName": "type",
                  "mapping": {
                    "decision": "#/components/schemas/PlainEntryCreate",
                    "attempt": "#/components/schemas/PlainEntryCreate",
                    "finding": "#/components/schemas/PlainEntryCreate",
                    "artifact": "#/components/schemas/PlainEntryCreate",
                    "note": "#/components/schemas/PlainEntryCreate",
                    "summary": "#/components/schemas/SummaryEntryCreate",
                    "question": "#/components/schemas/QuestionEntryCreate",
                    "answer": "#/components/schemas/AnswerEntryCreate",
                    "verdict": "#/components/schemas/VerdictEntryCreate",
                    "remark": "#/components/schemas/RemarkEntryCreate",
                    "resolution": "#/components/schemas/ResolutionEntryCreate"
                  }
                },
                "title": "Entry"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DataResponse_EntryRead_"
                }
              }
            }
          },
          "401": {
            "description": "Token is missing, unknown or revoked",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "403": {
            "description": "Action is not allowed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "404": {
            "description": "Object not found",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "409": {
            "description": "State conflict",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "422": {
            "description": "Request validation failed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "500": {
            "description": "Unexpected error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      },
      "get": {
        "tags": [
          "tasks"
        ],
        "summary": "Read case entries",
        "description": "Записи дела с телами и нагрузкой, в порядке `no`.\n\nФильтры сужают выборку вместе: `types=summary` даёт все сводки дела, `after_no`\n— всё, что случилось после названной записи, и вместе они отвечают на вопрос «что\nпроизошло после последней сводки». `after_no` и `cursor` не спорят: первый задаёт\nклиент, второй продолжает страницу, действуют оба. Записи неизменяемы: маршрутов\nправки и удаления нет.",
        "operationId": "list_task_entries",
        "security": [
          {
            "ApiToken": []
          }
        ],
        "parameters": [
          {
            "name": "task_key",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "description": "Task key `QUEUE-number`; matching ignores case",
              "examples": [
                "TRK-42"
              ],
              "title": "Task Key"
            },
            "description": "Task key `QUEUE-number`; matching ignores case"
          },
          {
            "name": "nos",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "array",
                  "items": {
                    "type": "integer"
                  }
                },
                {
                  "type": "null"
                }
              ],
              "description": "Read only these entry numbers",
              "examples": [
                [
                  3,
                  12
                ]
              ],
              "title": "Nos"
            },
            "description": "Read only these entry numbers"
          },
          {
            "name": "types",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/EntryType"
                  }
                },
                {
                  "type": "null"
                }
              ],
              "description": "Read only entries of these types",
              "examples": [
                [
                  "summary"
                ]
              ],
              "title": "Types"
            },
            "description": "Read only entries of these types"
          },
          {
            "name": "after_no",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 1
                },
                {
                  "type": "null"
                }
              ],
              "description": "Read only entries after this number — what happened since",
              "examples": [
                12
              ],
              "title": "After No"
            },
            "description": "Read only entries after this number — what happened since"
          },
          {
            "name": "limit",
            "in": "query",
            "required": false,
            "schema": {
              "type": "integer",
              "maximum": 200,
              "minimum": 1,
              "description": "Page size",
              "default": 50,
              "title": "Limit"
            },
            "description": "Page size"
          },
          {
            "name": "cursor",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Cursor from `meta.next_cursor` of a previous page",
              "title": "Cursor"
            },
            "description": "Cursor from `meta.next_cursor` of a previous page"
          },
          {
            "name": "X-Actor-Label",
            "in": "header",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Signature of a temporary agent, latin snake_case. Required with a shared agent token (one issued without a participant), ignored with a participant token",
              "examples": [
                "nightly_agent"
              ],
              "title": "X-Actor-Label"
            },
            "description": "Signature of a temporary agent, latin snake_case. Required with a shared agent token (one issued without a participant), ignored with a participant token"
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CollectionResponse_EntryRead_"
                }
              }
            }
          },
          "401": {
            "description": "Token is missing, unknown or revoked",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "403": {
            "description": "Action is not allowed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "404": {
            "description": "Object not found",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "409": {
            "description": "State conflict",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "422": {
            "description": "Request validation failed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "500": {
            "description": "Unexpected error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/tasks/{task_key}/entries/{entry_no}": {
      "get": {
        "tags": [
          "tasks"
        ],
        "summary": "Read one case entry",
        "description": "Одна запись по номеру внутри задачи — адрес из ссылки `TRK-42#12`.\n\nНомера, которого в задаче нет, — `404 entry_not_found`: номера выдаются без дыр,\nпоэтому промах означает ссылку на чужое дело, а не пропущенную страницу.",
        "operationId": "read_task_entry",
        "security": [
          {
            "ApiToken": []
          }
        ],
        "parameters": [
          {
            "name": "task_key",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "description": "Task key `QUEUE-number`; matching ignores case",
              "examples": [
                "TRK-42"
              ],
              "title": "Task Key"
            },
            "description": "Task key `QUEUE-number`; matching ignores case"
          },
          {
            "name": "entry_no",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer",
              "minimum": 1,
              "description": "Entry number inside the task, from 1",
              "examples": [
                12
              ],
              "title": "Entry No"
            },
            "description": "Entry number inside the task, from 1"
          },
          {
            "name": "X-Actor-Label",
            "in": "header",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Signature of a temporary agent, latin snake_case. Required with a shared agent token (one issued without a participant), ignored with a participant token",
              "examples": [
                "nightly_agent"
              ],
              "title": "X-Actor-Label"
            },
            "description": "Signature of a temporary agent, latin snake_case. Required with a shared agent token (one issued without a participant), ignored with a participant token"
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DataResponse_EntryRead_"
                }
              }
            }
          },
          "401": {
            "description": "Token is missing, unknown or revoked",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "403": {
            "description": "Action is not allowed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "404": {
            "description": "Object not found",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "409": {
            "description": "State conflict",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "422": {
            "description": "Request validation failed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "500": {
            "description": "Unexpected error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/tasks/{task_key}/links": {
      "post": {
        "tags": [
          "links"
        ],
        "summary": "Link two tasks",
        "description": "Ставит связь между задачами и подшивает `link_added` в дела обеих.\n\nВид связи называет роль задачи из пути: `{\"kind\": \"blocks\", \"other\": \"TRK-7\"}` у\n`TRK-1` означает «TRK-1 блокирует TRK-7», и в карточке `TRK-7` та же связь\nпоказывается как `blocked_by TRK-1`. Хранится она одной строкой, поэтому повтор с\nдругой стороны — `409 link_exists`, а не вторая связь.\n\nОтказы: связь с самой собой — `422 link_self_not_allowed`; кольцо в иерархии или в\nблокировках — `409 link_cycle_detected` (виды не смешиваются: родитель, у которого\n`blocked_by` на своих детей, кольцом не считается); `parent` или `blocks` с задачей в\n`done` или `cancelled` с любой стороны — `409 task_closed`. `relates` с закрытой\nзадачей проходит: им связывают её с продолжением. Повтор с тем же `Idempotency-Key`\nотвечает первой связью, а не `409 link_exists`.",
        "operationId": "create_task_link",
        "security": [
          {
            "ApiToken": []
          }
        ],
        "parameters": [
          {
            "name": "task_key",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "description": "Task key `QUEUE-number`; matching ignores case",
              "examples": [
                "TRK-42"
              ],
              "title": "Task Key"
            },
            "description": "Task key `QUEUE-number`; matching ignores case"
          },
          {
            "name": "X-Actor-Label",
            "in": "header",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Signature of a temporary agent, latin snake_case. Required with a shared agent token (one issued without a participant), ignored with a participant token",
              "examples": [
                "nightly_agent"
              ],
              "title": "X-Actor-Label"
            },
            "description": "Signature of a temporary agent, latin snake_case. Required with a shared agent token (one issued without a participant), ignored with a participant token"
          },
          {
            "name": "Idempotency-Key",
            "in": "header",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Makes this creating call safe to repeat. A retry with the same key and the same request answers with the first response instead of creating a second object; the same key with a different request answers 409 idempotency_key_reused. Keys are paired with the token, are at most 255 characters long and are forgotten after 24 hours",
              "examples": [
                "6b1f0c34-9b2e-4b0a-9a5f-3f1d6c8e0a11"
              ],
              "title": "Idempotency-Key"
            },
            "description": "Makes this creating call safe to repeat. A retry with the same key and the same request answers with the first response instead of creating a second object; the same key with a different request answers 409 idempotency_key_reused. Keys are paired with the token, are at most 255 characters long and are forgotten after 24 hours"
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/LinkCreate"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DataResponse_TaskLinkRead_"
                }
              }
            }
          },
          "401": {
            "description": "Token is missing, unknown or revoked",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "403": {
            "description": "Action is not allowed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "404": {
            "description": "Object not found",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "409": {
            "description": "State conflict",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "422": {
            "description": "Request validation failed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "500": {
            "description": "Unexpected error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/tasks/{task_key}/links/{kind}/{other_key}": {
      "delete": {
        "tags": [
          "links"
        ],
        "summary": "Remove a link between two tasks",
        "description": "Снимает связь и подшивает `link_removed` в дела обеих задач.\n\nАдресуется связь так же, как ставилась, — видом со стороны задачи из пути. Снять её\nможно с любой стороны: `blocks` у одной и `blocked_by` у другой — одна строка.\nСвязи нет — `404 link_not_found`; `parent` или `blocks` у закрытой задачи —\n`409 task_closed`, `relates` снимается и у закрытой.",
        "operationId": "delete_task_link",
        "security": [
          {
            "ApiToken": []
          }
        ],
        "parameters": [
          {
            "name": "task_key",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "description": "Task key `QUEUE-number`; matching ignores case",
              "examples": [
                "TRK-42"
              ],
              "title": "Task Key"
            },
            "description": "Task key `QUEUE-number`; matching ignores case"
          },
          {
            "name": "kind",
            "in": "path",
            "required": true,
            "schema": {
              "$ref": "#/components/schemas/LinkKind",
              "description": "Link kind as seen from the task in the path, not from the other one",
              "examples": [
                "blocks"
              ]
            },
            "description": "Link kind as seen from the task in the path, not from the other one"
          },
          {
            "name": "other_key",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "description": "Key of the task on the other side; matching ignores case",
              "examples": [
                "TRK-7"
              ],
              "title": "Other Key"
            },
            "description": "Key of the task on the other side; matching ignores case"
          },
          {
            "name": "X-Actor-Label",
            "in": "header",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Signature of a temporary agent, latin snake_case. Required with a shared agent token (one issued without a participant), ignored with a participant token",
              "examples": [
                "nightly_agent"
              ],
              "title": "X-Actor-Label"
            },
            "description": "Signature of a temporary agent, latin snake_case. Required with a shared agent token (one issued without a participant), ignored with a participant token"
          }
        ],
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "401": {
            "description": "Token is missing, unknown or revoked",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "403": {
            "description": "Action is not allowed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "404": {
            "description": "Object not found",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "409": {
            "description": "State conflict",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "422": {
            "description": "Request validation failed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "500": {
            "description": "Unexpected error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/questions": {
      "get": {
        "tags": [
          "questions"
        ],
        "summary": "List questions",
        "description": "Вопросы всех задач с фильтрами; по умолчанию — открытые вопросы текущего участника.\n\nПорядок — от самого старого: дольше всех ждёт ответа тот, кого задали первым.\nОтвет на вопрос убирает его из выдачи, потому что открытость считается по делу, а\nне хранится флагом. Адресовать можно только участника реестра, поэтому запрос общим\nагентским токеном без явного `addressee` — `422 actor_not_addressable`: пустой\nсписок молча соврал бы, что вопросов не пришло.",
        "operationId": "list_questions",
        "security": [
          {
            "ApiToken": []
          }
        ],
        "parameters": [
          {
            "name": "addressee",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Participant the question is addressed to; matching ignores case. Defaults to the participant whose token made the request",
              "examples": [
                "owner"
              ],
              "title": "Addressee"
            },
            "description": "Participant the question is addressed to; matching ignores case. Defaults to the participant whose token made the request"
          },
          {
            "name": "queue",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Queue key of the question's task; matching ignores case",
              "examples": [
                "TRK"
              ],
              "title": "Queue"
            },
            "description": "Queue key of the question's task; matching ignores case"
          },
          {
            "name": "blocking",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "boolean"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Keep only questions that do (or do not) block the work",
              "title": "Blocking"
            },
            "description": "Keep only questions that do (or do not) block the work"
          },
          {
            "name": "open",
            "in": "query",
            "required": false,
            "schema": {
              "type": "boolean",
              "description": "true (the default) keeps only questions with no answer yet; false drops the filter and returns every question, answered or not. To read the questions of one task use its case with `types=question`",
              "default": true,
              "title": "Open"
            },
            "description": "true (the default) keeps only questions with no answer yet; false drops the filter and returns every question, answered or not. To read the questions of one task use its case with `types=question`"
          },
          {
            "name": "limit",
            "in": "query",
            "required": false,
            "schema": {
              "type": "integer",
              "maximum": 200,
              "minimum": 1,
              "description": "Page size",
              "default": 50,
              "title": "Limit"
            },
            "description": "Page size"
          },
          {
            "name": "cursor",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Cursor from `meta.next_cursor` of a previous page",
              "title": "Cursor"
            },
            "description": "Cursor from `meta.next_cursor` of a previous page"
          },
          {
            "name": "X-Actor-Label",
            "in": "header",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Signature of a temporary agent, latin snake_case. Required with a shared agent token (one issued without a participant), ignored with a participant token",
              "examples": [
                "nightly_agent"
              ],
              "title": "X-Actor-Label"
            },
            "description": "Signature of a temporary agent, latin snake_case. Required with a shared agent token (one issued without a participant), ignored with a participant token"
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CollectionResponse_QuestionEntryRead_"
                }
              }
            }
          },
          "401": {
            "description": "Token is missing, unknown or revoked",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "403": {
            "description": "Action is not allowed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "404": {
            "description": "Object not found",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "409": {
            "description": "State conflict",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "422": {
            "description": "Request validation failed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "500": {
            "description": "Unexpected error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/remarks": {
      "get": {
        "tags": [
          "remarks"
        ],
        "summary": "List remarks",
        "description": "Замечания всех задач с фильтрами; по умолчанию — все неразобранные.\n\nПорядок — от самого старого: дольше всех ждёт разбора то, что оставили первым.\nРезолюция убирает замечание из выдачи, потому что открытость считается по делу, а не\nхранится флагом; из дела оно при этом никуда не девается.\n\nАвтор не проверяется по реестру: подписью бывает и метка временного агента, которой\nв реестре нет. Неизвестная подпись поэтому даёт пустой список, а не отказ.",
        "operationId": "list_remarks",
        "security": [
          {
            "ApiToken": []
          }
        ],
        "parameters": [
          {
            "name": "author",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Signature the remark is filed under: a participant name or a temporary agent label; matching ignores case. Omit to get remarks by anyone",
              "examples": [
                "owner"
              ],
              "title": "Author"
            },
            "description": "Signature the remark is filed under: a participant name or a temporary agent label; matching ignores case. Omit to get remarks by anyone"
          },
          {
            "name": "queue",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Queue key of the remark's task; matching ignores case",
              "examples": [
                "TRK"
              ],
              "title": "Queue"
            },
            "description": "Queue key of the remark's task; matching ignores case"
          },
          {
            "name": "open",
            "in": "query",
            "required": false,
            "schema": {
              "type": "boolean",
              "description": "true (the default) keeps only remarks with no resolution yet; false drops the filter and returns every remark, resolved or not. To read the remarks of one task use its case with `types=remark`",
              "default": true,
              "title": "Open"
            },
            "description": "true (the default) keeps only remarks with no resolution yet; false drops the filter and returns every remark, resolved or not. To read the remarks of one task use its case with `types=remark`"
          },
          {
            "name": "limit",
            "in": "query",
            "required": false,
            "schema": {
              "type": "integer",
              "maximum": 200,
              "minimum": 1,
              "description": "Page size",
              "default": 50,
              "title": "Limit"
            },
            "description": "Page size"
          },
          {
            "name": "cursor",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Cursor from `meta.next_cursor` of a previous page",
              "title": "Cursor"
            },
            "description": "Cursor from `meta.next_cursor` of a previous page"
          },
          {
            "name": "X-Actor-Label",
            "in": "header",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Signature of a temporary agent, latin snake_case. Required with a shared agent token (one issued without a participant), ignored with a participant token",
              "examples": [
                "nightly_agent"
              ],
              "title": "X-Actor-Label"
            },
            "description": "Signature of a temporary agent, latin snake_case. Required with a shared agent token (one issued without a participant), ignored with a participant token"
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CollectionResponse_RemarkEntryRead_"
                }
              }
            }
          },
          "401": {
            "description": "Token is missing, unknown or revoked",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "403": {
            "description": "Action is not allowed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "404": {
            "description": "Object not found",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "409": {
            "description": "State conflict",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "422": {
            "description": "Request validation failed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "500": {
            "description": "Unexpected error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/journal": {
      "get": {
        "tags": [
          "journal"
        ],
        "summary": "Read the journal tail",
        "description": "Записи журнала после сквозного номера `after`, по возрастанию `seq`.\n\nОтдельной таблицы событий в трекере нет: журнал всех записей дела и есть лента для\nвнешнего мира (`CONCEPT.md`, 4.1). Записи постоянны, поэтому догнать ленту можно с\nлюбого номера, а срока хранения и «слишком старого» курсора не существует.\n\nОтсюда строится цикл назначателя и живого харнесса: «прочитать хвост с курсора,\nпересчитать кандидатов, запустить харнесс». Различать записи для этого не нужно —\nпересчёт дёшев.\n\n`wait` превращает чтение в долгое ожидание: ответ приходит, как только появилась\nпервая подходящая запись, и не позже, чем через указанное число секунд. Ожидание\nпостроено на оповещениях PostgreSQL, а не на опросе, и не держит соединение с базой.\nПустой список по истечении ожидания означает «ничего не случилось» — это не ошибка.\nУшедший клиент ожидание прекращает: сценарию передаётся `Request.is_disconnected`,\nи брошенный запрос отпускает соединение, не досиживая до конца `wait`.\n\nФильтры складываются по «и» и совпадают с фильтрами потока. `after` и `cursor` — не\nдубль: первый задаёт клиент, второй продолжает страницу; действуют оба, побеждает\nбольший.",
        "operationId": "read_journal",
        "security": [
          {
            "ApiToken": []
          }
        ],
        "parameters": [
          {
            "name": "after",
            "in": "query",
            "required": false,
            "schema": {
              "type": "integer",
              "minimum": 0,
              "description": "Read only entries after this tracker-wide sequence number. 0 means from the very beginning: entries are permanent, so any number is a valid position",
              "examples": [
                1024
              ],
              "default": 0,
              "title": "After"
            },
            "description": "Read only entries after this tracker-wide sequence number. 0 means from the very beginning: entries are permanent, so any number is a valid position"
          },
          {
            "name": "task",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Only entries of this task; matching ignores case",
              "examples": [
                "TRK-42"
              ],
              "title": "Task"
            },
            "description": "Only entries of this task; matching ignores case"
          },
          {
            "name": "queue",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Only entries of tasks in this queue; matching ignores case",
              "examples": [
                "TRK"
              ],
              "title": "Queue"
            },
            "description": "Only entries of tasks in this queue; matching ignores case"
          },
          {
            "name": "types",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/EntryType"
                  }
                },
                {
                  "type": "null"
                }
              ],
              "description": "Only entries of these types",
              "examples": [
                [
                  "answer",
                  "status_changed"
                ]
              ],
              "title": "Types"
            },
            "description": "Only entries of these types"
          },
          {
            "name": "limit",
            "in": "query",
            "required": false,
            "schema": {
              "type": "integer",
              "maximum": 200,
              "minimum": 1,
              "description": "Page size",
              "default": 50,
              "title": "Limit"
            },
            "description": "Page size"
          },
          {
            "name": "cursor",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Cursor from `meta.next_cursor` of a previous page",
              "title": "Cursor"
            },
            "description": "Cursor from `meta.next_cursor` of a previous page"
          },
          {
            "name": "wait",
            "in": "query",
            "required": false,
            "schema": {
              "type": "number",
              "maximum": 60.0,
              "minimum": 0,
              "description": "Seconds to wait for a new entry when the tail is empty, at most 60. 0 answers right away. The wait is woken by a PostgreSQL notification, not by polling, and returns as soon as the first matching entry is committed",
              "examples": [
                30
              ],
              "default": 0.0,
              "title": "Wait"
            },
            "description": "Seconds to wait for a new entry when the tail is empty, at most 60. 0 answers right away. The wait is woken by a PostgreSQL notification, not by polling, and returns as soon as the first matching entry is committed"
          },
          {
            "name": "X-Actor-Label",
            "in": "header",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Signature of a temporary agent, latin snake_case. Required with a shared agent token (one issued without a participant), ignored with a participant token",
              "examples": [
                "nightly_agent"
              ],
              "title": "X-Actor-Label"
            },
            "description": "Signature of a temporary agent, latin snake_case. Required with a shared agent token (one issued without a participant), ignored with a participant token"
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CollectionResponse_EntryRead_"
                }
              }
            }
          },
          "401": {
            "description": "Token is missing, unknown or revoked",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "403": {
            "description": "Action is not allowed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "404": {
            "description": "Object not found",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "409": {
            "description": "State conflict",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "422": {
            "description": "Request validation failed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "500": {
            "description": "Unexpected error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/journal/stream": {
      "get": {
        "tags": [
          "journal"
        ],
        "summary": "Stream the journal as it grows",
        "description": "Тот же хвост журнала, что отдаёт лента, но потоком по мере появления записей.\n\nФильтры те же, что у `GET /api/v1/journal`, и складываются так же. Кадр несёт `seq`\nзаписи в `id:`, тип записи в `event:` и саму запись в `data:` — той же формы, что и\nэлемент ленты.\n\nНачало выдачи: без курсора — конец журнала, то есть «что будет дальше». С курсором\n(`Last-Event-ID`, параметр `last_event_id` или `after`) — сразу после названного\nномера. Слишком старого курсора не бывает: записи постоянны, и поток, оборванный на\nкадре `id: 17`, переоткрытый с `Last-Event-ID: 17`, продолжает с 18 без пропусков и\nповторов. Отказ здесь один — `Last-Event-ID`, который не разбирается как\nнеотрицательное целое: молчаливый старт с конца заставил бы клиента считать, что за\nвремя обрыва ничего не произошло.\n\nЧисло одновременно открытых потоков на процесс ограничено настройкой; сверх неё —\n`429`, который клиент повторяет через паузу.",
        "operationId": "stream_journal",
        "security": [
          {
            "ApiToken": []
          }
        ],
        "parameters": [
          {
            "name": "task",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Only entries of this task; matching ignores case",
              "examples": [
                "TRK-42"
              ],
              "title": "Task"
            },
            "description": "Only entries of this task; matching ignores case"
          },
          {
            "name": "queue",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Only entries of tasks in this queue; matching ignores case",
              "examples": [
                "TRK"
              ],
              "title": "Queue"
            },
            "description": "Only entries of tasks in this queue; matching ignores case"
          },
          {
            "name": "types",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/EntryType"
                  }
                },
                {
                  "type": "null"
                }
              ],
              "description": "Only entries of these types",
              "examples": [
                [
                  "answer",
                  "status_changed"
                ]
              ],
              "title": "Types"
            },
            "description": "Only entries of these types"
          },
          {
            "name": "after",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 0,
                  "description": "Read only entries after this tracker-wide sequence number. 0 means from the very beginning: entries are permanent, so any number is a valid position",
                  "examples": [
                    1024
                  ]
                },
                {
                  "type": "null"
                }
              ],
              "title": "After"
            }
          },
          {
            "name": "last_event_id",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Same as the `Last-Event-ID` header, for clients that cannot set it",
              "title": "Last Event Id"
            },
            "description": "Same as the `Last-Event-ID` header, for clients that cannot set it"
          },
          {
            "name": "Last-Event-ID",
            "in": "header",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "`seq` of the last frame received; the stream resumes right after it",
              "title": "Last-Event-Id"
            },
            "description": "`seq` of the last frame received; the stream resumes right after it"
          },
          {
            "name": "X-Actor-Label",
            "in": "header",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Signature of a temporary agent, latin snake_case. Required with a shared agent token (one issued without a participant), ignored with a participant token",
              "examples": [
                "nightly_agent"
              ],
              "title": "X-Actor-Label"
            },
            "description": "Signature of a temporary agent, latin snake_case. Required with a shared agent token (one issued without a participant), ignored with a participant token"
          }
        ],
        "responses": {
          "200": {
            "description": "An endless `text/event-stream`. Every frame carries the entry `seq` in `id:`, the entry type in `event:` and the entry itself in `data:`. Lines starting with `:` are heartbeats and carry no data",
            "content": {
              "text/event-stream": {
                "schema": {
                  "discriminator": {
                    "mapping": {
                      "answer": "#/components/schemas/AnswerEntryRead",
                      "artifact": "#/components/schemas/PlainEntryRead",
                      "assignee_changed": "#/components/schemas/AssigneeChangedEntryRead",
                      "attempt": "#/components/schemas/PlainEntryRead",
                      "created": "#/components/schemas/PlainEntryRead",
                      "decision": "#/components/schemas/PlainEntryRead",
                      "field_changed": "#/components/schemas/FieldChangedEntryRead",
                      "finding": "#/components/schemas/PlainEntryRead",
                      "link_added": "#/components/schemas/LinkEntryRead",
                      "link_removed": "#/components/schemas/LinkEntryRead",
                      "note": "#/components/schemas/PlainEntryRead",
                      "question": "#/components/schemas/QuestionEntryRead",
                      "remark": "#/components/schemas/RemarkEntryRead",
                      "resolution": "#/components/schemas/ResolutionEntryRead",
                      "section_changed": "#/components/schemas/SectionChangedEntryRead",
                      "status_changed": "#/components/schemas/StatusChangedEntryRead",
                      "summary": "#/components/schemas/SummaryEntryRead",
                      "verdict": "#/components/schemas/VerdictEntryRead"
                    },
                    "propertyName": "type"
                  },
                  "oneOf": [
                    {
                      "$ref": "#/components/schemas/PlainEntryRead"
                    },
                    {
                      "$ref": "#/components/schemas/SummaryEntryRead"
                    },
                    {
                      "$ref": "#/components/schemas/QuestionEntryRead"
                    },
                    {
                      "$ref": "#/components/schemas/AnswerEntryRead"
                    },
                    {
                      "$ref": "#/components/schemas/VerdictEntryRead"
                    },
                    {
                      "$ref": "#/components/schemas/RemarkEntryRead"
                    },
                    {
                      "$ref": "#/components/schemas/ResolutionEntryRead"
                    },
                    {
                      "$ref": "#/components/schemas/StatusChangedEntryRead"
                    },
                    {
                      "$ref": "#/components/schemas/SectionChangedEntryRead"
                    },
                    {
                      "$ref": "#/components/schemas/FieldChangedEntryRead"
                    },
                    {
                      "$ref": "#/components/schemas/AssigneeChangedEntryRead"
                    },
                    {
                      "$ref": "#/components/schemas/LinkEntryRead"
                    }
                  ]
                }
              }
            }
          },
          "401": {
            "description": "Token is missing, unknown or revoked",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "403": {
            "description": "Action is not allowed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "404": {
            "description": "Object not found",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "409": {
            "description": "State conflict",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "422": {
            "description": "Malformed `Last-Event-ID`",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "500": {
            "description": "Unexpected error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "429": {
            "description": "Too many open streams on this server",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    }
  },
  "components": {
    "schemas": {
      "AnswerEntryCreate": {
        "properties": {
          "body": {
            "type": "string",
            "maxLength": 65536,
            "title": "Body",
            "description": "Markdown body of the entry",
            "default": ""
          },
          "refs": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "maxItems": 50,
            "title": "Refs",
            "description": "References to entries `KEY-N#M`, tasks `KEY-N` and addresses. Entry and task references must exist; addresses are not checked",
            "examples": [
              []
            ]
          },
          "type": {
            "type": "string",
            "const": "answer",
            "title": "Type"
          },
          "payload": {
            "$ref": "#/components/schemas/AnswerPayload"
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "type",
          "payload"
        ],
        "title": "AnswerEntryCreate",
        "description": "Ответ. Заголовок не принимается: он собирается из ссылки на вопрос."
      },
      "AnswerEntryRead": {
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid",
            "title": "Id"
          },
          "seq": {
            "type": "integer",
            "title": "Seq",
            "description": "Tracker-wide monotonic number; journal cursor",
            "examples": [
              1024
            ]
          },
          "no": {
            "type": "integer",
            "title": "No",
            "description": "Number inside the task, from 1; `TRK-42#12`",
            "examples": [
              12
            ]
          },
          "task_key": {
            "type": "string",
            "title": "Task Key",
            "examples": [
              "TRK-42"
            ]
          },
          "author": {
            "$ref": "#/components/schemas/AuthorRead"
          },
          "title": {
            "type": "string",
            "title": "Title",
            "description": "One line; this is what the case index shows",
            "examples": [
              "Status changed: backlog -> open"
            ]
          },
          "body": {
            "type": "string",
            "title": "Body",
            "description": "Markdown; empty for service entries, whose content is the payload",
            "examples": [
              ""
            ]
          },
          "refs": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Refs",
            "description": "References to entries `KEY-N#M`, tasks `KEY-N` and addresses. Entry and task references must exist; addresses are not checked",
            "examples": [
              [
                "TRK-42#3",
                "TRK-7"
              ]
            ]
          },
          "created_at": {
            "type": "string",
            "format": "date-time",
            "title": "Created At"
          },
          "type": {
            "type": "string",
            "const": "answer",
            "title": "Type"
          },
          "payload": {
            "$ref": "#/components/schemas/AnswerPayload"
          }
        },
        "type": "object",
        "required": [
          "id",
          "seq",
          "no",
          "task_key",
          "author",
          "title",
          "body",
          "created_at",
          "type",
          "payload"
        ],
        "title": "AnswerEntryRead",
        "description": "Ответ на вопрос. Ответить может кто угодно; первый ответ закрывает вопрос."
      },
      "AnswerFactsRead": {
        "properties": {
          "type": {
            "type": "string",
            "const": "answer",
            "title": "Type"
          },
          "question_no": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Question No",
            "description": "Number of the question answered",
            "examples": [
              7
            ]
          }
        },
        "type": "object",
        "required": [
          "type"
        ],
        "title": "AnswerFactsRead",
        "description": "Ответ: на какой вопрос той же задачи."
      },
      "AnswerPayload": {
        "properties": {
          "question_no": {
            "type": "integer",
            "minimum": 1.0,
            "title": "Question No",
            "description": "Number of a `question` entry of the same task",
            "examples": [
              7
            ]
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "question_no"
        ],
        "title": "AnswerPayload",
        "description": "Ответ на вопрос той же задачи."
      },
      "AssigneeChangedEntryRead": {
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid",
            "title": "Id"
          },
          "seq": {
            "type": "integer",
            "title": "Seq",
            "description": "Tracker-wide monotonic number; journal cursor",
            "examples": [
              1024
            ]
          },
          "no": {
            "type": "integer",
            "title": "No",
            "description": "Number inside the task, from 1; `TRK-42#12`",
            "examples": [
              12
            ]
          },
          "task_key": {
            "type": "string",
            "title": "Task Key",
            "examples": [
              "TRK-42"
            ]
          },
          "author": {
            "$ref": "#/components/schemas/AuthorRead"
          },
          "title": {
            "type": "string",
            "title": "Title",
            "description": "One line; this is what the case index shows",
            "examples": [
              "Status changed: backlog -> open"
            ]
          },
          "body": {
            "type": "string",
            "title": "Body",
            "description": "Markdown; empty for service entries, whose content is the payload",
            "examples": [
              ""
            ]
          },
          "refs": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Refs",
            "description": "References to entries `KEY-N#M`, tasks `KEY-N` and addresses. Entry and task references must exist; addresses are not checked",
            "examples": [
              [
                "TRK-42#3",
                "TRK-7"
              ]
            ]
          },
          "created_at": {
            "type": "string",
            "format": "date-time",
            "title": "Created At"
          },
          "type": {
            "type": "string",
            "const": "assignee_changed",
            "title": "Type"
          },
          "payload": {
            "$ref": "#/components/schemas/AssigneeChangedPayload"
          }
        },
        "type": "object",
        "required": [
          "id",
          "seq",
          "no",
          "task_key",
          "author",
          "title",
          "body",
          "created_at",
          "type",
          "payload"
        ],
        "title": "AssigneeChangedEntryRead",
        "description": "Служебная запись о смене исполнителя."
      },
      "AssigneeChangedFactsRead": {
        "properties": {
          "type": {
            "type": "string",
            "const": "assignee_changed",
            "title": "Type"
          },
          "assignee_from": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Assignee From",
            "description": "Assignee before, null if there was none"
          },
          "assignee_to": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Assignee To",
            "description": "Assignee after, null if unassigned"
          }
        },
        "type": "object",
        "required": [
          "type"
        ],
        "title": "AssigneeChangedFactsRead",
        "description": "Смена исполнителя: имена участников коротки и видны прямо в описи."
      },
      "AssigneeChangedPayload": {
        "properties": {
          "before": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Before",
            "examples": [
              null
            ]
          },
          "after": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "After",
            "examples": [
              "release_bot"
            ]
          }
        },
        "additionalProperties": false,
        "type": "object",
        "title": "AssigneeChangedPayload",
        "description": "Смена исполнителя. `null` с любой стороны означает «исполнителя не было»."
      },
      "AuthorKind": {
        "type": "string",
        "enum": [
          "agent",
          "human",
          "tracker"
        ],
        "title": "AuthorKind",
        "description": "Кто именно сделал действие.\n\n`tracker` — сам трекер: этим родом подписаны служебные записи дела и строки,\nзаведённые командой первичной инициализации. У него нет подписи, потому что\nназывать себя по имени ему незачем: трекер в установке один."
      },
      "AuthorRead": {
        "properties": {
          "kind": {
            "$ref": "#/components/schemas/AuthorKind",
            "examples": [
              "agent"
            ]
          },
          "signature": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Signature",
            "description": "Participant name or temporary agent label; null for the tracker itself",
            "examples": [
              "release_bot"
            ]
          }
        },
        "type": "object",
        "required": [
          "kind"
        ],
        "title": "AuthorRead",
        "description": "Кто сделал действие: род и подпись.\n\nПодпись пуста только у рода `tracker` — так подписано то, что трекер сделал сам.\nУ человека и агента это имя участника, у временного агента — его метка из заголовка\n`X-Actor-Label`; отличить одно от другого по подписи нельзя намеренно: читающему\nдело важно, кто говорит, а не как он аутентифицировался."
      },
      "BootstrapRead": {
        "properties": {
          "participant": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/ParticipantRead"
              },
              {
                "type": "null"
              }
            ],
            "description": "Participant behind the token; null for a shared agent token, whose author is a temporary agent and has no registry entry"
          },
          "queues": {
            "items": {
              "$ref": "#/components/schemas/QueueRead"
            },
            "type": "array",
            "title": "Queues",
            "description": "Queues of the installation, one page capped at the common page ceiling. An installation with more queues than that pages `GET /api/v1/queues`"
          },
          "open_questions": {
            "type": "integer",
            "title": "Open Questions",
            "description": "Questions with no answer yet addressed to `participant`. Zero with a shared agent token: a temporary agent cannot be addressed at all",
            "examples": [
              3
            ]
          }
        },
        "type": "object",
        "required": [
          "queues",
          "open_questions"
        ],
        "title": "BootstrapRead",
        "description": "Всё, что нужно интерфейсу до первой отрисовки, и ничего сверх этого."
      },
      "CheckUpdate": {
        "properties": {
          "no": {
            "type": "integer",
            "minimum": 1.0,
            "title": "No",
            "description": "Position in the current `checks` list, numbered from 1",
            "examples": [
              3
            ]
          },
          "text": {
            "type": "string",
            "maxLength": 2000,
            "minLength": 1,
            "title": "Text",
            "description": "New wording of that check; the rest of the list stays byte for byte",
            "examples": [
              "`docker compose run --rm test`: the whole suite is green"
            ]
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "no",
          "text"
        ],
        "title": "CheckUpdate",
        "description": "Правка одной проверки: её номер и новый текст.\n\nЗаведена затем, что список правился только целиком: чтобы поменять третью строку из\nвосьми, приходилось переслать все восемь, и опечатка в неизменённых семи проходила\nмолча. Пересылка списка осталась и лишней не стала — она единственный способ\nизменить **состав**: добавить проверку, снять или переставить."
      },
      "ClosingEntryCreate": {
        "oneOf": [
          {
            "$ref": "#/components/schemas/PlainEntryCreate"
          },
          {
            "$ref": "#/components/schemas/RemarkEntryCreate"
          }
        ],
        "discriminator": {
          "propertyName": "type",
          "mapping": {
            "artifact": "#/components/schemas/PlainEntryCreate",
            "attempt": "#/components/schemas/PlainEntryCreate",
            "decision": "#/components/schemas/PlainEntryCreate",
            "finding": "#/components/schemas/PlainEntryCreate",
            "note": "#/components/schemas/PlainEntryCreate",
            "remark": "#/components/schemas/RemarkEntryCreate"
          }
        }
      },
      "CollectionResponse_EntryRead_": {
        "properties": {
          "data": {
            "items": {
              "$ref": "#/components/schemas/EntryRead"
            },
            "type": "array",
            "title": "Data"
          },
          "meta": {
            "$ref": "#/components/schemas/PageMeta"
          }
        },
        "type": "object",
        "required": [
          "data"
        ],
        "title": "CollectionResponse[EntryRead]"
      },
      "CollectionResponse_ParticipantRead_": {
        "properties": {
          "data": {
            "items": {
              "$ref": "#/components/schemas/ParticipantRead"
            },
            "type": "array",
            "title": "Data"
          },
          "meta": {
            "$ref": "#/components/schemas/PageMeta"
          }
        },
        "type": "object",
        "required": [
          "data"
        ],
        "title": "CollectionResponse[ParticipantRead]"
      },
      "CollectionResponse_QuestionEntryRead_": {
        "properties": {
          "data": {
            "items": {
              "$ref": "#/components/schemas/QuestionEntryRead"
            },
            "type": "array",
            "title": "Data"
          },
          "meta": {
            "$ref": "#/components/schemas/PageMeta"
          }
        },
        "type": "object",
        "required": [
          "data"
        ],
        "title": "CollectionResponse[QuestionEntryRead]"
      },
      "CollectionResponse_QueueRead_": {
        "properties": {
          "data": {
            "items": {
              "$ref": "#/components/schemas/QueueRead"
            },
            "type": "array",
            "title": "Data"
          },
          "meta": {
            "$ref": "#/components/schemas/PageMeta"
          }
        },
        "type": "object",
        "required": [
          "data"
        ],
        "title": "CollectionResponse[QueueRead]"
      },
      "CollectionResponse_RemarkEntryRead_": {
        "properties": {
          "data": {
            "items": {
              "$ref": "#/components/schemas/RemarkEntryRead"
            },
            "type": "array",
            "title": "Data"
          },
          "meta": {
            "$ref": "#/components/schemas/PageMeta"
          }
        },
        "type": "object",
        "required": [
          "data"
        ],
        "title": "CollectionResponse[RemarkEntryRead]"
      },
      "CollectionResponse_TaskSearchRead_": {
        "properties": {
          "data": {
            "items": {
              "$ref": "#/components/schemas/TaskSearchRead"
            },
            "type": "array",
            "title": "Data"
          },
          "meta": {
            "$ref": "#/components/schemas/PageMeta"
          }
        },
        "type": "object",
        "required": [
          "data"
        ],
        "title": "CollectionResponse[TaskSearchRead]"
      },
      "CollectionResponse_TokenRead_": {
        "properties": {
          "data": {
            "items": {
              "$ref": "#/components/schemas/TokenRead"
            },
            "type": "array",
            "title": "Data"
          },
          "meta": {
            "$ref": "#/components/schemas/PageMeta"
          }
        },
        "type": "object",
        "required": [
          "data"
        ],
        "title": "CollectionResponse[TokenRead]"
      },
      "DataResponse_BootstrapRead_": {
        "properties": {
          "data": {
            "$ref": "#/components/schemas/BootstrapRead"
          }
        },
        "type": "object",
        "required": [
          "data"
        ],
        "title": "DataResponse[BootstrapRead]"
      },
      "DataResponse_EntryRead_": {
        "properties": {
          "data": {
            "$ref": "#/components/schemas/EntryRead"
          }
        },
        "type": "object",
        "required": [
          "data"
        ],
        "title": "DataResponse[EntryRead]"
      },
      "DataResponse_ParticipantRead_": {
        "properties": {
          "data": {
            "$ref": "#/components/schemas/ParticipantRead"
          }
        },
        "type": "object",
        "required": [
          "data"
        ],
        "title": "DataResponse[ParticipantRead]"
      },
      "DataResponse_QueueRead_": {
        "properties": {
          "data": {
            "$ref": "#/components/schemas/QueueRead"
          }
        },
        "type": "object",
        "required": [
          "data"
        ],
        "title": "DataResponse[QueueRead]"
      },
      "DataResponse_TaskLinkRead_": {
        "properties": {
          "data": {
            "$ref": "#/components/schemas/TaskLinkRead"
          }
        },
        "type": "object",
        "required": [
          "data"
        ],
        "title": "DataResponse[TaskLinkRead]"
      },
      "DataResponse_TaskPackageRead_": {
        "properties": {
          "data": {
            "$ref": "#/components/schemas/TaskPackageRead"
          }
        },
        "type": "object",
        "required": [
          "data"
        ],
        "title": "DataResponse[TaskPackageRead]"
      },
      "DataResponse_TaskRead_": {
        "properties": {
          "data": {
            "$ref": "#/components/schemas/TaskRead"
          }
        },
        "type": "object",
        "required": [
          "data"
        ],
        "title": "DataResponse[TaskRead]"
      },
      "DataResponse_TokenIssued_": {
        "properties": {
          "data": {
            "$ref": "#/components/schemas/TokenIssued"
          }
        },
        "type": "object",
        "required": [
          "data"
        ],
        "title": "DataResponse[TokenIssued]"
      },
      "EmptyPayload": {
        "properties": {},
        "additionalProperties": false,
        "type": "object",
        "title": "EmptyPayload",
        "description": "Нагрузки нет: всё содержание записи в её заголовке, теле и ссылках."
      },
      "EntryFactsRead": {
        "oneOf": [
          {
            "$ref": "#/components/schemas/NoFactsRead"
          },
          {
            "$ref": "#/components/schemas/StatusChangedFactsRead"
          },
          {
            "$ref": "#/components/schemas/SectionChangedFactsRead"
          },
          {
            "$ref": "#/components/schemas/FieldChangedFactsRead"
          },
          {
            "$ref": "#/components/schemas/AssigneeChangedFactsRead"
          },
          {
            "$ref": "#/components/schemas/LinkFactsRead"
          },
          {
            "$ref": "#/components/schemas/QuestionFactsRead"
          },
          {
            "$ref": "#/components/schemas/AnswerFactsRead"
          },
          {
            "$ref": "#/components/schemas/VerdictFactsRead"
          },
          {
            "$ref": "#/components/schemas/ResolutionFactsRead"
          }
        ],
        "discriminator": {
          "propertyName": "type",
          "mapping": {
            "answer": "#/components/schemas/AnswerFactsRead",
            "artifact": "#/components/schemas/NoFactsRead",
            "assignee_changed": "#/components/schemas/AssigneeChangedFactsRead",
            "attempt": "#/components/schemas/NoFactsRead",
            "created": "#/components/schemas/NoFactsRead",
            "decision": "#/components/schemas/NoFactsRead",
            "field_changed": "#/components/schemas/FieldChangedFactsRead",
            "finding": "#/components/schemas/NoFactsRead",
            "link_added": "#/components/schemas/LinkFactsRead",
            "link_removed": "#/components/schemas/LinkFactsRead",
            "note": "#/components/schemas/NoFactsRead",
            "question": "#/components/schemas/QuestionFactsRead",
            "remark": "#/components/schemas/NoFactsRead",
            "resolution": "#/components/schemas/ResolutionFactsRead",
            "section_changed": "#/components/schemas/SectionChangedFactsRead",
            "status_changed": "#/components/schemas/StatusChangedFactsRead",
            "summary": "#/components/schemas/NoFactsRead",
            "verdict": "#/components/schemas/VerdictFactsRead"
          }
        }
      },
      "EntryHeadingRead": {
        "properties": {
          "no": {
            "type": "integer",
            "title": "No",
            "description": "Number inside the task, from 1; `TRK-42#12`",
            "examples": [
              12
            ]
          },
          "type": {
            "$ref": "#/components/schemas/EntryType",
            "examples": [
              "status_changed"
            ]
          },
          "author": {
            "$ref": "#/components/schemas/AuthorRead"
          },
          "created_at": {
            "type": "string",
            "format": "date-time",
            "title": "Created At"
          },
          "title": {
            "type": "string",
            "title": "Title",
            "examples": [
              "Status changed: backlog -> open"
            ]
          },
          "facts": {
            "$ref": "#/components/schemas/EntryFactsRead",
            "description": "Length-bounded facts of the entry: enough to name it in any language without reading the English title the tracker builds. Which fields there are follows from `type`; entries whose title is written by their author have none"
          }
        },
        "type": "object",
        "required": [
          "no",
          "type",
          "author",
          "created_at",
          "title",
          "facts"
        ],
        "title": "EntryHeadingRead",
        "description": "Строка описи дела: то, что видно о записи, не читая её тела."
      },
      "EntryRead": {
        "oneOf": [
          {
            "$ref": "#/components/schemas/PlainEntryRead"
          },
          {
            "$ref": "#/components/schemas/SummaryEntryRead"
          },
          {
            "$ref": "#/components/schemas/QuestionEntryRead"
          },
          {
            "$ref": "#/components/schemas/AnswerEntryRead"
          },
          {
            "$ref": "#/components/schemas/VerdictEntryRead"
          },
          {
            "$ref": "#/components/schemas/RemarkEntryRead"
          },
          {
            "$ref": "#/components/schemas/ResolutionEntryRead"
          },
          {
            "$ref": "#/components/schemas/StatusChangedEntryRead"
          },
          {
            "$ref": "#/components/schemas/SectionChangedEntryRead"
          },
          {
            "$ref": "#/components/schemas/FieldChangedEntryRead"
          },
          {
            "$ref": "#/components/schemas/AssigneeChangedEntryRead"
          },
          {
            "$ref": "#/components/schemas/LinkEntryRead"
          }
        ],
        "discriminator": {
          "propertyName": "type",
          "mapping": {
            "answer": "#/components/schemas/AnswerEntryRead",
            "artifact": "#/components/schemas/PlainEntryRead",
            "assignee_changed": "#/components/schemas/AssigneeChangedEntryRead",
            "attempt": "#/components/schemas/PlainEntryRead",
            "created": "#/components/schemas/PlainEntryRead",
            "decision": "#/components/schemas/PlainEntryRead",
            "field_changed": "#/components/schemas/FieldChangedEntryRead",
            "finding": "#/components/schemas/PlainEntryRead",
            "link_added": "#/components/schemas/LinkEntryRead",
            "link_removed": "#/components/schemas/LinkEntryRead",
            "note": "#/components/schemas/PlainEntryRead",
            "question": "#/components/schemas/QuestionEntryRead",
            "remark": "#/components/schemas/RemarkEntryRead",
            "resolution": "#/components/schemas/ResolutionEntryRead",
            "section_changed": "#/components/schemas/SectionChangedEntryRead",
            "status_changed": "#/components/schemas/StatusChangedEntryRead",
            "summary": "#/components/schemas/SummaryEntryRead",
            "verdict": "#/components/schemas/VerdictEntryRead"
          }
        }
      },
      "EntryType": {
        "type": "string",
        "enum": [
          "summary",
          "decision",
          "attempt",
          "finding",
          "artifact",
          "question",
          "answer",
          "verdict",
          "remark",
          "resolution",
          "note",
          "created",
          "status_changed",
          "section_changed",
          "field_changed",
          "assignee_changed",
          "link_added",
          "link_removed"
        ],
        "title": "EntryType",
        "description": "Тип записи дела. Записи агента и человека — до `NOTE`, служебные — после."
      },
      "ErrorDetail": {
        "properties": {
          "code": {
            "type": "string",
            "title": "Code",
            "examples": [
              "task_not_found"
            ]
          },
          "message": {
            "type": "string",
            "title": "Message",
            "examples": [
              "Task not found"
            ]
          },
          "details": {
            "additionalProperties": true,
            "type": "object",
            "title": "Details"
          }
        },
        "type": "object",
        "required": [
          "code",
          "message"
        ],
        "title": "ErrorDetail",
        "description": "Тело ошибки. `code` — стабильный идентификатор, на него завязывается фронтенд."
      },
      "ErrorResponse": {
        "properties": {
          "error": {
            "$ref": "#/components/schemas/ErrorDetail"
          }
        },
        "type": "object",
        "required": [
          "error"
        ],
        "title": "ErrorResponse",
        "description": "Единый формат ошибки: всё содержательное лежит под ключом `error`."
      },
      "FieldChangedEntryRead": {
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid",
            "title": "Id"
          },
          "seq": {
            "type": "integer",
            "title": "Seq",
            "description": "Tracker-wide monotonic number; journal cursor",
            "examples": [
              1024
            ]
          },
          "no": {
            "type": "integer",
            "title": "No",
            "description": "Number inside the task, from 1; `TRK-42#12`",
            "examples": [
              12
            ]
          },
          "task_key": {
            "type": "string",
            "title": "Task Key",
            "examples": [
              "TRK-42"
            ]
          },
          "author": {
            "$ref": "#/components/schemas/AuthorRead"
          },
          "title": {
            "type": "string",
            "title": "Title",
            "description": "One line; this is what the case index shows",
            "examples": [
              "Status changed: backlog -> open"
            ]
          },
          "body": {
            "type": "string",
            "title": "Body",
            "description": "Markdown; empty for service entries, whose content is the payload",
            "examples": [
              ""
            ]
          },
          "refs": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Refs",
            "description": "References to entries `KEY-N#M`, tasks `KEY-N` and addresses. Entry and task references must exist; addresses are not checked",
            "examples": [
              [
                "TRK-42#3",
                "TRK-7"
              ]
            ]
          },
          "created_at": {
            "type": "string",
            "format": "date-time",
            "title": "Created At"
          },
          "type": {
            "type": "string",
            "const": "field_changed",
            "title": "Type"
          },
          "payload": {
            "$ref": "#/components/schemas/FieldChangedPayload"
          }
        },
        "type": "object",
        "required": [
          "id",
          "seq",
          "no",
          "task_key",
          "author",
          "title",
          "body",
          "created_at",
          "type",
          "payload"
        ],
        "title": "FieldChangedEntryRead",
        "description": "Служебная запись о правке обвязки: сегодня это только `priority`."
      },
      "FieldChangedFactsRead": {
        "properties": {
          "type": {
            "type": "string",
            "const": "field_changed",
            "title": "Type"
          },
          "field": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/TaskField"
              },
              {
                "type": "null"
              }
            ],
            "description": "Which field was edited"
          }
        },
        "type": "object",
        "required": [
          "type"
        ],
        "title": "FieldChangedFactsRead",
        "description": "Правка обвязки: какое поле. Значения не здесь, они в самой записи."
      },
      "FieldChangedPayload": {
        "properties": {
          "field": {
            "type": "string",
            "title": "Field",
            "examples": [
              "priority"
            ]
          },
          "before": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Before",
            "description": "Previous value"
          },
          "after": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "After",
            "description": "New value"
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "field"
        ],
        "title": "FieldChangedPayload",
        "description": "Правка обвязки задачи: «было» и «стало» целиком.\n\nОтдельно от `SectionChangedPayload`, хотя поля похожи: там правка задания и только\nв `backlog`, здесь — то, что меняется в любом незакрытом статусе. Одна модель на\nоба случая означала бы «section» у приоритета.\n\nЗначения — строки, а не «строка либо список»: список остался здесь от снятых меток\n(TRK-18), ни одно сегодняшнее поле обвязки списком не является, и записей со списком\nв базе нет ни одной. Пока объединение было плоским, оно только расширяло тип «на\nвсякий случай»; появится поле-список — у него будет своя форма, а не общая на всех."
      },
      "HealthResponse": {
        "properties": {
          "status": {
            "type": "string",
            "const": "ok",
            "title": "Status"
          },
          "version": {
            "type": "string",
            "title": "Version"
          },
          "environment": {
            "type": "string",
            "title": "Environment"
          },
          "database": {
            "type": "string",
            "const": "ok",
            "title": "Database"
          }
        },
        "type": "object",
        "required": [
          "status",
          "version",
          "environment",
          "database"
        ],
        "title": "HealthResponse",
        "description": "Состояние сервиса. `database` подтверждается реальным запросом, а не наличием конфига."
      },
      "LinkCreate": {
        "properties": {
          "kind": {
            "$ref": "#/components/schemas/LinkKind",
            "description": "What this task is to the other one: `parent`, `child`, `blocks`, `blocked_by` or `relates`. The same link shows up on the other side under the opposite kind",
            "examples": [
              "blocked_by"
            ]
          },
          "other": {
            "type": "string",
            "title": "Other",
            "description": "Key of the task on the other side; matching ignores case",
            "examples": [
              "TRK-7"
            ]
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "kind",
          "other"
        ],
        "title": "LinkCreate",
        "description": "Постановка связи. Вид называет роль **этой** задачи, а не той, что в `other`."
      },
      "LinkEntryRead": {
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid",
            "title": "Id"
          },
          "seq": {
            "type": "integer",
            "title": "Seq",
            "description": "Tracker-wide monotonic number; journal cursor",
            "examples": [
              1024
            ]
          },
          "no": {
            "type": "integer",
            "title": "No",
            "description": "Number inside the task, from 1; `TRK-42#12`",
            "examples": [
              12
            ]
          },
          "task_key": {
            "type": "string",
            "title": "Task Key",
            "examples": [
              "TRK-42"
            ]
          },
          "author": {
            "$ref": "#/components/schemas/AuthorRead"
          },
          "title": {
            "type": "string",
            "title": "Title",
            "description": "One line; this is what the case index shows",
            "examples": [
              "Status changed: backlog -> open"
            ]
          },
          "body": {
            "type": "string",
            "title": "Body",
            "description": "Markdown; empty for service entries, whose content is the payload",
            "examples": [
              ""
            ]
          },
          "refs": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Refs",
            "description": "References to entries `KEY-N#M`, tasks `KEY-N` and addresses. Entry and task references must exist; addresses are not checked",
            "examples": [
              [
                "TRK-42#3",
                "TRK-7"
              ]
            ]
          },
          "created_at": {
            "type": "string",
            "format": "date-time",
            "title": "Created At"
          },
          "type": {
            "type": "string",
            "enum": [
              "link_added",
              "link_removed"
            ],
            "title": "Type"
          },
          "payload": {
            "$ref": "#/components/schemas/LinkPayload"
          }
        },
        "type": "object",
        "required": [
          "id",
          "seq",
          "no",
          "task_key",
          "author",
          "title",
          "body",
          "created_at",
          "type",
          "payload"
        ],
        "title": "LinkEntryRead",
        "description": "Служебная запись о появлении или снятии связи."
      },
      "LinkFactsRead": {
        "properties": {
          "type": {
            "type": "string",
            "enum": [
              "link_added",
              "link_removed"
            ],
            "title": "Type"
          },
          "link_kind": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/LinkKind"
              },
              {
                "type": "null"
              }
            ],
            "description": "Kind of the link"
          },
          "other_key": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Other Key",
            "description": "The task on the other side",
            "examples": [
              "TRK-7"
            ]
          }
        },
        "type": "object",
        "required": [
          "type"
        ],
        "title": "LinkFactsRead",
        "description": "Связь появилась или снята: её вид и вторая сторона."
      },
      "LinkKind": {
        "type": "string",
        "enum": [
          "parent",
          "child",
          "blocks",
          "blocked_by",
          "relates"
        ],
        "title": "LinkKind",
        "description": "Вид связи. Перечислены обе стороны каждой пары: клиент адресует любую из них."
      },
      "LinkPayload": {
        "properties": {
          "kind": {
            "type": "string",
            "title": "Kind",
            "examples": [
              "blocked_by"
            ]
          },
          "other": {
            "type": "string",
            "title": "Other",
            "description": "Key of the task on the other side",
            "examples": [
              "TRK-7"
            ]
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "kind",
          "other"
        ],
        "title": "LinkPayload",
        "description": "Связь между задачами. Подшивается в дело обеих сторон под своим именем.\n\nМодель объявлена здесь, а не в задаче 24 вместе со связями: объединение по `type`\nобязано покрывать **все** типы записей, иначе первая же запись о связи развалила бы\nчтение дела целиком."
      },
      "LinkTaskRead": {
        "properties": {
          "key": {
            "type": "string",
            "title": "Key",
            "examples": [
              "TRK-7"
            ]
          },
          "title": {
            "type": "string",
            "title": "Title",
            "examples": [
              "Выдать номера очередям"
            ]
          },
          "status": {
            "$ref": "#/components/schemas/TaskStatus",
            "description": "Status of the other task; `blocked` is computed from exactly this",
            "examples": [
              "open"
            ]
          }
        },
        "type": "object",
        "required": [
          "key",
          "title",
          "status"
        ],
        "title": "LinkTaskRead",
        "description": "Задача на другой стороне связи: ключ, название и статус."
      },
      "NoFactsRead": {
        "properties": {
          "type": {
            "type": "string",
            "enum": [
              "summary",
              "decision",
              "attempt",
              "finding",
              "artifact",
              "remark",
              "note",
              "created"
            ],
            "title": "Type"
          }
        },
        "type": "object",
        "required": [
          "type"
        ],
        "title": "NoFactsRead",
        "description": "Фактов нет: заголовок записи пишет её автор, и он осмыслен сам по себе."
      },
      "PageMeta": {
        "properties": {
          "next_cursor": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Next Cursor",
            "description": "Cursor for the next page; null means there is nothing more to fetch"
          },
          "has_more": {
            "type": "boolean",
            "title": "Has More",
            "description": "Whether another page exists",
            "default": false
          },
          "total": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Total",
            "description": "Total number of rows matching the filter, across all pages; null means this collection does not count them. Only `GET /api/v1/tasks` fills it in",
            "examples": [
              98
            ]
          }
        },
        "type": "object",
        "title": "PageMeta",
        "description": "Служебные поля коллекции. Всё, что не сам ресурс, живёт здесь, а не рядом с `data`."
      },
      "ParticipantCreate": {
        "properties": {
          "kind": {
            "$ref": "#/components/schemas/ParticipantKind",
            "examples": [
              "agent"
            ]
          },
          "name": {
            "type": "string",
            "pattern": "^[A-Za-z][A-Za-z0-9_]{1,63}$",
            "title": "Name",
            "description": "Latin snake_case name; stored lowercase and unique case-insensitively",
            "examples": [
              "release_bot"
            ]
          },
          "description": {
            "type": "string",
            "maxLength": 1000,
            "title": "Description",
            "description": "Short note on who this is: it is all the reader of a case knows about them",
            "default": "",
            "examples": [
              "Релизный бот, ведёт задачи выкладки"
            ]
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "kind",
          "name"
        ],
        "title": "ParticipantCreate",
        "description": "Регистрация участника.\n\nИмя принимается в любом регистре и хранится в нижнем: имена уникальны без учёта\nрегистра, и шаблон «только нижний регистр» отвечал бы на попытку занять `Alice` при\nсуществующем `alice` разговором о форме строки вместо ответа «имя занято»."
      },
      "ParticipantKind": {
        "type": "string",
        "enum": [
          "human",
          "agent"
        ],
        "title": "ParticipantKind",
        "description": "Род участника.\n\nРолей и прав за родом не стоит: любую запись и любой переход может сделать участник\nлюбого рода (`CONCEPT.md`, 3.1). Род нужен, чтобы читающий дело понимал, кто\nговорит, и чтобы интерфейс человека отличал людей от агентов в списке адресатов."
      },
      "ParticipantRead": {
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid",
            "title": "Id"
          },
          "kind": {
            "$ref": "#/components/schemas/ParticipantKind"
          },
          "name": {
            "type": "string",
            "title": "Name",
            "examples": [
              "release_bot"
            ]
          },
          "description": {
            "type": "string",
            "title": "Description",
            "examples": [
              "Релизный бот, ведёт задачи выкладки"
            ]
          },
          "created_by": {
            "$ref": "#/components/schemas/AuthorRead"
          },
          "created_at": {
            "type": "string",
            "format": "date-time",
            "title": "Created At"
          },
          "updated_at": {
            "type": "string",
            "format": "date-time",
            "title": "Updated At"
          }
        },
        "type": "object",
        "required": [
          "id",
          "kind",
          "name",
          "description",
          "created_by",
          "created_at",
          "updated_at"
        ],
        "title": "ParticipantRead",
        "description": "Участник в ответе."
      },
      "ParticipantUpdate": {
        "properties": {
          "description": {
            "type": "string",
            "maxLength": 1000,
            "title": "Description",
            "examples": [
              "Релизный бот, ведёт задачи выкладки"
            ]
          }
        },
        "additionalProperties": false,
        "type": "object",
        "title": "ParticipantUpdate",
        "description": "Частичное обновление: применяется только переданное.\n\nМеняется одно описание. Имя стоит подписью в уже подшитых записях дела, а род\nобъясняет читателю, кто говорит, — переписывать их задним числом значит переписывать\nисторию, поэтому этих полей здесь нет вовсе, и лишнее поле схема отвергает.\n\nУ описания нет осмысленного `null`, поэтому поле объявлено не-nullable: передать\n`null` схема не даст. Молча отбросить его было бы хуже отказа — клиент получил бы\n`200` и уверенность, что поле изменено."
      },
      "PlainEntryCreate": {
        "properties": {
          "body": {
            "type": "string",
            "maxLength": 65536,
            "title": "Body",
            "description": "Markdown body of the entry",
            "default": ""
          },
          "refs": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "maxItems": 50,
            "title": "Refs",
            "description": "References to entries `KEY-N#M`, tasks `KEY-N` and addresses. Entry and task references must exist; addresses are not checked",
            "examples": [
              []
            ]
          },
          "title": {
            "type": "string",
            "maxLength": 255,
            "minLength": 1,
            "title": "Title",
            "description": "One line; this is what the case index shows",
            "examples": [
              "Номер задачи выдаётся до валидации"
            ]
          },
          "type": {
            "type": "string",
            "enum": [
              "decision",
              "attempt",
              "finding",
              "artifact",
              "note"
            ],
            "title": "Type"
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "title",
          "type"
        ],
        "title": "PlainEntryCreate",
        "description": "Решение, попытка, находка, артефакт, заметка. Нагрузки нет.\n\nЗамечание сюда не входит: у него свой вариант (`RemarkEntryCreate`), потому что\n`type` — разметка объединения, и один вариант на шесть типов не дал бы фронтенду\nсузить тип до замечания там, где это нужно."
      },
      "PlainEntryRead": {
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid",
            "title": "Id"
          },
          "seq": {
            "type": "integer",
            "title": "Seq",
            "description": "Tracker-wide monotonic number; journal cursor",
            "examples": [
              1024
            ]
          },
          "no": {
            "type": "integer",
            "title": "No",
            "description": "Number inside the task, from 1; `TRK-42#12`",
            "examples": [
              12
            ]
          },
          "task_key": {
            "type": "string",
            "title": "Task Key",
            "examples": [
              "TRK-42"
            ]
          },
          "author": {
            "$ref": "#/components/schemas/AuthorRead"
          },
          "title": {
            "type": "string",
            "title": "Title",
            "description": "One line; this is what the case index shows",
            "examples": [
              "Status changed: backlog -> open"
            ]
          },
          "body": {
            "type": "string",
            "title": "Body",
            "description": "Markdown; empty for service entries, whose content is the payload",
            "examples": [
              ""
            ]
          },
          "refs": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Refs",
            "description": "References to entries `KEY-N#M`, tasks `KEY-N` and addresses. Entry and task references must exist; addresses are not checked",
            "examples": [
              [
                "TRK-42#3",
                "TRK-7"
              ]
            ]
          },
          "created_at": {
            "type": "string",
            "format": "date-time",
            "title": "Created At"
          },
          "type": {
            "type": "string",
            "enum": [
              "decision",
              "attempt",
              "finding",
              "artifact",
              "note",
              "created"
            ],
            "title": "Type"
          },
          "payload": {
            "$ref": "#/components/schemas/EmptyPayload"
          }
        },
        "type": "object",
        "required": [
          "id",
          "seq",
          "no",
          "task_key",
          "author",
          "title",
          "body",
          "created_at",
          "type"
        ],
        "title": "PlainEntryRead",
        "description": "Запись без нагрузки: решение, попытка, находка, артефакт, заметка, заведение задачи."
      },
      "QuestionEntryCreate": {
        "properties": {
          "body": {
            "type": "string",
            "maxLength": 65536,
            "title": "Body",
            "description": "Markdown body of the entry",
            "default": ""
          },
          "refs": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "maxItems": 50,
            "title": "Refs",
            "description": "References to entries `KEY-N#M`, tasks `KEY-N` and addresses. Entry and task references must exist; addresses are not checked",
            "examples": [
              []
            ]
          },
          "title": {
            "type": "string",
            "maxLength": 255,
            "minLength": 1,
            "title": "Title",
            "description": "One line; this is what the case index shows",
            "examples": [
              "Номер задачи выдаётся до валидации"
            ]
          },
          "type": {
            "type": "string",
            "const": "question",
            "title": "Type"
          },
          "payload": {
            "$ref": "#/components/schemas/QuestionPayload"
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "title",
          "type",
          "payload"
        ],
        "title": "QuestionEntryCreate",
        "description": "Вопрос участникам реестра."
      },
      "QuestionEntryRead": {
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid",
            "title": "Id"
          },
          "seq": {
            "type": "integer",
            "title": "Seq",
            "description": "Tracker-wide monotonic number; journal cursor",
            "examples": [
              1024
            ]
          },
          "no": {
            "type": "integer",
            "title": "No",
            "description": "Number inside the task, from 1; `TRK-42#12`",
            "examples": [
              12
            ]
          },
          "task_key": {
            "type": "string",
            "title": "Task Key",
            "examples": [
              "TRK-42"
            ]
          },
          "author": {
            "$ref": "#/components/schemas/AuthorRead"
          },
          "title": {
            "type": "string",
            "title": "Title",
            "description": "One line; this is what the case index shows",
            "examples": [
              "Status changed: backlog -> open"
            ]
          },
          "body": {
            "type": "string",
            "title": "Body",
            "description": "Markdown; empty for service entries, whose content is the payload",
            "examples": [
              ""
            ]
          },
          "refs": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Refs",
            "description": "References to entries `KEY-N#M`, tasks `KEY-N` and addresses. Entry and task references must exist; addresses are not checked",
            "examples": [
              [
                "TRK-42#3",
                "TRK-7"
              ]
            ]
          },
          "created_at": {
            "type": "string",
            "format": "date-time",
            "title": "Created At"
          },
          "type": {
            "type": "string",
            "const": "question",
            "title": "Type"
          },
          "payload": {
            "$ref": "#/components/schemas/QuestionPayload"
          }
        },
        "type": "object",
        "required": [
          "id",
          "seq",
          "no",
          "task_key",
          "author",
          "title",
          "body",
          "created_at",
          "type",
          "payload"
        ],
        "title": "QuestionEntryRead",
        "description": "Вопрос участникам. Открыт, пока в деле нет `answer` с его номером."
      },
      "QuestionFactsRead": {
        "properties": {
          "type": {
            "type": "string",
            "const": "question",
            "title": "Type"
          },
          "addressees": {
            "anyOf": [
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "title": "Addressees",
            "description": "Who is asked, at most 20 names"
          },
          "blocking": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "title": "Blocking",
            "description": "Whether the question holds the work"
          }
        },
        "type": "object",
        "required": [
          "type"
        ],
        "title": "QuestionFactsRead",
        "description": "Вопрос: кому адресован и держит ли работу."
      },
      "QuestionPayload": {
        "properties": {
          "addressees": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "maxItems": 20,
            "minItems": 1,
            "title": "Addressees",
            "description": "Participant names from the registry, at least one. Temporary agents cannot be addressed: they have no registry row",
            "examples": [
              [
                "owner"
              ]
            ]
          },
          "blocking": {
            "type": "boolean",
            "title": "Blocking",
            "description": "Whether work can continue without an answer. Required, no default",
            "examples": [
              true
            ]
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "addressees",
          "blocking"
        ],
        "title": "QuestionPayload",
        "description": "Вопрос участникам реестра."
      },
      "QueueCreate": {
        "properties": {
          "key": {
            "type": "string",
            "pattern": "^[A-Za-z][A-Za-z0-9]{1,15}$",
            "title": "Key",
            "description": "Latin key, stored uppercase, immutable: it is part of every task key",
            "examples": [
              "TRK"
            ]
          },
          "title": {
            "type": "string",
            "maxLength": 255,
            "minLength": 1,
            "title": "Title",
            "examples": [
              "Трекер"
            ]
          },
          "description": {
            "type": "string",
            "maxLength": 20000,
            "title": "Description",
            "default": "",
            "examples": [
              "Бэкенд трекера. Код в `app/`, соглашения в `docs/CONVENTIONS.md`"
            ]
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "key",
          "title"
        ],
        "title": "QueueCreate",
        "description": "Создание очереди.\n\nКлюч принимается в любом регистре и хранится в верхнем: он идёт в ключ каждой задачи\n(`TRK-42`) и там обязан читаться как ключ. Уникальность — без учёта регистра."
      },
      "QueueRead": {
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid",
            "title": "Id"
          },
          "key": {
            "type": "string",
            "title": "Key",
            "examples": [
              "TRK"
            ]
          },
          "title": {
            "type": "string",
            "title": "Title",
            "examples": [
              "Трекер"
            ]
          },
          "description": {
            "type": "string",
            "title": "Description",
            "description": "Markdown context shared by every task of the queue",
            "examples": [
              "Бэкенд трекера. Код в `app/`, соглашения в `docs/CONVENTIONS.md`"
            ]
          },
          "last_task_number": {
            "type": "integer",
            "title": "Last Task Number",
            "description": "Last task number handed out; numbers are never reused",
            "examples": [
              42
            ]
          },
          "created_by": {
            "$ref": "#/components/schemas/AuthorRead"
          },
          "created_at": {
            "type": "string",
            "format": "date-time",
            "title": "Created At"
          },
          "updated_at": {
            "type": "string",
            "format": "date-time",
            "title": "Updated At"
          }
        },
        "type": "object",
        "required": [
          "id",
          "key",
          "title",
          "description",
          "last_task_number",
          "created_by",
          "created_at",
          "updated_at"
        ],
        "title": "QueueRead",
        "description": "Очередь в ответе."
      },
      "QueueUpdate": {
        "properties": {
          "title": {
            "type": "string",
            "maxLength": 255,
            "minLength": 1,
            "title": "Title",
            "examples": [
              "Трекер"
            ]
          },
          "description": {
            "type": "string",
            "maxLength": 20000,
            "title": "Description",
            "examples": [
              "Бэкенд трекера. Код в `app/`, соглашения в `docs/CONVENTIONS.md`"
            ]
          }
        },
        "additionalProperties": false,
        "type": "object",
        "title": "QueueUpdate",
        "description": "Частичное обновление: применяется только переданное.\n\nПоля `key` здесь нет и не будет: ключ вшит в ключ каждой задачи очереди, и правка\nзадним числом порвала бы все уже записанные ссылки. Схема отвергает лишнее поле, а\nне игнорирует его молча — иначе клиент получил бы `200` на изменение, которого не\nбыло."
      },
      "RemarkEntryCreate": {
        "properties": {
          "body": {
            "type": "string",
            "maxLength": 65536,
            "title": "Body",
            "description": "Markdown body of the entry",
            "default": ""
          },
          "refs": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "maxItems": 50,
            "title": "Refs",
            "description": "References to entries `KEY-N#M`, tasks `KEY-N` and addresses. Entry and task references must exist; addresses are not checked",
            "examples": [
              []
            ]
          },
          "title": {
            "type": "string",
            "maxLength": 255,
            "minLength": 1,
            "title": "Title",
            "description": "One line; this is what the case index shows",
            "examples": [
              "Номер задачи выдаётся до валидации"
            ]
          },
          "type": {
            "type": "string",
            "const": "remark",
            "title": "Type"
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "title",
          "type"
        ],
        "title": "RemarkEntryCreate",
        "description": "Замечание: «вышло не то». Нагрузки нет, заголовок пишет автор."
      },
      "RemarkEntryRead": {
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid",
            "title": "Id"
          },
          "seq": {
            "type": "integer",
            "title": "Seq",
            "description": "Tracker-wide monotonic number; journal cursor",
            "examples": [
              1024
            ]
          },
          "no": {
            "type": "integer",
            "title": "No",
            "description": "Number inside the task, from 1; `TRK-42#12`",
            "examples": [
              12
            ]
          },
          "task_key": {
            "type": "string",
            "title": "Task Key",
            "examples": [
              "TRK-42"
            ]
          },
          "author": {
            "$ref": "#/components/schemas/AuthorRead"
          },
          "title": {
            "type": "string",
            "title": "Title",
            "description": "One line; this is what the case index shows",
            "examples": [
              "Status changed: backlog -> open"
            ]
          },
          "body": {
            "type": "string",
            "title": "Body",
            "description": "Markdown; empty for service entries, whose content is the payload",
            "examples": [
              ""
            ]
          },
          "refs": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Refs",
            "description": "References to entries `KEY-N#M`, tasks `KEY-N` and addresses. Entry and task references must exist; addresses are not checked",
            "examples": [
              [
                "TRK-42#3",
                "TRK-7"
              ]
            ]
          },
          "created_at": {
            "type": "string",
            "format": "date-time",
            "title": "Created At"
          },
          "type": {
            "type": "string",
            "const": "remark",
            "title": "Type"
          },
          "payload": {
            "$ref": "#/components/schemas/EmptyPayload"
          }
        },
        "type": "object",
        "required": [
          "id",
          "seq",
          "no",
          "task_key",
          "author",
          "title",
          "body",
          "created_at",
          "type"
        ],
        "title": "RemarkEntryRead",
        "description": "Замечание к сделанному. Открыто, пока в деле нет `resolution` с его номером."
      },
      "RemarkOutcome": {
        "type": "string",
        "enum": [
          "fixed",
          "accepted",
          "needs_detail",
          "declined"
        ],
        "title": "RemarkOutcome",
        "description": "Чем разобрано замечание (`CONCEPT.md`, 3.4).\n\nСписок закрыт и покрывает все четыре судьбы претензии: поправили сразу, приняли в\nработу отдельной задачей, не поняли и ждём уточнения, менять не будем. Свободного\n«прочее» здесь нет намеренно — оно снова сделало бы исход текстом."
      },
      "ResolutionEntryCreate": {
        "properties": {
          "body": {
            "type": "string",
            "maxLength": 65536,
            "title": "Body",
            "description": "Markdown body of the entry",
            "default": ""
          },
          "refs": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "maxItems": 50,
            "title": "Refs",
            "description": "References to entries `KEY-N#M`, tasks `KEY-N` and addresses. Entry and task references must exist; addresses are not checked",
            "examples": [
              []
            ]
          },
          "type": {
            "type": "string",
            "const": "resolution",
            "title": "Type"
          },
          "payload": {
            "$ref": "#/components/schemas/ResolutionPayload"
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "type",
          "payload"
        ],
        "title": "ResolutionEntryCreate",
        "description": "Резолюция. Заголовок не принимается: он собирается из ссылки на замечание и исхода."
      },
      "ResolutionEntryRead": {
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid",
            "title": "Id"
          },
          "seq": {
            "type": "integer",
            "title": "Seq",
            "description": "Tracker-wide monotonic number; journal cursor",
            "examples": [
              1024
            ]
          },
          "no": {
            "type": "integer",
            "title": "No",
            "description": "Number inside the task, from 1; `TRK-42#12`",
            "examples": [
              12
            ]
          },
          "task_key": {
            "type": "string",
            "title": "Task Key",
            "examples": [
              "TRK-42"
            ]
          },
          "author": {
            "$ref": "#/components/schemas/AuthorRead"
          },
          "title": {
            "type": "string",
            "title": "Title",
            "description": "One line; this is what the case index shows",
            "examples": [
              "Status changed: backlog -> open"
            ]
          },
          "body": {
            "type": "string",
            "title": "Body",
            "description": "Markdown; empty for service entries, whose content is the payload",
            "examples": [
              ""
            ]
          },
          "refs": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Refs",
            "description": "References to entries `KEY-N#M`, tasks `KEY-N` and addresses. Entry and task references must exist; addresses are not checked",
            "examples": [
              [
                "TRK-42#3",
                "TRK-7"
              ]
            ]
          },
          "created_at": {
            "type": "string",
            "format": "date-time",
            "title": "Created At"
          },
          "type": {
            "type": "string",
            "const": "resolution",
            "title": "Type"
          },
          "payload": {
            "$ref": "#/components/schemas/ResolutionPayload"
          }
        },
        "type": "object",
        "required": [
          "id",
          "seq",
          "no",
          "task_key",
          "author",
          "title",
          "body",
          "created_at",
          "type",
          "payload"
        ],
        "title": "ResolutionEntryRead",
        "description": "Резолюция по замечанию: чем разобрано и куда ушла работа."
      },
      "ResolutionFactsRead": {
        "properties": {
          "type": {
            "type": "string",
            "const": "resolution",
            "title": "Type"
          },
          "remark_no": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Remark No",
            "description": "The remark it resolves",
            "examples": [
              7
            ]
          },
          "outcome": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/RemarkOutcome"
              },
              {
                "type": "null"
              }
            ],
            "description": "How the remark was resolved"
          },
          "continuation_key": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Continuation Key",
            "description": "Key of the task the work moved to; set only when the outcome is `accepted`",
            "examples": [
              null
            ]
          }
        },
        "type": "object",
        "required": [
          "type"
        ],
        "title": "ResolutionFactsRead",
        "description": "Резолюция: какое замечание разобрано, чем и куда ушла работа."
      },
      "ResolutionPayload": {
        "properties": {
          "remark_no": {
            "type": "integer",
            "minimum": 1.0,
            "title": "Remark No",
            "description": "Number of the `remark` entry in the same task",
            "examples": [
              7
            ]
          },
          "outcome": {
            "$ref": "#/components/schemas/RemarkOutcome",
            "description": "How the remark was resolved: fixed right away, accepted into a separate task, needs more detail, or declined",
            "examples": [
              "accepted"
            ]
          },
          "task": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Task",
            "description": "Key of the continuation task; required with `accepted` and not accepted with any other outcome",
            "examples": [
              "TRK-43"
            ]
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "remark_no",
          "outcome"
        ],
        "title": "ResolutionPayload",
        "description": "Нагрузка резолюции: какое замечание разобрано, чем и куда ушла работа."
      },
      "SectionChangedEntryRead": {
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid",
            "title": "Id"
          },
          "seq": {
            "type": "integer",
            "title": "Seq",
            "description": "Tracker-wide monotonic number; journal cursor",
            "examples": [
              1024
            ]
          },
          "no": {
            "type": "integer",
            "title": "No",
            "description": "Number inside the task, from 1; `TRK-42#12`",
            "examples": [
              12
            ]
          },
          "task_key": {
            "type": "string",
            "title": "Task Key",
            "examples": [
              "TRK-42"
            ]
          },
          "author": {
            "$ref": "#/components/schemas/AuthorRead"
          },
          "title": {
            "type": "string",
            "title": "Title",
            "description": "One line; this is what the case index shows",
            "examples": [
              "Status changed: backlog -> open"
            ]
          },
          "body": {
            "type": "string",
            "title": "Body",
            "description": "Markdown; empty for service entries, whose content is the payload",
            "examples": [
              ""
            ]
          },
          "refs": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Refs",
            "description": "References to entries `KEY-N#M`, tasks `KEY-N` and addresses. Entry and task references must exist; addresses are not checked",
            "examples": [
              [
                "TRK-42#3",
                "TRK-7"
              ]
            ]
          },
          "created_at": {
            "type": "string",
            "format": "date-time",
            "title": "Created At"
          },
          "type": {
            "type": "string",
            "const": "section_changed",
            "title": "Type"
          },
          "payload": {
            "$ref": "#/components/schemas/SectionChangedPayload"
          }
        },
        "type": "object",
        "required": [
          "id",
          "seq",
          "no",
          "task_key",
          "author",
          "title",
          "body",
          "created_at",
          "type",
          "payload"
        ],
        "title": "SectionChangedEntryRead",
        "description": "Служебная запись о правке названия, описания или раздела."
      },
      "SectionChangedFactsRead": {
        "properties": {
          "type": {
            "type": "string",
            "const": "section_changed",
            "title": "Type"
          },
          "field": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/TaskField"
              },
              {
                "type": "null"
              }
            ],
            "description": "Which section was edited"
          },
          "check_no": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Check No",
            "description": "Which check was reworded, for a point edit of `checks`. Absent when the whole list was replaced",
            "examples": [
              3
            ]
          }
        },
        "type": "object",
        "required": [
          "type"
        ],
        "title": "SectionChangedFactsRead",
        "description": "Правка задания: какой раздел. Значения не здесь — раздел бывает длиннее задачи."
      },
      "SectionChangedPayload": {
        "properties": {
          "field": {
            "type": "string",
            "title": "Field",
            "examples": [
              "goal"
            ]
          },
          "check_no": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Check No",
            "description": "Which check was reworded, for a point edit of `checks`. Absent when the whole list was replaced: then the set could have changed and the numbers could have shifted",
            "examples": [
              null
            ]
          },
          "before": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "title": "Before",
            "description": "Previous value; a list for `checks`"
          },
          "after": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "title": "After",
            "description": "New value; a list for `checks`"
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "field"
        ],
        "title": "SectionChangedPayload",
        "description": "Правка названия, описания или раздела в `backlog`: «было» и «стало» целиком.\n\nУ точечной правки проверки «целиком» — это тексты самой проверки, а не всего\nсписка, и её номер стоит в `check_no`."
      },
      "StatusChangedEntryRead": {
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid",
            "title": "Id"
          },
          "seq": {
            "type": "integer",
            "title": "Seq",
            "description": "Tracker-wide monotonic number; journal cursor",
            "examples": [
              1024
            ]
          },
          "no": {
            "type": "integer",
            "title": "No",
            "description": "Number inside the task, from 1; `TRK-42#12`",
            "examples": [
              12
            ]
          },
          "task_key": {
            "type": "string",
            "title": "Task Key",
            "examples": [
              "TRK-42"
            ]
          },
          "author": {
            "$ref": "#/components/schemas/AuthorRead"
          },
          "title": {
            "type": "string",
            "title": "Title",
            "description": "One line; this is what the case index shows",
            "examples": [
              "Status changed: backlog -> open"
            ]
          },
          "body": {
            "type": "string",
            "title": "Body",
            "description": "Markdown; empty for service entries, whose content is the payload",
            "examples": [
              ""
            ]
          },
          "refs": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Refs",
            "description": "References to entries `KEY-N#M`, tasks `KEY-N` and addresses. Entry and task references must exist; addresses are not checked",
            "examples": [
              [
                "TRK-42#3",
                "TRK-7"
              ]
            ]
          },
          "created_at": {
            "type": "string",
            "format": "date-time",
            "title": "Created At"
          },
          "type": {
            "type": "string",
            "const": "status_changed",
            "title": "Type"
          },
          "payload": {
            "$ref": "#/components/schemas/StatusChangedPayload"
          }
        },
        "type": "object",
        "required": [
          "id",
          "seq",
          "no",
          "task_key",
          "author",
          "title",
          "body",
          "created_at",
          "type",
          "payload"
        ],
        "title": "StatusChangedEntryRead",
        "description": "Служебная запись о переходе статуса."
      },
      "StatusChangedFactsRead": {
        "properties": {
          "type": {
            "type": "string",
            "const": "status_changed",
            "title": "Type"
          },
          "from_status": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/TaskStatus"
              },
              {
                "type": "null"
              }
            ],
            "description": "Status before the move"
          },
          "to_status": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/TaskStatus"
              },
              {
                "type": "null"
              }
            ],
            "description": "Status after the move"
          },
          "has_reason": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "title": "Has Reason",
            "description": "Whether a reason was given. The reason itself is free text and stays in the entry body"
          }
        },
        "type": "object",
        "required": [
          "type"
        ],
        "title": "StatusChangedFactsRead",
        "description": "Переход статуса: оба конца и был ли назван повод."
      },
      "StatusChangedPayload": {
        "properties": {
          "from": {
            "type": "string",
            "title": "From",
            "examples": [
              "backlog"
            ]
          },
          "to": {
            "type": "string",
            "title": "To",
            "examples": [
              "open"
            ]
          },
          "reason": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Reason",
            "description": "Why the task moved; required for a step back and for `cancelled`",
            "examples": [
              "Нужны уточнения по разделу context"
            ]
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "from",
          "to"
        ],
        "title": "StatusChangedPayload",
        "description": "Переход статуса. `from` — ключевое слово Python, поэтому поле объявлено псевдонимом."
      },
      "SummaryEntryCreate": {
        "properties": {
          "body": {
            "type": "string",
            "maxLength": 65536,
            "title": "Body",
            "description": "Markdown body of the entry",
            "default": ""
          },
          "refs": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "maxItems": 50,
            "title": "Refs",
            "description": "References to entries `KEY-N#M`, tasks `KEY-N` and addresses. Entry and task references must exist; addresses are not checked",
            "examples": [
              []
            ]
          },
          "type": {
            "type": "string",
            "const": "summary",
            "title": "Type"
          },
          "payload": {
            "$ref": "#/components/schemas/SummaryPayload"
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "type",
          "payload"
        ],
        "title": "SummaryEntryCreate",
        "description": "Сводка. Заголовок не принимается: он равен первой строке `done`."
      },
      "SummaryEntryRead": {
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid",
            "title": "Id"
          },
          "seq": {
            "type": "integer",
            "title": "Seq",
            "description": "Tracker-wide monotonic number; journal cursor",
            "examples": [
              1024
            ]
          },
          "no": {
            "type": "integer",
            "title": "No",
            "description": "Number inside the task, from 1; `TRK-42#12`",
            "examples": [
              12
            ]
          },
          "task_key": {
            "type": "string",
            "title": "Task Key",
            "examples": [
              "TRK-42"
            ]
          },
          "author": {
            "$ref": "#/components/schemas/AuthorRead"
          },
          "title": {
            "type": "string",
            "title": "Title",
            "description": "One line; this is what the case index shows",
            "examples": [
              "Status changed: backlog -> open"
            ]
          },
          "body": {
            "type": "string",
            "title": "Body",
            "description": "Markdown; empty for service entries, whose content is the payload",
            "examples": [
              ""
            ]
          },
          "refs": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Refs",
            "description": "References to entries `KEY-N#M`, tasks `KEY-N` and addresses. Entry and task references must exist; addresses are not checked",
            "examples": [
              [
                "TRK-42#3",
                "TRK-7"
              ]
            ]
          },
          "created_at": {
            "type": "string",
            "format": "date-time",
            "title": "Created At"
          },
          "type": {
            "type": "string",
            "const": "summary",
            "title": "Type"
          },
          "payload": {
            "$ref": "#/components/schemas/SummaryPayload"
          }
        },
        "type": "object",
        "required": [
          "id",
          "seq",
          "no",
          "task_key",
          "author",
          "title",
          "body",
          "created_at",
          "type",
          "payload"
        ],
        "title": "SummaryEntryRead",
        "description": "Сводка: точка входа преемника. Последняя главнее предыдущих."
      },
      "SummaryPayload": {
        "properties": {
          "done": {
            "type": "string",
            "maxLength": 16384,
            "minLength": 1,
            "title": "Done",
            "description": "What has been done. Its first line becomes the entry title, so make it one phrase naming what happened; an over-long line is cut at a word boundary",
            "examples": [
              "Разобрался, где сгорает номер"
            ]
          },
          "remaining": {
            "type": "string",
            "maxLength": 16384,
            "minLength": 1,
            "title": "Remaining",
            "description": "What is left",
            "examples": [
              "Перенести выдачу номера после валидации"
            ]
          },
          "blockers": {
            "type": "string",
            "maxLength": 16384,
            "minLength": 1,
            "title": "Blockers",
            "description": "What is in the way; write `нет` rather than leaving it empty",
            "examples": [
              "Нет"
            ]
          },
          "next_step": {
            "type": "string",
            "maxLength": 16384,
            "minLength": 1,
            "title": "Next Step",
            "description": "The next step: one concrete action for whoever picks the case up",
            "examples": [
              "Перенести вызов next_task_number в конец create_task"
            ]
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "done",
          "remaining",
          "blockers",
          "next_step"
        ],
        "title": "SummaryPayload",
        "description": "Справка при передаче. Четыре части, все непустые."
      },
      "TaskClosing": {
        "properties": {
          "summary": {
            "$ref": "#/components/schemas/SummaryPayload",
            "description": "The closing summary. Filed last, after the entries and the verdicts, so that it speaks of their outcome"
          },
          "verdicts": {
            "items": {
              "$ref": "#/components/schemas/TaskClosingVerdict"
            },
            "type": "array",
            "title": "Verdicts",
            "description": "Verdicts filed by this call. May be empty: verdicts filed earlier during the work count as well, and the transition checks the case, not the request",
            "examples": [
              []
            ]
          },
          "entries": {
            "items": {
              "$ref": "#/components/schemas/ClosingEntryCreate"
            },
            "type": "array",
            "title": "Entries",
            "description": "Entries filed before the verdicts, usually an `artifact` pointing at the result",
            "examples": [
              []
            ]
          },
          "version": {
            "anyOf": [
              {
                "type": "integer",
                "minimum": 1.0
              },
              {
                "type": "null"
              }
            ],
            "title": "Version",
            "description": "Version the client last saw; omit it to skip the check",
            "examples": [
              3
            ]
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "summary"
        ],
        "title": "TaskClosing",
        "description": "Чем закрывают задачу: записи, вердикты и финальная сводка одного вызова."
      },
      "TaskClosingVerdict": {
        "properties": {
          "check_no": {
            "type": "integer",
            "minimum": 1.0,
            "title": "Check No",
            "description": "Position in the task `checks` list, numbered from 1",
            "examples": [
              3
            ]
          },
          "outcome": {
            "$ref": "#/components/schemas/VerdictOutcome",
            "examples": [
              "passed"
            ]
          },
          "evidence": {
            "type": "string",
            "maxLength": 65536,
            "title": "Evidence",
            "description": "Proof of the outcome; it becomes the body of the verdict entry",
            "default": "",
            "examples": [
              "docker compose run --rm test: 214 passed"
            ]
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "check_no",
          "outcome"
        ],
        "title": "TaskClosingVerdict",
        "description": "Исход одной обзорной проверки с доказательством."
      },
      "TaskCreate": {
        "properties": {
          "queue": {
            "type": "string",
            "title": "Queue",
            "description": "Queue key; matching ignores case",
            "examples": [
              "TRK"
            ]
          },
          "title": {
            "type": "string",
            "maxLength": 255,
            "minLength": 1,
            "title": "Title",
            "examples": [
              "Починить выдачу ключей задач"
            ]
          },
          "description": {
            "type": "string",
            "maxLength": 65536,
            "minLength": 1,
            "title": "Description",
            "examples": [
              "Ключ выдаётся до валидации и сгорает на неудачном запросе"
            ]
          },
          "goal": {
            "type": "string",
            "maxLength": 65536,
            "title": "Goal",
            "description": "Markdown section; editable only in `backlog`",
            "default": ""
          },
          "context": {
            "type": "string",
            "maxLength": 65536,
            "title": "Context",
            "description": "Markdown section; editable only in `backlog`",
            "default": ""
          },
          "constraints": {
            "type": "string",
            "maxLength": 65536,
            "title": "Constraints",
            "description": "Markdown section; editable only in `backlog`",
            "default": ""
          },
          "output": {
            "type": "string",
            "maxLength": 65536,
            "title": "Output",
            "description": "Markdown section; editable only in `backlog`",
            "default": ""
          },
          "checks": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "maxItems": 100,
            "title": "Checks",
            "description": "Ordered list of checks, numbered from 1 by position; each one must be written so that it can fail. The assignee runs them and records a verdict per check before `done`. Editable only in `backlog`",
            "examples": [
              [
                "docker compose run --rm test: the whole suite is green"
              ]
            ]
          },
          "assignee": {
            "anyOf": [
              {
                "type": "string",
                "maxLength": 64
              },
              {
                "type": "null"
              }
            ],
            "title": "Assignee",
            "description": "Participant name or temporary agent label; free text the tracker never validates against the registry",
            "examples": [
              "release_bot"
            ]
          },
          "priority": {
            "$ref": "#/components/schemas/TaskPriority",
            "default": "normal"
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "queue",
          "title",
          "description"
        ],
        "title": "TaskCreate",
        "description": "Создание задачи. Статуса нет: новая задача рождается в `backlog`.\n\nКлюча тоже нет — его выдаёт счётчик очереди. Разделы можно оставить пустыми и\nдописать в `backlog`; перед `open` они обязаны быть заполнены."
      },
      "TaskFeaturesRead": {
        "properties": {
          "blocked": {
            "type": "boolean",
            "title": "Blocked",
            "description": "Whether the task has a `blocked_by` link to a task that is neither `done` nor `cancelled`. Entering `in_progress` is refused while it is true",
            "examples": [
              false
            ]
          },
          "open_questions": {
            "type": "integer",
            "title": "Open Questions",
            "description": "Questions with no answer",
            "examples": [
              2
            ]
          },
          "open_blocking_questions": {
            "type": "integer",
            "title": "Open Blocking Questions",
            "description": "Of those, the ones marked `blocking`",
            "examples": [
              1
            ]
          },
          "open_remarks": {
            "type": "integer",
            "title": "Open Remarks",
            "description": "Remarks with no resolution: someone said the result is not what was needed and nobody has answered yet",
            "examples": [
              1
            ]
          },
          "last_summary_at": {
            "anyOf": [
              {
                "type": "string",
                "format": "date-time"
              },
              {
                "type": "null"
              }
            ],
            "title": "Last Summary At",
            "description": "When the latest summary was filed; null if the case has none"
          },
          "last_entry_at": {
            "anyOf": [
              {
                "type": "string",
                "format": "date-time"
              },
              {
                "type": "null"
              }
            ],
            "title": "Last Entry At",
            "description": "When an entry by an agent or a human was last filed into the case. Service entries (`created`, `status_changed`, `section_changed`, `assignee_changed`, `link_added`, `link_removed`) do not count: `link_added` is filed into both cases when a link is made from the other side, and a task nobody touched would look alive. Null while the case has no such entry — a freshly created task holds only `created`. This is not `updated_at`: that one moves when the card changes"
          }
        },
        "type": "object",
        "required": [
          "blocked",
          "open_questions",
          "open_blocking_questions",
          "open_remarks"
        ],
        "title": "TaskFeaturesRead",
        "description": "Вычисляемые признаки задачи (`CONCEPT.md`, 4.3).\n\nНе хранятся колонками, а считаются из дела и связей: колонка была бы вторым местом,\nгде живёт правда, и разошлась бы с делом при первом же откате."
      },
      "TaskField": {
        "type": "string",
        "enum": [
          "title",
          "description",
          "goal",
          "context",
          "constraints",
          "output",
          "checks",
          "status",
          "assignee",
          "priority"
        ],
        "title": "TaskField",
        "description": "Поле задачи в записи об изменении и в правилах редактирования.\n\nЗначения совпадают с именами полей в API: по ним строится `details.fields` ошибки и\n`payload.field` записи `section_changed`, и читающий видит то же имя, что в схеме."
      },
      "TaskLinkRead": {
        "properties": {
          "kind": {
            "$ref": "#/components/schemas/LinkKind",
            "description": "What this task is to the other one: `parent`, `child`, `blocks`, `blocked_by` or `relates`. The same link shows up on the other side under the opposite kind",
            "examples": [
              "blocked_by"
            ]
          },
          "other": {
            "$ref": "#/components/schemas/LinkTaskRead"
          },
          "author": {
            "$ref": "#/components/schemas/AuthorRead"
          },
          "created_at": {
            "type": "string",
            "format": "date-time",
            "title": "Created At"
          }
        },
        "type": "object",
        "required": [
          "kind",
          "other",
          "author",
          "created_at"
        ],
        "title": "TaskLinkRead",
        "description": "Связь со стороны одной задачи."
      },
      "TaskPackageRead": {
        "properties": {
          "task": {
            "$ref": "#/components/schemas/TaskRead"
          },
          "links": {
            "items": {
              "$ref": "#/components/schemas/TaskLinkRead"
            },
            "type": "array",
            "title": "Links",
            "description": "Links on both sides, each named from this task's point of view, with the status of the task on the other side"
          },
          "features": {
            "$ref": "#/components/schemas/TaskFeaturesRead"
          },
          "summary": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/SummaryEntryRead"
              },
              {
                "type": "null"
              }
            ],
            "description": "The latest summary in full: what was done, what is left, what is in the way, what is next. Null until the case has one"
          },
          "questions": {
            "items": {
              "$ref": "#/components/schemas/QuestionEntryRead"
            },
            "type": "array",
            "title": "Questions",
            "description": "Every question with no answer yet, in full"
          },
          "remarks": {
            "items": {
              "$ref": "#/components/schemas/RemarkEntryRead"
            },
            "type": "array",
            "title": "Remarks",
            "description": "Every remark with no resolution yet, in full"
          },
          "transitions": {
            "items": {
              "$ref": "#/components/schemas/TaskStatus"
            },
            "type": "array",
            "title": "Transitions",
            "description": "Targets allowed by the transition table from the current status. Transition validations (sections, summary, verdicts, blockers) are checked on the move",
            "examples": [
              [
                "open",
                "cancelled"
              ]
            ]
          },
          "index": {
            "items": {
              "$ref": "#/components/schemas/EntryHeadingRead"
            },
            "type": "array",
            "title": "Index",
            "description": "Case index: heading of every entry, in order; bodies are read separately"
          }
        },
        "type": "object",
        "required": [
          "task",
          "links",
          "features",
          "questions",
          "remarks",
          "transitions",
          "index"
        ],
        "title": "TaskPackageRead",
        "description": "Пакет преемника (`CONCEPT.md`, 4.2).\n\nВсё, что нужно агенту с чистым контекстом, одним вызовом. Полно хранится, по\nоглавлению читается: карточка, связи, признаки, последняя сводка и открытые вопросы\nприходят целиком, остальные записи — строками описи, а их тела запрашиваются\nточечно."
      },
      "TaskPriority": {
        "type": "string",
        "enum": [
          "low",
          "normal",
          "high",
          "critical"
        ],
        "title": "TaskPriority",
        "description": "Приоритет. Порядок членов — от низшего к высшему, на него опирается сортировка поиска."
      },
      "TaskQueueRead": {
        "properties": {
          "key": {
            "type": "string",
            "title": "Key",
            "examples": [
              "TRK"
            ]
          },
          "title": {
            "type": "string",
            "title": "Title",
            "examples": [
              "Трекер"
            ]
          }
        },
        "type": "object",
        "required": [
          "key",
          "title"
        ],
        "title": "TaskQueueRead",
        "description": "Очередь в карточке задачи: ключ и название. Описание запрашивается отдельно."
      },
      "TaskRead": {
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid",
            "title": "Id"
          },
          "key": {
            "type": "string",
            "title": "Key",
            "description": "Immutable and never reused",
            "examples": [
              "TRK-42"
            ]
          },
          "queue": {
            "$ref": "#/components/schemas/TaskQueueRead"
          },
          "title": {
            "type": "string",
            "title": "Title",
            "examples": [
              "Починить выдачу ключей задач"
            ]
          },
          "description": {
            "type": "string",
            "title": "Description",
            "examples": [
              "Ключ выдаётся до валидации и сгорает на неудачном запросе"
            ]
          },
          "goal": {
            "type": "string",
            "title": "Goal",
            "examples": [
              "Ключи не сгорают на отклонённых запросах"
            ]
          },
          "context": {
            "type": "string",
            "title": "Context",
            "examples": [
              "Номер выдаёт `queues.next_task_number`"
            ]
          },
          "constraints": {
            "type": "string",
            "title": "Constraints",
            "examples": [
              "Счётчик очереди не переписывать"
            ]
          },
          "output": {
            "type": "string",
            "title": "Output",
            "examples": [
              "Тест на несгоревший номер"
            ]
          },
          "checks": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Checks",
            "description": "Ordered list of checks, numbered from 1 by position; each one must be written so that it can fail. The assignee runs them and records a verdict per check before `done`. Editable only in `backlog`",
            "examples": [
              [
                "docker compose run --rm test: the whole suite is green"
              ]
            ]
          },
          "status": {
            "$ref": "#/components/schemas/TaskStatus",
            "examples": [
              "backlog"
            ]
          },
          "assignee": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Assignee",
            "description": "Participant name or temporary agent label; free text the tracker never validates against the registry",
            "examples": [
              "release_bot"
            ]
          },
          "priority": {
            "$ref": "#/components/schemas/TaskPriority",
            "examples": [
              "normal"
            ]
          },
          "version": {
            "type": "integer",
            "title": "Version",
            "description": "Grows with every actual change; send it back to detect a lost update",
            "examples": [
              3
            ]
          },
          "created_by": {
            "$ref": "#/components/schemas/AuthorRead"
          },
          "created_at": {
            "type": "string",
            "format": "date-time",
            "title": "Created At"
          },
          "updated_at": {
            "type": "string",
            "format": "date-time",
            "title": "Updated At"
          }
        },
        "type": "object",
        "required": [
          "id",
          "key",
          "queue",
          "title",
          "description",
          "goal",
          "context",
          "constraints",
          "output",
          "checks",
          "status",
          "assignee",
          "priority",
          "version",
          "created_by",
          "created_at",
          "updated_at"
        ],
        "title": "TaskRead",
        "description": "Задача в ответе."
      },
      "TaskSearchRead": {
        "properties": {
          "key": {
            "type": "string",
            "title": "Key",
            "description": "Immutable and never reused",
            "examples": [
              "TRK-42"
            ]
          },
          "id": {
            "anyOf": [
              {
                "type": "string",
                "format": "uuid"
              },
              {
                "type": "null"
              }
            ],
            "title": "Id"
          },
          "queue": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/TaskQueueRead"
              },
              {
                "type": "null"
              }
            ]
          },
          "title": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Title"
          },
          "description": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Description"
          },
          "goal": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Goal"
          },
          "context": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Context"
          },
          "constraints": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Constraints"
          },
          "output": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Output"
          },
          "checks": {
            "anyOf": [
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "title": "Checks"
          },
          "status": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/TaskStatus"
              },
              {
                "type": "null"
              }
            ]
          },
          "assignee": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Assignee"
          },
          "priority": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/TaskPriority"
              },
              {
                "type": "null"
              }
            ]
          },
          "version": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Version"
          },
          "created_by": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/AuthorRead"
              },
              {
                "type": "null"
              }
            ]
          },
          "created_at": {
            "anyOf": [
              {
                "type": "string",
                "format": "date-time"
              },
              {
                "type": "null"
              }
            ],
            "title": "Created At"
          },
          "updated_at": {
            "anyOf": [
              {
                "type": "string",
                "format": "date-time"
              },
              {
                "type": "null"
              }
            ],
            "title": "Updated At"
          },
          "features": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/TaskFeaturesRead"
              },
              {
                "type": "null"
              }
            ],
            "description": "Computed features of the task, the same object the successor package carries. Included unless `fields` asks for a narrower set without `features`"
          }
        },
        "type": "object",
        "required": [
          "key"
        ],
        "title": "TaskSearchRead",
        "description": "Задача в выдаче списка. Приезжают только запрошенные поля.\n\nКлюч приходит всегда: выдача без него бесполезна — по ней нельзя ни прочитать\nзадачу, ни сослаться на неё."
      },
      "TaskStatus": {
        "type": "string",
        "enum": [
          "backlog",
          "open",
          "in_progress",
          "waiting",
          "done",
          "cancelled"
        ],
        "title": "TaskStatus",
        "description": "Зашитый список статусов (`CONCEPT.md`, 3.3)."
      },
      "TaskTransition": {
        "properties": {
          "to": {
            "$ref": "#/components/schemas/TaskStatus",
            "description": "Target status",
            "examples": [
              "open"
            ]
          },
          "reason": {
            "anyOf": [
              {
                "type": "string",
                "maxLength": 65536
              },
              {
                "type": "null"
              }
            ],
            "title": "Reason",
            "description": "Why the task moves. Required for any step back along the chain and for `cancelled`; optional otherwise. Recorded in the `status_changed` entry",
            "examples": [
              "Нужны уточнения по разделу context"
            ]
          },
          "version": {
            "anyOf": [
              {
                "type": "integer",
                "minimum": 1.0
              },
              {
                "type": "null"
              }
            ],
            "title": "Version",
            "description": "Version the client last saw; omit it to skip the check",
            "examples": [
              3
            ]
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "to"
        ],
        "title": "TaskTransition",
        "description": "Перевод статуса по таблице переходов."
      },
      "TaskUpdate": {
        "properties": {
          "title": {
            "type": "string",
            "maxLength": 255,
            "minLength": 1,
            "title": "Title",
            "examples": [
              "Починить выдачу ключей задач"
            ]
          },
          "description": {
            "type": "string",
            "maxLength": 65536,
            "minLength": 1,
            "title": "Description",
            "examples": [
              "Ключ выдаётся до валидации и сгорает на неудачном запросе"
            ]
          },
          "goal": {
            "type": "string",
            "maxLength": 65536,
            "title": "Goal",
            "description": "Markdown section; editable only in `backlog`"
          },
          "context": {
            "type": "string",
            "maxLength": 65536,
            "title": "Context",
            "description": "Markdown section; editable only in `backlog`"
          },
          "constraints": {
            "type": "string",
            "maxLength": 65536,
            "title": "Constraints",
            "description": "Markdown section; editable only in `backlog`"
          },
          "output": {
            "type": "string",
            "maxLength": 65536,
            "title": "Output",
            "description": "Markdown section; editable only in `backlog`"
          },
          "checks": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "maxItems": 100,
            "title": "Checks",
            "description": "Ordered list of checks, numbered from 1 by position; each one must be written so that it can fail. The assignee runs them and records a verdict per check before `done`. Editable only in `backlog`. Replaces the whole list: use it to change the **set** of checks — add one, drop one, reorder. To reword one check in place send `check` instead",
            "examples": [
              [
                "docker compose run --rm test: the whole suite is green"
              ]
            ]
          },
          "check": {
            "$ref": "#/components/schemas/CheckUpdate",
            "description": "Rewords one check in place, leaving the rest of the list byte for byte. The main way to edit a check: rewording is what happens in practice, and sending the whole list back for it lets a typo into the lines nobody meant to touch. Not accepted together with `checks`: the two would answer the same question differently"
          },
          "assignee": {
            "anyOf": [
              {
                "type": "string",
                "maxLength": 64
              },
              {
                "type": "null"
              }
            ],
            "title": "Assignee",
            "description": "Participant name or temporary agent label; free text the tracker never validates against the registry. Pass null to unassign",
            "examples": [
              "release_bot"
            ]
          },
          "priority": {
            "$ref": "#/components/schemas/TaskPriority",
            "examples": [
              "high"
            ]
          },
          "version": {
            "anyOf": [
              {
                "type": "integer",
                "minimum": 1.0
              },
              {
                "type": "null"
              }
            ],
            "title": "Version",
            "description": "Version the client last saw. Sent back it turns a lost update into a `version_conflict` instead of a silent overwrite; omit it to skip the check",
            "examples": [
              3
            ]
          }
        },
        "additionalProperties": false,
        "type": "object",
        "title": "TaskUpdate",
        "description": "Частичное обновление: применяется только переданное.\n\n`assignee` объявлен как `str | None`: `null` осмыслен и снимает исполнителя. У\nостальных полей `null` смысла не имеет, и схема его не пропустит. Статуса здесь нет\n— он меняется переходом; ключа нет — он неизменяем. Лишнее поле схема отвергает, а\nне игнорирует: клиент должен узнать, что изменения не произошло, из ответа."
      },
      "TokenCreate": {
        "properties": {
          "name": {
            "type": "string",
            "maxLength": 255,
            "minLength": 1,
            "title": "Name",
            "description": "Free-form note to tell tokens apart when revoking one",
            "examples": [
              "release-bot on ci"
            ]
          },
          "scope": {
            "$ref": "#/components/schemas/TokenScope",
            "description": "`task` opens the working cycle, `main` adds writes to registries",
            "default": "task",
            "examples": [
              "task"
            ]
          },
          "participant": {
            "anyOf": [
              {
                "type": "string",
                "pattern": "^[A-Za-z][A-Za-z0-9_]{1,63}$"
              },
              {
                "type": "null"
              }
            ],
            "title": "Participant",
            "description": "Participant this token speaks for; omit it to issue a shared agent token",
            "examples": [
              "release_bot"
            ]
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "name"
        ],
        "title": "TokenCreate",
        "description": "Выпуск токена.\n\nБез `participant` получается **общий агентский** токен: он не называет автора сам, и\nкаждый запрос с ним обязан нести заголовок `X-Actor-Label` с меткой временного\nагента. Так работают агенты, которых незачем заводить в реестре."
      },
      "TokenIssued": {
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid",
            "title": "Id"
          },
          "name": {
            "type": "string",
            "title": "Name",
            "examples": [
              "release-bot on ci"
            ]
          },
          "scope": {
            "$ref": "#/components/schemas/TokenScope",
            "examples": [
              "task"
            ]
          },
          "participant": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Participant",
            "description": "Name of the participant this token belongs to; null makes it a shared agent token, which must carry the X-Actor-Label header on every request",
            "examples": [
              "release_bot"
            ]
          },
          "created_by": {
            "$ref": "#/components/schemas/AuthorRead"
          },
          "created_at": {
            "type": "string",
            "format": "date-time",
            "title": "Created At"
          },
          "last_used_at": {
            "anyOf": [
              {
                "type": "string",
                "format": "date-time"
              },
              {
                "type": "null"
              }
            ],
            "title": "Last Used At",
            "description": "Updated at most once a minute to keep reads from becoming writes"
          },
          "revoked_at": {
            "anyOf": [
              {
                "type": "string",
                "format": "date-time"
              },
              {
                "type": "null"
              }
            ],
            "title": "Revoked At",
            "description": "Set when the token is revoked; the record stays for the audit trail"
          },
          "secret": {
            "type": "string",
            "title": "Secret",
            "description": "Full token value, shown once and never stored in plain text",
            "examples": [
              "trk_0oUCtWtA6d9v0j0N1cMBAxk2wKAKopWzvbf_wQ8sDLc"
            ]
          }
        },
        "type": "object",
        "required": [
          "id",
          "name",
          "scope",
          "created_by",
          "created_at",
          "secret"
        ],
        "title": "TokenIssued",
        "description": "Ответ на выпуск токена: единственное место, где секрет виден целиком."
      },
      "TokenRead": {
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid",
            "title": "Id"
          },
          "name": {
            "type": "string",
            "title": "Name",
            "examples": [
              "release-bot on ci"
            ]
          },
          "scope": {
            "$ref": "#/components/schemas/TokenScope",
            "examples": [
              "task"
            ]
          },
          "participant": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Participant",
            "description": "Name of the participant this token belongs to; null makes it a shared agent token, which must carry the X-Actor-Label header on every request",
            "examples": [
              "release_bot"
            ]
          },
          "created_by": {
            "$ref": "#/components/schemas/AuthorRead"
          },
          "created_at": {
            "type": "string",
            "format": "date-time",
            "title": "Created At"
          },
          "last_used_at": {
            "anyOf": [
              {
                "type": "string",
                "format": "date-time"
              },
              {
                "type": "null"
              }
            ],
            "title": "Last Used At",
            "description": "Updated at most once a minute to keep reads from becoming writes"
          },
          "revoked_at": {
            "anyOf": [
              {
                "type": "string",
                "format": "date-time"
              },
              {
                "type": "null"
              }
            ],
            "title": "Revoked At",
            "description": "Set when the token is revoked; the record stays for the audit trail"
          }
        },
        "type": "object",
        "required": [
          "id",
          "name",
          "scope",
          "created_by",
          "created_at"
        ],
        "title": "TokenRead",
        "description": "Токен без секрета: секрет показывается один раз при выпуске и больше нигде."
      },
      "TokenScope": {
        "type": "string",
        "enum": [
          "task",
          "main"
        ],
        "title": "TokenScope",
        "description": "Набор токена — единственное право в трекере.\n\nРолей, владельцев и разрешений по роду участника нет и не будет (`CONCEPT.md`, 6):\nлюбую запись и любой переход может сделать кто угодно, а набор отделяет рабочий цикл\nагента от управления установкой.\n\n| Набор | Открывает |\n|---|---|\n| `task` | Рабочий цикл: задачи, дело, связи, поиск, лента, чтение реестров. |\n| `main` | То же плюс запись очередей, участников и токенов. |"
      },
      "VerdictEntryCreate": {
        "properties": {
          "body": {
            "type": "string",
            "maxLength": 65536,
            "title": "Body",
            "description": "Markdown body of the entry",
            "default": ""
          },
          "refs": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "maxItems": 50,
            "title": "Refs",
            "description": "References to entries `KEY-N#M`, tasks `KEY-N` and addresses. Entry and task references must exist; addresses are not checked",
            "examples": [
              []
            ]
          },
          "type": {
            "type": "string",
            "const": "verdict",
            "title": "Type"
          },
          "payload": {
            "$ref": "#/components/schemas/VerdictPayload"
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "type",
          "payload"
        ],
        "title": "VerdictEntryCreate",
        "description": "Вердикт. Заголовок не принимается; тело записи — доказательство исхода."
      },
      "VerdictEntryRead": {
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid",
            "title": "Id"
          },
          "seq": {
            "type": "integer",
            "title": "Seq",
            "description": "Tracker-wide monotonic number; journal cursor",
            "examples": [
              1024
            ]
          },
          "no": {
            "type": "integer",
            "title": "No",
            "description": "Number inside the task, from 1; `TRK-42#12`",
            "examples": [
              12
            ]
          },
          "task_key": {
            "type": "string",
            "title": "Task Key",
            "examples": [
              "TRK-42"
            ]
          },
          "author": {
            "$ref": "#/components/schemas/AuthorRead"
          },
          "title": {
            "type": "string",
            "title": "Title",
            "description": "One line; this is what the case index shows",
            "examples": [
              "Status changed: backlog -> open"
            ]
          },
          "body": {
            "type": "string",
            "title": "Body",
            "description": "Markdown; empty for service entries, whose content is the payload",
            "examples": [
              ""
            ]
          },
          "refs": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Refs",
            "description": "References to entries `KEY-N#M`, tasks `KEY-N` and addresses. Entry and task references must exist; addresses are not checked",
            "examples": [
              [
                "TRK-42#3",
                "TRK-7"
              ]
            ]
          },
          "created_at": {
            "type": "string",
            "format": "date-time",
            "title": "Created At"
          },
          "type": {
            "type": "string",
            "const": "verdict",
            "title": "Type"
          },
          "payload": {
            "$ref": "#/components/schemas/VerdictPayload"
          }
        },
        "type": "object",
        "required": [
          "id",
          "seq",
          "no",
          "task_key",
          "author",
          "title",
          "body",
          "created_at",
          "type",
          "payload"
        ],
        "title": "VerdictEntryRead",
        "description": "Вердикт по обзорной проверке. Тело записи — доказательство."
      },
      "VerdictFactsRead": {
        "properties": {
          "type": {
            "type": "string",
            "const": "verdict",
            "title": "Type"
          },
          "check_no": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Check No",
            "description": "Number of the review check",
            "examples": [
              3
            ]
          },
          "outcome": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/VerdictOutcome"
              },
              {
                "type": "null"
              }
            ],
            "description": "How the check ended"
          },
          "outdated": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "title": "Outdated",
            "description": "Whether the check was reworded after this verdict was filed. A verdict points at a check by **number**, not by text, so an outdated one reads as «check 3 passed» while what passed was its previous wording. The record itself is never touched: this is computed when the case is read",
            "examples": [
              false
            ]
          }
        },
        "type": "object",
        "required": [
          "type"
        ],
        "title": "VerdictFactsRead",
        "description": "Вердикт: какая обзорная проверка, чем кончилась и не переписали ли её после."
      },
      "VerdictOutcome": {
        "type": "string",
        "enum": [
          "passed",
          "failed"
        ],
        "title": "VerdictOutcome",
        "description": "Исход обзорной проверки. Значений ровно два: третьего состояния у проверки нет."
      },
      "VerdictPayload": {
        "properties": {
          "check_no": {
            "type": "integer",
            "minimum": 1.0,
            "title": "Check No",
            "description": "Position in the task `checks` list, numbered from 1",
            "examples": [
              3
            ]
          },
          "outcome": {
            "$ref": "#/components/schemas/VerdictOutcome",
            "examples": [
              "passed"
            ]
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "check_no",
          "outcome"
        ],
        "title": "VerdictPayload",
        "description": "Исход одной обзорной проверки."
      }
    },
    "securitySchemes": {
      "ApiToken": {
        "type": "http",
        "description": "Tracker API token: `Authorization: Bearer trk_...`",
        "scheme": "bearer"
      }
    }
  }
}
