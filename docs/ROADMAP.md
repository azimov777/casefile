# Порядок выполнения задач

Задачи лежат в `tasks/`. Номер файла — рекомендуемый порядок, но реальное ограничение —
зависимости: задачу можно брать, когда выполнены все, от которых она зависит.

Задачи 01–19 построили трекер общего назначения; 2026-09-03 принято решение переписать
доменный слой под трекер для агентов (`CONCEPT.md`, `SCOPE.md`). Задачи 20–29 реализуют
переписывание. Задачи ревизии 20–24 от 1 сентября удалены и перекрыты этим планом;
номера переиспользованы.

## Граф зависимостей

```
20 Снос старого доменного слоя
 └─ 21 Фундамент: участники, токены с наборами, очереди
     └─ 22 Задача: поля, статусы, переходы, хранилище дела
         └─ 23 Дело: записи агента, сводка, вопросы, вердикты, пакет преемника
             └─ 24 Связи
                 ├─ 25 Поиск и список задач
                 ├─ 26 Лента журнала, ожидание, SSE
                 └─ 27 Идемпотентность
                     └─ 28 MCP-сервер  (нужны также 25, 26)
                         └─ 29 Сборка: bootstrap, демо, OpenAPI, ошибки, README
```

## Линейный порядок запуска агентов

| Шаг | Задача | Статус | Комментарий |
|---|---|---|---|
| 1 | 20 Снос | ✅ | Старый домен удалён целиком, остался фундамент, одна ревизия Alembic |
| 2 | 21 Фундамент | ✅ | Участники, токены `task` / `main`, метка временного агента, очереди со счётчиком |
| 3 | 22 Задача | ✅ | Пять разделов, зашитые статусы и таблица переходов с точками подключения проверок, служебные записи дела |
| 4 | 23 Дело | ✅ | Типизированные записи, обязательная сводка, вердикты, вопросы, пакет преемника, «входящая» |
| 5 | 24 Связи | ✅ | Три вида одной строкой, циклы в двух графах, блокировка входа в работу, дети при закрытии, `blocked` |
| 6 | 25 Поиск | ✅ | Язык запросов и структурный фильтр над одними полями, вычисляемые признаки в запросе, курсор, выбор полей |
| 7 | 26 Лента | ✅ | Хвост по `seq` с фильтрами, ожидание на LISTEN/NOTIFY после фиксации, очередь на подшивку, SSE с `Last-Event-ID` |
| 8 | 27 Идемпотентность | ✅ | `Idempotency-Key` на шести создающих маршрутах, пара «токен и ключ» под уникальностью, ответ целиком на сутки |
| 9 | 28 MCP | ✅ | Двадцать инструментов по наборам токена, фильтр `tools/list`, промпт `tracker-discipline` и `instructions` из скила, слушатель оповещений в своём процессе |
| 10 | 29 Сборка | ✅ | `bootstrap`, демо одной командой, `openapi.json` в репозитории, `ERRORS.md`, README, три сквозных сценария |

Переписывание закончено: очередь `tasks/` пуста, все десять задач закрыты. Дальше
работа идёт не по этому плану, а по наблюдениям — раздел «Что откладывается до
наблюдений» ниже и точка наблюдения «после 28».

## Правки по итогам ревизии 2026-09-04

После закрытия 20–29 прогнан полный набор тестов (525 зелёных), живой контур с нуля
и четыре ревизии кода против концепции. Расхождений с концепцией не найдено; заведены
задачи на найденные дыры и долги. Все четыре независимы друг от друга.

| Шаг | Задача | Статус | Комментарий |
|---|---|---|---|
| 11 | 30 Надёжность | ✅ | Очередь изменений `app/db/locks.py`, перевод `IntegrityError` на границе транзакции, ожидание ленты бросается при уходе клиента |
| 12 | 31 Вердикты в текущем обзоре | ⏸ | Меняет правило концепции; брать только после решения владельца |
| 13 | 32 Признаки в списке | ⬜ | `features` в строках `GET /api/v1/tasks` и `search_tasks` |
| 14 | 33 Документационный долг | ⬜ | Заметки, мёртвые настройки, следы старой модели в тестах |

## Что можно делать параллельно

После 24 независимы друг от друга 25, 26 и 27: разные модули, общие только готовые
сервисы. Задачи 20–24 трогают одно ядро и ведутся строго последовательно.

## Точки, где стоит остановиться и посмотреть результат

- **После 23**: цикл задачи проходится через REST целиком: создать, открыть, взять,
  подшить, сводка, `review`, вердикты, `done`. Хороший момент проверить, удобно ли
  читается пакет преемника, и поправить форму записей до того, как её увидит агент.
- **После 26**: назначатель может строить свой цикл на ленте. Стоит написать простейший
  внешний скрипт «прочитать хвост, пересчитать кандидатов» и посмотреть, чего в ленте
  не хватает.
- **После 28**: агент работает через MCP. Подключить Claude Code, установить скил, дать
  задачу и смотреть на дело: видно, какие записи агент пишет сам, а какие приходится
  вытягивать текстом скила. Это главная точка наблюдения всего проекта.
- **После 29**: можно отдавать фронтенду и назначателю. Схема лежит в `openapi.json`,
  контракт описан в `FRONTEND.md`, демо-данные ставятся одной командой — этого хватает,
  чтобы начать интерфейс, не поднимая переписку с бэкендом.

## Что откладывается до наблюдений

Решения, которые владелец пометил как временные и вернётся к ним после экспериментов:

- Поле «исполнитель» как свободная строка без валидации.
- Две формы идентичности агента: постоянный участник и временная метка.
- Прямой переход `in_progress → backlog`: оставлен, но за частотой стоит следить.
- Роль человека в системе целиком.
- Вебхуки: вернуть, если назначателю не хватит ленты.
