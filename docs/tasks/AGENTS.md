# docs/tasks

Задания для агентов-исполнителей: один файл — одна задача, выполняемая за один заход.
Порядок запуска и зависимости — в `docs/ROADMAP.md`, общие правила и формат задания —
в `docs/CONVENTIONS.md`. Выполненная задача удаляется вместе со своей строкой в этом списке.

## Файлы
Задачи переписывания (20–29), надёжности (30), области вердиктов (31), признаков в
списке (32), документационного долга (33) и прод-контура (34) выполнены и удалены —
очередь пуста. Новая работа начинается с файла здесь — по формату задания из
`docs/CONVENTIONS.md` — и строки в `docs/ROADMAP.md`.
- `35-docs-cleanup.md` — чистка документации после переписывания: устаревшие заметки, `SCOPE.md`, повторы; ожидает решения владельца
