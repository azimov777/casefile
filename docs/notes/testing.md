# Тесты

## Откат между тестами держится на join_transaction_mode

**Что:** сессия теста создаётся привязанной к соединению с уже открытой транзакцией и с
`join_transaction_mode="create_savepoint"`.
**Почему важно:** без этого режима `session.commit()` внутри сценария закоммитит внешнюю
транзакцию теста, откат перестанет работать, и тесты начнут зависеть от порядка выполнения.
Сломается это не сразу, а когда появятся сервисы с честными коммитами — то есть с задачи 02.
**Как правильно:** транзакцию открывает фикстура соединения и откатывает её после теста;
сессия внутри работает через savepoint и коммитить может сколько угодно.
**Где:** `tests/conftest.py`, фикстуры `db_connection` и `db_session`.

## Alembic внутри тестов запускается в отдельном потоке

**Что:** прямой вызов `command.upgrade(...)` из async-фикстуры падает: `env.py` вызывает
`asyncio.run`, а в потоке pytest-asyncio уже крутится цикл событий.
**Почему важно:** ошибка выглядит как проблема Alembic, хотя дело в цикле событий, — можно
потерять час.
**Как правильно:** `await asyncio.to_thread(_upgrade_to_head, url)` — в новом потоке цикла нет.
**Где:** `tests/conftest.py`, фикстура `engine`.

## Область цикла событий у фикстур и тестов должна совпадать

**Что:** в `pyproject.toml` заданы обе настройки: `asyncio_default_fixture_loop_scope` и
`asyncio_default_test_loop_scope`, обе — `session`.
**Почему важно:** если фикстура создана в session-цикле, а тест идёт в function-цикле,
соединение с БД падает с «attached to a different loop».
**Как правильно:** менять обе настройки вместе; при переходе на function-scope придётся
пересобирать движок на каждый тест.
**Где:** `pyproject.toml`, секция `[tool.pytest.ini_options]`.

## Тестовая база создаётся из подключения к postgres

**Что:** `CREATE DATABASE` нельзя выполнить, находясь внутри создаваемой базы, поэтому
фикстура подключается asyncpg к служебной базе `postgres`.
**Почему важно:** первый запуск тестов на чистом томе иначе падает с `database "tracker_test"
does not exist`.
**Как правильно:** адрес тестовой базы берётся из настроек (`effective_test_database_url`,
по умолчанию — основная с суффиксом `_test`); DSN для asyncpg получается заменой префикса
`postgresql+asyncpg://` на `postgresql://`.
**Где:** `tests/conftest.py`, `_create_database_if_missing`.

## Задачи одного теста получают одинаковый `created_at`

**Что:** проверять в тестах пагинации конкретный порядок ключей нельзя: `now()` — время
начала транзакции, а весь тест живёт в одной транзакции, поэтому у всех созданных задач
`created_at` совпадает и сортировка по паре `(created_at, id)` вырождается в сортировку
по случайным UUID.
**Почему важно:** такой тест зелёный примерно в половине запусков, и его чинят
подгонкой ожиданий, а не пониманием причины.
**Как правильно:** проверять полноту страниц и отсутствие пересечения между ними
(`sorted(seen) == [...]`), а не порядок. Ради этого случая второй элемент в ключе
сортировки и нужен — без него страницы теряли бы записи.
**Где:** `app/db/base.py`, `TimestampsMixin`; `tests/test_search_api.py`,
`test_an_insertion_between_pages_neither_duplicates_nor_loses_tasks`.

## Гонку между процессами на фикстуре `db_session` поставить нельзя

**Что:** проверка «два процесса делают это одновременно» требует отдельных сессий поверх
отдельных соединений и настоящего `commit` у каждой.
**Почему важно:** `db_session` живёт внутри одной транзакции теста, которая в конце
откатывается, — а конфликт на уникальном ключе виден только между **разными**
транзакциями. Тест, написанный на общей сессии, зелёный и на сломанном коде: он проверяет
последовательный вызов под видом одновременного.
**Как правильно:** брать сессионную фикстуру `engine` и делать свою `async_sessionmaker`;
убирать за собой в `finally` руками — откат теста до закоммиченных строк не достаёт.
Одновременность задавать `asyncio.Barrier`, а не надеждой на планировщик задач: без него
первый вызов успевает закоммитить до того, как второй начнёт вставку.
**Где:** `tests/test_entry_numbering.py`, `committing_sessions`; то же в
`tests/test_queue_numbering.py` и `tests/test_idempotency_race.py`.

## Отказ инструмента MCP откатывает написанное, отказ запроса REST — нет

**Что:** тест на «отказ не оставил следов» ставится через клиент MCP, а не через
`auth_client`. Сессию инструмента даёт фабрика теста, и она откатывается вместе с
вызовом; сессию HTTP-запроса фикстура `app` подменяет **сессией самого теста**
(`app.dependency_overrides[get_session]`), которую откатывать некому до конца теста.
**Почему важно:** отказавший запрос REST оставляет в тесте всё, что успел записать до
отказа, — и проверка «в деле не появилось ни одной записи» проходит там, где она обязана
падать, либо падает там, где код исправен. На `TRK-32` неудачное закрытие оставило в
сквозном цикле лишнюю сводку и сломало сверку описи, хотя в бою запрос откатился бы.
**Как правильно:** всё-или-ничего проверять клиентом MCP
(`tests/test_mcp_tools.py::test_a_refused_closing_files_nothing_at_all`), а в тестах REST
считать, что после отказа в сессии осталось написанное до него. Настоящую границу
транзакции HTTP проверяет `tests/test_transactions.py` — мимо фикстуры `app`, на
настоящей зависимости.
**Где:** `tests/conftest.py`, фикстуры `app` и `mcp_sessions`; `tests/test_transactions.py`.

## `auth_client` — это тот же объект, что и `client`

**Что:** фикстура `auth_client` не создаёт нового клиента, а проставляет заголовок
существующему и возвращает его же.
**Почему важно:** тест, который берёт обе фикстуры, чтобы «сходить от двух акторов»,
на самом деле ходит от одного: подмена заголовка на «втором» клиенте меняет и первый.
Тест при этом зелёный или падает в неожиданном месте — например, счётчик непрочитанных
показывает чужой инбокс.
**Как правильно:** менять токен на одном клиенте по ходу теста, проверив сначала
первого актора, потом второго. И проверять, кому принадлежит токен, — `bootstrap`
отдаёт актора вместе с данными.
**Где:** `tests/conftest.py`, `auth_client` и `client`.

## Сплошная проверка маршрутов роняет тест на неизвестном параметре, а не пропускает

**Что:** развёртка по всем `GET` подставляет значения параметров пути из словаря
`sample`. Параметр, которого в словаре нет, — это `AssertionError` с объяснением.
**Почему важно:** пропуск неизвестного маршрута выглядит ровно как покрытый: тест
зелёный, счётчик почти тот же. Новый маршрут с новым параметром тихо выпал бы из
проверки, а она продолжала бы называться сплошной.
**Как правильно:** заводя маршрут с новым параметром пути, добавить его значение в
`sample`. Значения берутся из демо-набора: держать вторую копию такого же набора ради
тестов — значит получить два набора, расходящихся с первой правки.
**Где:** `tests/test_api_contract.py`, `_substitute` и фикстура `sample`.

## Гонка за номером задачи ставится своими сессиями поверх `engine`

**Что:** `tests/test_queue_numbering.py` — рабочий пример проверки «два процесса
одновременно»: своя `async_sessionmaker` поверх сессионной фикстуры `engine`, настоящие
коммиты, `asyncio.Barrier` на 50 задач и ручная уборка закоммиченной строки в `finally`.
**Почему важно:** это тот самый случай, о котором говорит соседняя заметка про фикстуру
`db_session` (её прежний пример, `tests/test_automation_engine.py`, снесён вместе со старым
доменом). Тест, написанный на общей сессии, зелёный и на сломанном коде: он проверяет
последовательный вызов под видом одновременного.
**Как правильно:** проверять, что тест **умеет падать**: подменить `UPDATE ... RETURNING`
наивным «прочитать и записать +1» и убедиться, что проверка краснеет. Без этого шага гонка
«проверяется» ровно до первого настоящего дефекта.
**Где:** `tests/test_queue_numbering.py`.

## Фикстуры доступа названы по набору токена, а не по актору

**Что:** `main_secret`, `task_secret` и `shared_secret` — три токена одного и того же
владельца (последний вообще без участника); `main_actor` и `task_actor` — структуры автора
для прямых вызовов сценариев, мимо HTTP.
**Почему важно:** отказ единой точки прав зависит только от набора, и тест «этому нельзя»
пишется сменой одной фикстуры, а не заведением второго участника. Про `auth_client` при этом
помнить прежнее: он возвращает **тот же** объект, что и `client`, поэтому смена набора внутри
теста делается присваиванием заголовка, а не второй фикстурой клиента.
**Как правильно:** тест на «этому набору нельзя» берёт другую `*_secret`-фикстуру и
больше ничего не заводит; второй участник нужен только там, где проверяется именно
принадлежность, а не права. Смена набора внутри теста — присваивание заголовка
`auth_client`, потому что клиент один и тот же объект.
**Где:** `tests/conftest.py`.

## Гонка одним токеном не начнётся без прогрева: мешает `tokens.last_used_at`

**Что:** первый запрос новым токеном обновляет `tokens.last_used_at`, и сессия
приложения сбрасывает это изменение на первом же `SELECT`. Строка токена оказывается
заблокированной до конца транзакции, и все остальные запросы **тем же токеном** ждут на
ней ещё до того, как дойдут до проверяемого места.
**Почему важно:** проверка гонки за ключом идемпотентности из-за этого выродилась в
последовательность: девять «одновременных» запросов находили готовый ответ первого и до
уникального индекса не добирались вовсе. Тест был зелёным и на наивной реализации —
ровно тот случай, о котором предупреждает соседняя заметка про `db_session`.
**Как правильно:** перед барьером сделать один прогревочный запрос тем же токеном.
Отметка держится минуту (`LAST_USED_THROTTLE`), и на время прогона гонка становится
настоящей. Проверять это надо инструментально: временно залогировать ветку разбора
`IntegrityError` и убедиться, что она срабатывает нужное число раз.
**Где:** `tests/test_idempotency_race.py`.

## Тест повтора на общей сессии проверяет повтор, но не гонку

**Что:** фикстура `app` подменяет `get_session` одной сессией теста, поэтому все запросы
теста идут одной незакоммиченной транзакцией. Строка ключа, занятая первым запросом,
видна второму — повтор, конфликт отпечатков и срок жизни проверяются так честно.
**Почему важно:** а вот отказ, откатывающий транзакцию, на такой фикстуре не
откатывается: подмена — обычная функция, а не генератор, и отката у неё нет. Проверка
«неудачный вызов не занимает ключ» на ней была бы зелёной по неверной причине.
**Как правильно:** всё, что зависит от границы транзакции, выносить в тест на настоящих
сессиях поверх `engine`.
**Где:** `tests/test_idempotency.py` и `tests/test_idempotency_race.py`.

## Гонку «прочитал и зафиксировал» ставят паузой, а не барьером

**Что:** барьер сводит две транзакции в одной точке — этого хватает гонке за номером, но
не гонке перехода со связью. Там нужно другое: одна транзакция обязана **остановиться
между** чтением факта и фиксацией и дать второй пройти целиком. Пауза ставится подменой
функции факта (`links_service.unclosed_children`, `open_blockers`): она честно считает
своё и ждёт события теста.
**Почему важно:** тест с барьером на этой гонке зелёный и на сломанном коде — обе
транзакции стартуют одновременно, и кто-то из них всё равно окажется первым. Дефект живёт
именно в окне между чтением и коммитом, и открывать окно надо руками.
**Как правильно:** проверять, что тест умеет падать: убрать `lock_changes` из
`apply_task_changes` и `add_link` — обе проверки обязаны покраснеть.
**Где:** `tests/test_mutation_races.py`.

## Место встречи решает, какой из двух отказов получит проигравший

**Что:** барьер ставится **после** чтения, которым сценарий решает исход, а не до него.
В `tests/test_integrity_conflicts.py` встреча двух команд `init` стояла внутри
подменённой `TokenRepository.any_exists`, а подмена ничего не читала — обе команды
сходились до первого своего запроса в базу.
**Почему важно:** создающий сценарий устроен «проверить и вставить», и у проигравшего
два законных исхода: `participant_name_taken`, если он прочитал уже после чужого
коммита, и `conflict: Database constraint violated` с именем ограничения, если раньше.
Тест проверяет второй, а получал то один, то другой: сессии здесь на движке прогона с
`NullPool`, и первым запросом каждая команда платит полное установление соединения — кто
подключился первым, тот успевает пройти всю транзакцию целиком. На занятой машине это
выходило раз в несколько десятков прогонов (`TRK-48`): коды возврата сходились, а строки
про ограничение в `stderr` не было. Плавающий тест хуже отсутствующего — его первый отказ
читают как случайность.
**Как правильно:** сводить транзакции подменой самого чтения: она честно зовёт оригинал и
только потом встаёт на барьер (`meet_after_the_check`). Проверять это управляемо —
задержка второму читателю перед его первым запросом обязана менять исход **до** правки
(0,05 с хватало) и не менять после (тест выдерживает секунду).
**Где:** `tests/test_integrity_conflicts.py`, `meet_after_the_check`.

## Закоммиченные строки теста сносятся в порядке зависимостей, и связи в нём отдельно

**Что:** внешние ключи связей на задачи объявлены **без** `ON DELETE CASCADE`
(`app/db/models/link.py`),
поэтому уборка идёт так: записи (с выключенным триггером неизменяемости) → связи →
задачи → очередь.
**Почему важно:** упавшая уборка оставляет закоммиченную очередь, и следующий прогон
падает уже на фикстуре — `duplicate key value violates unique constraint "uq_queues_key"`,
далеко от настоящей причины. Чинится это руками в тестовой базе, и полчаса уходит на то,
чтобы понять, что тест не при чём.
**Как правильно:** ключи и имена в тестах на настоящих сессиях делать уникальными на
прогон (`uuid4().hex[:6]`), а уборку держать в `finally` фикстуры.
**Где:** `tests/test_mutation_races.py`, `tests/test_integrity_conflicts.py`.

## Отключение клиента изображается каналом ASGI, а не клиентом httpx

**Что:** проверка «ожидание прекращается, когда клиент ушёл» вызывает приложение напрямую
(`await app(scope, receive, send)`) и отдаёт `http.disconnect` из своего `receive`.
**Почему важно:** `httpx` с `ASGITransport` уход не изображает: снятая задача клиента не
превращается в сообщение канала, и серверная корутина о ней не узнаёт. Тест «отменим
запрос и посмотрим» был бы зелёным всегда.
**Как правильно:** `receive` до ухода обязан **молчать** (спать), а не возвращать пустое
сообщение: `Request.is_disconnected` читает канал под уже отменённой областью, то есть
спрашивает «есть ли сообщение прямо сейчас».
**Где:** `tests/test_journal_disconnect.py`.

## Формат заметок и состав `.env.example` держатся тестами, а не вычиткой

**Что:** `tests/test_notes.py` требует у каждой записи `docs/notes/*.md` все четыре поля
(«Что», «Почему важно», «Как правильно», «Где») и их порядок;
`tests/test_artifacts.py` сверяет поля `Settings` с `.env.example` в обе стороны и
требует, чтобы каждая подстановка `${...}` из обоих файлов Compose была в примере.
**Почему важно:** оба правила были описаны прозой в `CONVENTIONS.md` и оба разъехались
молча: к задаче 33 шестьдесят записей из трёхсот шестнадцати остались без «Как
правильно», а в `Settings` жили два поля, которых не читал никто. Проза не исполняется —
исполняется падающий тест.
**Как правильно:** новое поле `Settings` заводится вместе со строкой в `.env.example`,
иначе прогон краснеет; удалённое поле убирается из примера там же. Заметка дописывается
целиком: поле «Как правильно» — не украшение записи, а её единственная исполнимая часть.
Проверка формата намеренно не смотрит на длину текста — от неё пришлось бы отбиваться
водой, и это ровно тот случай, когда мерить нечего.
**Где:** `tests/test_notes.py`; `tests/test_artifacts.py`, раздел «Пример окружения».

## Долгое ожидание укорачивается копией настроек, а не правкой кешированной

**Что:** фикстура делает `get_settings().model_copy(update={...})` и подменяет
`get_settings` **в модуле сценария**, а не правит поля объекта, который отдаёт кеш.
**Почему важно:** объект настроек кеширован на процесс, и `setattr` по нему переживает
тест: следующий получит чужой интервал опроса и упадёт в месте, никак с ним не связанном.
С настройками по умолчанию проверка одного круга ожидания стоит секунд, и набор перестают
гонять целиком, — но платить за это протечкой между тестами нельзя.
**Как правильно:** копия настроек плюс `monkeypatch.setattr(<модуль>, "get_settings",
lambda: tuned)`. Подменять надо тот модуль, который зовёт `get_settings` в рантайме:
импортированное имя своё в каждом.
**Где:** `tests/test_journal_disconnect.py`, фикстура `quick_poll`.

## Имя из кода сторожится во всём тексте заметки, а не только в поле «Где:»

**Что:** тест берёт из записи все токены в обратных кавычках — из заголовка и всех
полей, — оставляет похожие на имя из кода (подчёркивание рядом с буквой или цифрой, как
в `get_actor`; смена регистра внутри слова, как в `LinkView`; цепочка через точку, которая
разбирается на звенья) и требует, чтобы такое имя нашлось где-нибудь в коде проекта,
кроме самих заметок. Имя ищется и внутри токена, который целиком именем не является: в
вызове (`string_enum(AuthorKind, name="author_kind")`), в присваивании
(`response_class=Response`), в перечислении через запятую. Не проверяется одиночное слово
(`null`, `open`, `SELECT`), число, слово с косой чертой — путь или адрес — и кусок, обрубленный
знаком образца: `normalize_*`, `100_percent`, `-updated_at`.
**Почему важно:** до этого сверялось только поле «Где:», и переименование символа ломало
текст заметки молча: набор оставался зелёным, а следующий агент читал заметку до кода и
верил ей. Вся гниль, которую нашла ревизия документации 2026-09-09, сидела именно в
«Что», «Почему важно» и «Как правильно» — имена от снесённых механик и от домена, которого
в проекте никогда не было. Искать имя в файлах, которые называет сама запись, нельзя:
запись законно поминает соседний модуль, библиотеку и SQL, и на живых заметках такое
правило дало 55 ложных падений из 196 записей.
**Как правильно:** падение чинится по существу — имя правится на живое или запись сносится
как мёртвая. Законно помянутое имя, которого в коде нет и не будет, вносится в один из
двух списков в шапке теста: чужое (библиотека, PostgreSQL, протокол языка) и снесённое или
несбывшееся у нас — с причиной в строке. Молча освободить имя формой токена нельзя, это и
есть цена: пропуск обязан быть виден на ревизии. Список стережёт своя проверка — имя из
него обязано и дальше отсутствовать в коде и поминаться хоть одной записью. Сам разбор
токена стережёт проверка на образцах, а не на живых заметках: заметка меняется каждой
задачей, а форма вызова и присваивания — нет. Дыры оставлены там, где искать нечего:
имена объектов базы (их собирает `NAMING_CONVENTION`, в исходниках их может не быть
вовсе), пути и адреса эндпоинтов вместе с подстановками внутри них, образец с подстановкой
вместо куска имени. И одна цена исключения: свой файл в стог имён не входит, поэтому
сослаться из заметки на имя, объявленное только в этом тесте, не получится — падение будет
выглядеть как «имени нет в коде».
**Где:** `tests/test_notes.py`.

## Зелёные родители слияния не означают зелёного результата

**Что:** набор прогоняется в ветке, а базой для следующей задачи становится слияние, и его
не прогонял никто. Ветка A переименовала символ, ветка B в это же время назвала его в
заметке или в докстроке: обе зелёные, git сводит их без конфликта — файлы разные, — а
красным оказывается результат.
**Почему важно:** это единственный класс расхождений, который рождается параллельной
работой, а не невнимательностью одного агента, и он тем шире, чем больше в наборе сторожей
документации: сторож ловит расхождение только там, где его прогнали. Хуков в репозитории
нет, а конвейер GitHub (`.github/workflows/ci.yml`) видит только то, что уже запушено, —
локальный main, который служит базой агентам, он не проверяет. Поэтому следующий агент
берёт непроверенную базу как есть и получает красный набор на своей первой же проверке —
на чужой работе, которой не касался.
На пробе TRK-45 две ветки по 717 passed дали на чистом слиянии 2 failed.
**Как правильно:** сливать ветку задачи скриптом, а не `git merge`: он сливает без
коммита, прогоняет набор на результате и коммитит только зелёное, оставляя в сообщении
строку с исходом прогона. Ревизия прошлых слияний (`report_unverified_merges`) читает
историю **настоящего** репозитория, а в дереве `git worktree` файл `.git` — указатель
абсолютным путём хоста, внутри контейнера неразрешимый; эта часть правила поэтому и дальше
живёт без сторожа в наборе, там, где есть и git, и docker, — на хосте, в самом скрипте. С
TRK-54 git в дев-образе всё же появился, но не ради этого: он нужен
`tests/test_merge_script.py`, который исполняет сам скрипт на временном репозитории,
собранном самим тестом, а не на настоящей истории проекта (см. следующую запись).
**Где:** `scripts/merge-task-branch.sh`, `docs/CONVENTIONS.md`.

## Метка `pytest.mark.anyio` в наборе под pytest-asyncio ломается при обновлении anyio

**Что:** весь набор идёт под pytest-asyncio в режиме `auto` с циклом событий на сессию, а
четыре файла были помечены `pytestmark = pytest.mark.anyio`. С anyio 4.15.0 это сходило
с рук; с 4.15.1 плагин anyio просит модульную фикстуру `anyio_backend` из запроса уровня
сессии, и тесты этих файлов падают: `ScopeMismatch` и `Future attached to a different
loop`. Пятьдесят красных тестов из ничего — на том же коде.
**Почему важно:** зависимости в дев-образе не закреплены, и версия anyio приходит из
первой сборки без кэша — в конвейере, в чистом клоне, на новой машине. Локально набор
оставался зелёным ровно до тех пор, пока в кэше лежал старый образ, поэтому поломка
выглядела как чужая правка, а не как смена зависимости. Нашлась она на первой сборке
образа с нуля (TRK-59); сравнение `pip list` в двух образах сразу показало anyio.
**Как правильно:** асинхронный тест в этом наборе пишется без метки: режим `auto` сам
гонит его в общем цикле сессии, там же, где живут асинхронные фикстуры. Метку anyio не
ставить — два плагина, запускающих один тест, спорят за цикл. Подозрительно зелёный или
красный набор сверять на образе, собранном без кэша, а не на том, что лежит под тегом.
**Где:** `pyproject.toml`, `asyncio_mode`; `tests/test_remarks.py`,
`tests/test_last_entry_at.py`.

## `MERGE_MSG` переживает выход процесса и отдельный повторный вызов — если в него написать до выхода

**Что:** `git merge --no-commit` с конфликтом сам кладёт своё предложение сообщения в
`$(git rev-parse --git-dir)/MERGE_MSG` и оставляет файл как есть до `git commit` или `git
merge --abort`. Файл — обычный файл в `.git`, а не состояние процесса: он переживает выход
скрипта, разбор конфликта руками и совершенно отдельный запуск `scripts/merge-task-branch.sh
--continue`, у которого от прежнего вызова нет ничего, кроме этого файла и `MERGE_HEAD`.
**Почему важно:** сообщение, переданное через `-m` при первом вызове, доступно только в его
собственном `$MESSAGE` — при `--continue` это уже новый процесс с пустым `$MESSAGE`, и если
не записать `-m` в `MERGE_MSG` до выхода на конфликте, оно теряется безвозвратно, а git молча
подставляет своё «Merge branch '…'» (TRK-54, тот же дефект и то же решение — UI-96).
**Как правильно:** писать `$MESSAGE` в `MERGE_MSG` (когда он не пуст) сразу в точке, где
скрипт обнаруживает конфликт и вот-вот выйдет — не только перед итоговым `git commit`.
Второго места для сообщения заводить не нужно: `MERGE_MSG` уже переживает ровно то, что
нужно пережить. Проверяется на временном git-репозитории с настоящим конфликтом: сам скрипт
запускается по-настоящему, а первый токен `TEST_COMMAND` (`docker`) подменяется поддельным
исполняемым файлом на `PATH` — мгновенным и не трогающим ни Docker, ни настоящий набор.
**Где:** `scripts/merge-task-branch.sh`, функция `stage_message` и её вызов в ветке
конфликта `git merge --no-ff --no-commit`; сторожит `tests/test_merge_script.py`.
