# e2e

Сквозные сценарии Playwright против настоящего бэкенда с демо-данными. Контур
поднимает и гасит сам прогон: `pnpm e2e`.

Ключ сценарии не подсевают: его выдаёт установка, а контур доносит до браузера
конфигурацией на своём источнике — тем же путём, каким его получает человек. Поэтому
сценарий просто открывает адрес и видит задачи. Сценариям запасного пути (экран входа)
установка без ключа выдаётся поимённо — `installWithoutKey` в `contour.ts`.

Все сценарии, кроме `access.spec.ts`, `answer.spec.ts`, `remark.spec.ts`, `live.spec.ts`, `paging.spec.ts`, `layout.spec.ts`, `live-list.spec.ts`, `live-board.spec.ts`, `case-readable.spec.ts`, `case-latest.spec.ts`, `board-column.spec.ts`, `task-list-screen.spec.ts`, `parents-long.spec.ts`, `link-groups.spec.ts`, `section-edits.spec.ts`, `service-i18n.spec.ts`, `long-word.spec.ts`, `key-wrap.spec.ts` и `moving.spec.ts`, только читают
и потому идут параллельно в обеих темах. Пишущие вынесены в проект `запись`: он идёт
после читающих и по одному сценарию за раз (`playwright.config.ts`).

Проектов не три, а четыре: рядом со светлой и тёмной темой стоит `английская`
(`locale: 'en-US'`), и в ней ровно три сценария — `language`, `language-formats`
и `english`. Остальные написаны под русский экран и остаются в нём: они ищут элементы
по подписям, но проверяют поведение, геометрию и тему, а не язык, и второй их прогон
на английском стоил бы столько же времени, сколько первый. Язык сценарию задаётся
`locale` контекста (`test.use`) или записью в `localStorage` через `addInitScript`
до загрузки страницы; параметра `?lang=` у интерфейса нет, и заводить его ради удобства
теста нельзя (UI-76).

Ожидаемая подпись в этих трёх вписана руками, а не взята из словаря: тест, берущий её
оттуда же, откуда её берёт код, проверял бы связь ключа с элементом, а не то, что
человек видит английский текст. Данные при этом не переводятся — названия задач
и тексты записей на английском экране остаются такими, какими лежат в базе, и это
проверяется сверкой с ответом бэкенда, а не тем, что тест не упал.

Сценарий, которому нужна своя подопытная задача, заводит её один раз и узнаёт между
прогонами **отбором `text` по фразе из своего описания** (`case-latest`, `case-readable`,
`task-list-screen` — константа `MARKER`). Машинных меток для этого больше нет: поле
`tags` снято у задачи целиком (UI-41). Фразу держат в описании, а не в названии: описание
в списке не показано и потому не участвует в замерах ширин и переносов. Фраза обязана
быть уникальной в очереди `DEMO`: отбор ищет подстроку и в названии, и в описании, и,
совпав с чужой задачей, увёл бы сценарий на неё. Односложную метку вместо фразы тут
держало то, что структурный отбор `text` отвергал значение из двух слов (`422
invalid_search_query`); ограничение снято (TRK-21).

## Файлы

- `login.spec.ts` — вход с токеном демо, отказ на неверном токене, прогон `axe`;
  установка без ключа берётся явно
- `account-login.spec.ts` — режим входа по учётным записям на втором экземпляре интерфейса
  (`LOGIN_URL`): `/config.json` — `401 {"login":"password"}` всем, чужой `Host` ключа не
  получает, форма почты и пароля вместо поля токена, отказ неверному паролю без куки,
  верный — свой токен сеанса по куке `HttpOnly`, перезагрузка без входа, выход гасит сеанс и
  токен на сервере; `axe` на экране входа
- `install-key.spec.ts` — ключ от установки: открытый адрес сразу показывает задачи (ничего
  не подменяется — конфигурацию положил контур) без выхода, людей и учётной записи, `/people` и `/account` — «страница не найдена», медленная конфигурация без вспышки входа,
  пустая конфигурация с прежним путём, отсутствие конфигурации без красной плашки
- `language.spec.ts` — порядок выбора языка четырьмя переходами: три контекста браузера
  (`en-US`, `ru-RU`, `de-DE`) на экране входа и выбор человека из `localStorage` поверх
  русского браузера на настоящих экранах; переключение без перезагрузки, чистые журнал
  и сеть, отсутствие языка в адресе
- `english.spec.ts` — дымовой прогон главных экранов на английском: список, карточка,
  дело, входящая; подписи английские, а названия задач, тексты записей и названия
  очередей русские — сверкой с ответом бэкенда
- `language-formats.spec.ts` — что языком задано помимо подписей: `lang` у корневого элемента, заголовок вкладки, подпись времени и точная дата в подсказке на обоих языках; смена языка без перезагрузки, пояс от языка не зависит
- `access.spec.ts` — экран «Доступы»: ключом установки заводится агент `e2e_agent`, ему
  выпускается токен, секрет показан один раз вместе с фрагментами и проверен запросом,
  после закрытия окна и перезагрузки его нет ни на экране, ни в хранилищах; отзыв метит
  строку и роняет следующий запрос в `401`; с ключом набора `task`, введённым на `/login`,
  список виден, а действия записи запрещены с причиной и без единого запроса записи;
  `axe` без единого нарушения на экране, в окне секрета и в обеих темах. Заводит
  участника (удалить его нельзя), поэтому проект «запись»
- `connect.spec.ts` — экран «Подключить агента»: вход из навигации ключом установки и ключом `task` на `/login`, адрес во фрагментах — из ответа установки контура (нарочно не умолчание), копирование кладёт в буфер текст фрагмента, флажок общего токена в адресе, ни одного нарушения `axe` в обоих видах
- `tasks.spec.ts` — список: отбор по статусу, признаки строки, пустая выдача, страница за концом выдачи, адрес, `axe`
- `archive.spec.ts` — архив со сдвинутыми часами браузера (`page.clock`): закрытые демо уходят из таблицы и с доски и возвращаются флажком; без сдвига скрыта только закрытая без записей; выбор в адресе переживает перезагрузку и ссылку; покой без лишних запросов; порог пересекается при следующем чтении, а не по часам; запрос человека по «и» с правилом и позиция опечатки в его строке; `axe` серьёзных нарушений и отдельно — `axe` вовсе без единого нарушения на доске и в таблице (UI-99)
- `board-column.spec.ts` — дочитывание столбца прокруткой: страница в разметке сразу после
  раскрытия, счёт запросов на весь обход столбца, десять секунд покоя на пустом, коротком
  и дочитанном столбце; заводит задачи сам, проект «запись»
- `board-tail.spec.ts` — низ доски: под полосой прокрутки столбцов остаётся зазор доски
  (`--ui-board-tail`), а не общий хвост страницы — на 1440×900 и 1280×720, с полосой
  видимой и точным сравнением с токеном темы; страница по-прежнему не прокручивается;
  у таблицы задач и у карточки хвост остался общим (`--ui-page-tail`), без признака
  `data-board` на их обёртке; снимки доски в обеих темах
- `board.spec.ts` — доска: столбцы из контракта, одна высота и одна ширина при любой длине столбцов, своя прокрутка столбца на широком экране и прокрутка страницы на узком, липкие заголовки столбцов на глубине прокрутки (видимость всех шести, фон и контраст, кнопка мышью и клавиатурой, прокрутка ряда вбок), прижатые к окну заголовки на узком экране (глубина, кнопка, все шесть столбцов вбок колесом, страница не едет вбок на 320 и 704, шапка не просвечивает, `axe`), знак края у нижней рамки (есть у переполненного столбца и нет у поместившегося, гаснет на конце прокрутки, тема и контраст, нажатие сквозь него, остановки `Tab`), свёрнутые столбцы и развёрнутое ожидание, переход в карточку, фильтр, `axe`
- `scrollbar.spec.ts` — своя полоса прокрутки у столбца доски и у ряда столбцов: место,
  которое она заняла (без границы рамки), равно толщине из стилей и меньше системных 15 px,
  цвет ползунка — точное значение токена темы, стандартная пара до этого движка не дошла;
  отдельным сценарием — запасной путь для движков без псевдоэлементов: та же пара,
  объявленная на живой странице, применяется и гасит наш ползунок. Снимки столбца до и
  после — доказательство для дела. Полосу прогон видит потому, что `playwright.config.ts`
  снимает `--hide-scrollbars`; окна этот файл больше не поднимает
- `task.spec.ts` — карточка и лента дела: пакет одним запросом, раскрытие записи, отбор по типам, `axe`
- `entry-link.spec.ts` — ссылка на запись (UI-155): знак у записи в описи и в ленте дела на 1280 и 390 кладёт в буфер адрес карточки с `?entry=N`, адрес в новой странице открывает запись раскрытой, знак виден без наведения; прежнее «Скопировать KEY#N» и отказ буфера словами
- `questions.spec.ts` — входящая: адресованный вопрос, отбор в адресе, ссылка вопроса открывает свою запись, область действия очереди, красная кромка блокирующего замером вычисленных стилей, `axe`; без параметров — входящая, история вторым видом и `axe` истории
- `remarks.spec.ts` — чтение замечаний: карточка закрытой задачи, разбор под замечанием, признак и отбор, входящая, `axe`
- `remark.spec.ts` — замечание с карточки: подшивка с ключом повтора, подтверждение, черновик; проект «запись»
- `remark-cancel.spec.ts` — отмена формы замечания (UI-142): пустой черновик сворачивает
  сразу, непустой — только после подтверждения окном, а не браузерным `confirm`; «Продолжить
  писать» его не трогает; ничего не отправляет, читает в обеих темах
- `people.spec.ts` — люди в режиме входа: администратор заводит товарища с паролем один раз, тот входит во втором браузере и видит те же задачи, людей ему нет ни пунктом, ни ссылкой, выход одного не трогает другого, отключённый выброшен и не входит; `axe` и 390 px на «Людях», «Моей учётной записи» и окне заведения; проект «запись»
- `own-access.spec.ts` — свои токены в режиме входа (UI-123): человек без флага администратора выпускает ключ себе, в выборе нет других людей, на экране только его ключ и его сеанс, секрет не возвращается перезагрузкой, отзыв гасит ключ следующим запросом; администратор во втором браузере видит все токены с отзывом чужого, «Мои» — `?tokens=mine`; `axe`; проект «запись» по имени файла
- `answer.spec.ts` — ответ на вопрос демо: пишущий сценарий, проект «запись»; отвеченный вопрос уходит из входящей и виден в истории с ответом и ссылкой `KEY#N`
- `answer-cancel.spec.ts` — отмена формы ответа (UI-156), в обоих местах показа (входящая и
  карточка задачи): пустой черновик сворачивает сразу, непустой — только после подтверждения
  окном, а не браузерным `confirm`; «Продолжить писать» его не трогает; адрес `?entry=N`
  после отмены форму заново не раскрывает; ничего не отправляет, читает в обеих темах
- `live.spec.ts` — живой поток: кадры доходят до списка, карточки и шапки; проект «запись»
- `paging.spec.ts` — страницы списка рядом номеров: общее число выдачи, переход, пересылка адреса,
  шаг назад; ссылка на запись с последней страницы дела; ссылка на ответ внутри вопроса; заводит
  задачи и записи сам, проект «запись»
- `case-latest.spec.ts` — дело длиннее четырёх страниц: панель «Фильтр» ставит типы подряд и
  переживает перезагрузку чипами (UI-137), переход к свежей
  записи окном, ссылка на далёкую запись с соседями, прыжок по описи, `axe`; заводит дело из 104
  записей само, проект «запись»
- `case-readable.spec.ts` — дело по-русски: заголовки по фактам, длинное содержание целиком,
  высота служебной записи, сводка без повтора, отбор «Служебные», `axe`; проект «запись»
- `live-list.spec.ts` — список под живым потоком: полоса вместо перестановки строк, фон, обрыв,
  правка приоритета, карточка обновляется сама; приход в таблицу с доски и из карточки без
  полосы и с одним запросом таблицы, запись, пришедшая, пока ответ таблицы придержан, остаётся
  в полосе (кадры считаются раздвоением настоящего потока); геометрия полосы против подвала
  боковой панели и против карточки вопроса предельной ширины на ширинах от порога панели до
  широкого экрана, «Показать» и «Выйти» мышью без `force`; стопка над пришедшей и ушедшей
  полосой едет движением места полосы (кадры, токены, уходящая полоса скрыта и не под
  курсором, место доезжает до нуля раньше, чем узел снимают), а на широком экране
  не сдвигается ни на пиксель; проект «запись»
- `live-board.spec.ts` — доска под живым потоком: задача, двинутая через API, переезжает
  в свой столбец без нажатий (с замером прокрутки столбца и числа запросов на кадр), пачка
  из десяти записей стоит одного перечитывания, после обрыва (гашение `api`) доска догоняет
  пропущенное сама, ключи доски не будят её запросов на таблице; заводит задачи сам
  и возвращает двинутую обратно, проект «запись»
- `filters.spec.ts` — свёрнутый отбор: высота строки, снятие чипа клавиатурой, список сортировки на Radix
- `marks.spec.ts` — статусы различаются формой без цвета; колонка «Приоритет» не выросла
- `foundation.spec.ts` — основа: нижняя граница кегля на пяти экранах, табличные цифры, ночные токены
- `fonts.spec.ts` — Fira с сетью и без неё, запасная гарнитура, тёмная тема без класса на `html`
- `tone.spec.ts` — тон плашек различает статусы и приоритеты
- `motion.spec.ts` — общий сторож гашения: словарь движения собран обходом живых таблиц стилей, места —
  обходом всех узлов; все три рода на одном экране и всё найденное гаснет по `prefers-reduced-motion`
- `navigation.spec.ts` — возврат в список с отбором, переключение «Карточка — Дело», ссылки на записи в ленте,
  смена вида таблица ↔ доска без потери условий; «Карточка → Дело → назад» браузера; действие и переключатель
  вида в строке задачи одной высоты и на одной оси на 1440 и 390 (UI-128)
- `narrow.spec.ts` — узкий экран: 320, 390 и 768 px без горизонтальной прокрутки, увеличение текста вдвое,
  клавиатура и состояние потока без открытия панели, прокрутка таблицы вбок внутри рамки,
  колонка названия не схлопывается ни на одной ширине полосы от `fold` до `wide`,
  имя рамки таблицы на обоих краях развилки (широкий экран молчит про прокрутку, узкий называет её)
  и его слежение за шириной места без перезагрузки,
  порядок чтения карточки и сводка подписью над текстом, `axe`
- `side.spec.ts` — оболочка: переход между очередями, подсветка места, отсутствие действий в панели,
  шторка на 390×844 с `Esc` и возвратом фокуса, её въезд и уход замером кадров, гашение движения
  и закрытие посреди появления, отсутствие горизонтальной прокрутки, `axe`
- `notice.spec.ts` — уведомление о вопросе: приход и уход замером кадров, стопка и дело под ней
  на кадрах закрытия, карточка у нижнего края своего места на каждом кадре прихода и ухода,
  закрытая карточка и её место доезжают до конца выхода раньше, чем узел снимают,
  гашение движения; живой поток подменён своим `ReadableStream`, поэтому сценарий
  ничего не пишет
- `reveal.spec.ts` — раскрытие по кадрам: низ видимого содержимого идёт с низом места на раскрытии
  и свёртывании записи описи и столбца доски, обрезания в покое нет; в переполненном
  столбце со своей прокруткой высота места растёт без плато до высоты содержимого
- `hit-target.spec.ts` — в задачу ведёт вся строка и вся карточка: клики, выделение, `cmd`-клик, обход табом
- `touch-target.spec.ts` — мишени нажатия на телефоне не мельче 24×24 px (UI-154): все `button`,
  `[role=tab]` и флажки вместе с подписью на `/connect`, карточке, деле и списке на 390 px,
  обеих темах; отдельно — ссылки вне абзацев (возврат «Ко всем задачам», крошка очереди
  с проверкой вычисленного `min-width`) и вкладки клиента на `/connect`; отдельно — чип
  отбора (`FilterChip`, обе кнопки) и «Сбросить» (`FilterResetButton`) с применённым
  условием на списке задач и в деле — без условия чипы вообще не рендерятся, поэтому
  общий сканирующий проход по экрану их не ловит (UI-164)
- `parents.spec.ts` — родитель задачи на демо (UI-119): подпись на карточке и в строке, клик по ней ведёт
  в родителя, по остальному — в саму задачу, у верхнего уровня подписи нет, высота строк одна до пикселя,
  ссылка на родителя — своя остановка табом без обводки строки, запросов столько же и ни одного на родителя
- `parents-long.spec.ts` — родитель с названием из путей и задача с двумя родителями: подпись в одну строку
  с многоточием и подсказкой, «+1» не усечён, ширина столбца и карточек та же, высота строк таблицы одна,
  снимки столбца и таблицы в выводе прогона; заводит задачи сам и отменяет их в `finally`, проект «запись»
- `layout.spec.ts` — раскладка карточки задачи: две колонки, длинное задание, пустые блоки, левый край шапки
  блока «Дело» вровень с заголовком и первой колонкой описи в обеих темах и на 1440/390 px, снимки в выводе
  прогона (UI-127); заводит задачи сам, проект «запись»
- `link-groups.spec.ts` — блок «Связи» насыщенной задачей (UI-125): пять видов, по нескольку
  задач в группе, длинное название среди детей; заголовок группы со счётчиком, порядок
  групп, ни один идентификатор не обрезан на 1440 и 390 px, статус связанной задачи тем
  же знаком, что в таблице; строки одной формы (UI-148): статус у всех с одного левого
  края, зазор между связями больше зазора внутри связи; снимки светлой и тёмной темы в
  выводе прогона; пустой блок (UI-141) замером держит те же поля от рамки, что и
  непустой список; заводит задачи
  сам и отменяет их в `finally`, проект «запись»
- `section-edits.spec.ts` — правки разделов одним действием (UI-133): две пачки по семь правок
  одной строкой в описи и одной группой в ленте, `?entry=N` раскрывает группу и ведёт к записи N,
  на 1440 и 390 px документ не шире окна, `axe` на ленте; заводит задачу сам и отменяет её после
  файла, проект «запись»
- `service-i18n.spec.ts` — служебное на языке человека (UI-140): карточка с раскрытой описью и
  лента дела задачи со всеми служебными записями, сводками и вердиктами на ru без латиницы и на
  en без кириллицы вне идентификаторов; данные набраны греческим; заводит и закрывает задачу сам,
  проект «запись»
- `long-word.spec.ts` — длинное слово и встроенный код без единой точки переноса (UI-150):
  вопрос с 200-символьным словом и длинным адресом в code-спане на карточке, в деле и во
  входящей на 390 px не тянет документ вбок и не обрезан многоточием; блок кода (`pre`)
  при этом остаётся при своей горизонтальной прокрутке; заводит задачу сам и отменяет её
  в `finally`, проект «запись»
- `key-wrap.spec.ts` — ключ задачи и ссылка на запись не рвутся переносом по дефису
  (UI-151): сводка с тем же ключом на тридцати разных отступах слов перед ним на 390 px —
  у каждой ссылки `getClientRects().length === 1`; вне markdown (опись — заголовок «Ответ
  на», таблица, шапка карточки, блок «Связи» — UI-161) — тем же замером через
  вычисленный `white-space: nowrap` на 390 и 320 px; заводит задачи, записи и связь сам,
  проект «запись»
- `moving.spec.ts` — перенос установки (UI-135): администратор скачивает архив кнопкой,
  принимает его на второй, полностью пустой установке (своя `db`/`api`/`ui` под именем
  `<проект>-moving-target`, гасится сама) — итог на экране, та же очередь и те же задачи
  на доске без перезагрузки, повторный приём — `installation_not_empty`;
  неадминистратору в режиме входа нет ни пункта панели, ни кнопок, ни запроса архива,
  снимок на 390 px в выводе прогона; проект «запись»
- `task-list-screen.spec.ts` — первый экран списка: сколько строк видно, липкая шапка, отказ разбора без сдвига,
  активность в деле и порядок по ней, пересылка адреса, ширина названия на 900 px; заводит задачи сам,
  проект «запись»
- `resilience.spec.ts` — отказ запроса объясняется по коду и чинится кнопкой «Повторить»
- `host-guard.spec.ts` — защита от DNS rebinding (UI-107): чужой `Host` не отдаёт
  `/config.json` и не проксирует `/api/v1/bootstrap`, свои адреса петли (`localhost`,
  `127.0.0.1`, `[::1]`) отвечают как прежде
- `global-setup.ts` — подъём контура путём человека: бэкенд, миграции, владелец, ключ
  интерфейса в файл, демо-данные, ключ набора `task` для входа на `/login` в свой файл,
  интерфейс с ключом в переменной и второй его экземпляр в режиме пароля — одноразовым
  контейнером той же службы на `UI_LOGIN_PORT`
- `global-teardown.ts` — гашение контура вместе с данными, ключом и одноразовым интерфейсом
- `contour.ts` — вызов `docker compose`, ключ установки из файла контура, ключ набора `task`
  для входа (`readTaskToken`), пароль владельца контура и адрес экземпляра под паролем
  (`E2E_PASSWORD`, `LOGIN_URL`), установка без ключа для сценариев входа, ожидание шрифтов,
  доступ к боковой панели оболочки, перечень статусов из контракта бэкенда,
  снятие кадра движения и разбор длительности с кривой; состав демо по статусам глазами
  человека — по умолчанию без архива, со своим правилом архива, записанным заново
  (`tasksByStatus`, `shownKeys`, `outsideArchive`); ожидание покоя движения, переживающее
  легально отменённый переход по наведению (`motionSettled`, UI-102); ожидание покоя
  раскладки узла для действий, а не только замеров (`layoutSettled`, UI-139)
