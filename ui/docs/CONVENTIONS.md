# Сквозные соглашения проекта

Обязательны к прочтению перед выполнением любой задачи репозитория. Задача описывает
*что* делать, соглашения — *как*.

## Стек

- Node 22 LTS, pnpm. Версии закреплены в `package.json` (`engines`) и `.nvmrc`.
- TypeScript в строгом режиме, React 19, Vite.
- Маршруты: React Router. Серверное состояние: TanStack Query. Клиент API: типы
  из `openapi-typescript`, вызовы через `openapi-fetch`.
- Живой поток: `@microsoft/fetch-event-source` (браузерный `EventSource` не умеет
  заголовок авторизации).
- Markdown: `react-markdown` с `remark-gfm`; HTML из markdown не разрешается.
- Стили: Tailwind CSS 4 подключён плагином Vite; токены объявлены в `@theme`
  (`shared/styles/theme.css`), значения — в `shared/styles/tokens.css`. Чужая шкала
  Tailwind погашена целиком: в разметке доступны только наши имена (`bg-surface`,
  `text-faint`, `rounded-block`), потому что чужие имена не переключаются вместе
  с темой и не подчиняются ни одному нашему решению.
- Компоненты: shadcn/ui на Radix. Готовый компонент берётся там, где он есть, — своё
  пишется только там, где готового нет. Причина: сложные образцы WAI-ARIA (диалог,
  выпадающий список, меню) — это ловушка фокуса, `Esc`, возврат фокуса и клавиатурная
  навигация, и написанные заново они всегда беднее.
- Шрифты: Fira Sans для подписей и Fira Code для идентификаторов контракта, с Google
  Fonts. У обеих в стеке обязательна системная запасная гарнитура: контур сквозных
  тестов поднимается без интернета, и страница обязана оставаться читаемой.
- Тесты: Vitest и Testing Library для модулей и страниц, MSW для подмены API,
  Playwright для сквозных сценариев против настоящего бэкенда.
- Качество: ESLint, Prettier, проверка типов `tsc --noEmit`, границы слоёв
  через `steiger` (Feature-Sliced Design).

Новые зависимости добавляются только если без них задача не решается разумным объёмом
кода; каждая называется в отчёте о задаче.

### Почему раньше было иначе

До 7 сентября 2026 здесь стояло обратное правило: «Стили: CSS Modules и переменные темы
в `shared/styles`. Библиотек компонентов нет: интерфейс маленький, а чужая библиотека
тянет свою тему и свой словарь». Оно **отменено решением владельца** от 7 сентября
2026 (UI-36): интерфейс перестал быть маленьким, а цена своей реализации диалогов,
меню и выпадающих списков оказалась выше цены чужой темы — тем более что Radix темы
не приносит вовсе, а Tailwind свою шкалу отдаёт нам погасить.

Прежнее правило названо здесь, а не вычищено: следующий агент, увидев Radix в коде,
должен понимать, что это решение, а не забытый запрет.

### Оформление живёт в разметке

Оформление набирается утилитами Tailwind по токенам `@theme`. Файлов `*.module.css`
в проекте не осталось ни одного, и заводить новый нельзя: программа UI-43 закончена
8 сентября 2026 — одиннадцать её детей перевели двадцать девять модулей и 2180 строк CSS,
не сдвинув вид ни на пиксель.

Прежнее правило названо здесь, а не вычищено. До этого дня рядом жили два языка
оформления, и разводил их каскад: CSS Modules стоят вне слоёв, а неслойное правило
сильнее любого слоя, поэтому модуль экрана выигрывал у утилиты. Правило звучало «экран
либо на модуле, либо на утилитах, промежутка нет», и у него было ровно одно названное
исключение — `size-(--ui-mark)` на иконке `lucide-react`. Ни правила, ни исключения
больше нет: смешивать стало нечего.

Про каскад знать всё равно нужно, и не из истории. Слои объявлены в
`shared/styles/index.css`, и любой неслойный CSS — приехавший с библиотекой, вписанный
в `index.html`, вернувшийся модулем — снова окажется сильнее любой утилиты. Проигрыш
будет молчаливым: утилита просто не подействует, а соседняя подействует. Поэтому новый
CSS кладётся **в слой**, а не рядом с ним (`docs/notes/ui.md`, «Два слоя стилей
разводятся каскадом, а не порядком импорта»).

## Запуск

Разработка идёт локально: `pnpm dev` поднимает Vite с прокси `/api` на бэкенд
(`VITE_API_URL`, по умолчанию `http://localhost:8000`). Бэкенд — корень этого
репозитория (`..`); поднимается по `../docs/DEVELOPMENT.md`, с демо-данными.

Сборка и сквозные тесты идут в Docker: `docker/Dockerfile` собирает статику и отдаёт её
через nginx, `docker-compose.yml` поднимает интерфейс вместе с бэкендом из корня
репозитория для Playwright. Задача, после которой что-то работает только «руками на моей машине»,
не считается выполненной.

## Структура кода

Feature-Sliced Design, слои — без `widgets`, ниже все, что есть:

```
src/
  app/        — точка входа, маршруты, провайдеры, тема, глобальные стили
  pages/      — по странице на экран из CONCEPT.md: login, tasks, task, case, questions
  features/   — действия и интерактив: фильтры списка, форма ответа, живой поток
  entities/   — задача, запись дела, участник, очередь: типы из контракта, запросы,
                представление одной сущности (строка, карточка, запись)
  shared/     — клиент API и сгенерированные типы, словарь ошибок, тема, утилиты, ui-кирпичи
```

Правила импортов: слой импортирует только слои ниже; срезы одного слоя друг друга
не импортируют; наружу срез отдаёт только `index.ts`. Нарушения ловит `steiger`
в `pnpm check`.

Компоненты shadcn/ui живут в `shared/ui`, а не в `components/ui`, куда их кладёт CLI:
у проекта слои Feature-Sliced Design, и кирпич интерфейса принадлежит `shared`. Псевдонимы
переопределены в `components.json` (`components` → `@/shared/ui`, `utils` → `@/shared/lib`),
поэтому `pnpm dlx shadcn@latest add <компонент>` кладёт файл сразу на место. Добавленный
компонент — не чужой файл, а наш: его правят под наши токены и подписи и описывают
в `src/shared/ui/AGENTS.md`.

## Клиент API

- Типы генерируются из `../openapi.json` командой `pnpm gen:api`
  в `src/shared/api/openapi.ts`; сгенерированный файл коммитится, чтобы сборка образа не
  зависела от бэкенда: её контекст — только `ui/`. Тест сверяет его с источником и падает
  при расхождении.
- Ручных типов ответов нет: всё берётся из сгенерированных. Если типа не хватает,
  чинится контракт бэкенда, а не дописывается тип на клиенте.
- Оболочка ответа (`data`, `meta`, `error`) разбирается в одном месте (`shared/api`);
  страницы получают уже распакованные данные или типизированную ошибку с `code`,
  `message`, `details`.
- Токен хранится в `localStorage` под одним ключом, добавляется в `Authorization: Bearer`
  одним перехватчиком. Ответ `401` сбрасывает токен и ведёт на вход.
- Тексты ошибок для человека берутся из словаря `shared/errors` по `error.code`;
  неизвестный код показывает `message` бэкенда и попадает в отчёт задачи как пункт
  «дополнить словарь».

## Состояние

- Серверное состояние живёт только в TanStack Query: ключи запросов строятся из
  идентификаторов контракта (`['task', key]`, `['tasks', filters]`), инвалидация по кадрам
  живого потока идёт по этим ключам.
- Фильтры, режим отображения, выбранная очередь и курсор живут в адресе страницы.
- Локальное состояние компонентов — `useState`; глобального хранилища нет, пока не
  появится состояние, которое нужно двум страницам сразу и не выражается адресом.

## Интерфейс

- **Подпись берётся из словаря, а не набирается в разметке.** Все тексты интерфейса
  живут в `src/shared/i18n/dictionaries/<язык>` и приходят на экран через
  `useTranslation`; в разметке не остаётся ни строки — ни в тексте, ни в `aria-label`,
  ни в `title`, ни в `placeholder`, ни в `alt`. Языков сегодня два, и добавляется
  третий одним словарём: ветвление по языку в компоненте (`lang === 'ru' ? … : …`)
  запрещено — язык обязан быть данными словаря. Язык по умолчанию английский, дальше
  берётся язык браузера, а поверх всего — выбор человека, который живёт в
  `localStorage` и в адрес не попадает (параметра `?lang=` у интерфейса нет).
- **Это стережёт `pnpm check`, а не внимательность.** Два правила в `eslint.config.js`:
  `i18next/no-literal-string` ловит подпись в разметке на любом языке, а
  `no-restricted-syntax` — русскую строку и язык литералом за её пределами. Список
  исключений и причина у каждого записаны там же; подгонять сторожа под код нельзя —
  нашёл подпись, значит правится код. Русский при этом остаётся языком разработки:
  комментарии, `AGENTS.md`, заметки и тесты сторож не трогает.
- Данные не переводятся: названия и описания задач, тексты записей, имена участников
  и названия очередей показываются такими, какими их написал агент. Идентификаторы
  контракта (статусы, типы записей, ключи) остаются как есть, моноширинным шрифтом.

  До 9 сентября 2026 здесь стояло правило «Подписи на русском; идентификаторы контракта
  (статусы, типы записей, ключи) как есть, моноширинным шрифтом». Первая его половина
  **отменена решением владельца** от 9 сентября 2026 (`UI-76`), вторая осталась и стоит
  строкой выше. Прежнее правило названо, а не вычищено: следующий агент должен понимать,
  что двуязычность — решение, а не забытый русский.
- Тема светлая и тёмная по `prefers-color-scheme`; цвета, отступы, кегли и радиусы —
  только через токены `@theme` (`shared/styles/theme.css`), значения которых объявлены
  в `shared/styles/tokens.css`. Литерал в разметке (`text-[13px]`, `bg-[#eee]`) не
  переключается вместе с темой и потому запрещён; примитивы (`--n-*`, `--d-*`) в разметке
  тоже не появляются — у них нет ответа на вопрос «что этим красят».
- Доступность: семантическая разметка, фокус виден, таблицы с заголовками, формы
  с подписями; Playwright гоняет `axe` на каждой странице.
- Пустые и ошибочные состояния у каждого запроса нарисованы явно; спиннер без текста
  не считается состоянием.
- Даты показываются в местном времени и относительно («3 minutes ago», «3 мин. назад» —
  форматирует `Intl` на языке интерфейса) с точным временем в подсказке.

## Тесты

- Каждая задача завершается тестами: модульные на разбор данных и словари, страничные
  с MSW на состояния (данные, пусто, ошибка, загрузка), сквозные Playwright на главный
  сценарий задачи против бэкенда с демо-данными.
- `pnpm check` = типы, линт (вместе со сторожем подписей), границы слоёв, модульные
  тесты. `pnpm e2e` = Playwright в Docker. Задача выполнена, когда зелёно и то и другое.
- Подгонять тест под поведение, ослаблять проверку или помечать пропущенным ради
  зелёного запрещено.
- Язык у сквозных сценариев задаётся `locale` проекта Playwright или записью в
  `localStorage` до загрузки страницы — параметра адреса ради удобства тестов не
  заводится. Русскими остаются сценарии, проверяющие поведение, геометрию и тему;
  язык проверяет свой проект «английская» (`playwright.config.ts`) — механизм выбора,
  форматы и дымовой прогон главных экранов. Ожидаемая подпись в нём вписана руками:
  взятая из словаря, она проверяла бы связь ключа с элементом, а не текст.
- Страничный тест берёт подпись из словаря `testing/say.ts` — тем же ключом, что и код:
  там проверяется как раз связь ключа с элементом, а формулировку правят в одном месте.

## Документация

- Каждая папка описывается `AGENTS.md`: что в ней лежит, одним уровнем. Создал
  или переместил файл — обновил карту.
- Соглашения бэкенда не копируются: контракт описан в `docs/FRONTEND.md`
  и `openapi.json`.
- **Число, которое считается из кода, в прозе не пишется.** «Пятнадцать типов записи
  дела», «тонов ровно шесть», «четыре слоя» — такое число разойдётся с кодом молча, при
  первой же правке в другом месте, и никто не обязан вспомнить про строку в заметке.
  Вместо числа называется, откуда читатель посчитает сам: тип (`EntryType` в
  сгенерированном `src/shared/api/openapi.ts`), файл (`src/shared/styles/tokens.css`,
  `steiger.config.ts`) или проверка (`theme.test.ts`, `pnpm check`). Из правила есть три
  законных исключения, и путать их с нарушением не стоит: число-замер с условиями
  и историей («508 → 477 пикселей», «был одним объектом из шестнадцати полей» — про
  модель, которой больше нет); число, при котором тут же, в том же месте, стоит само
  перечисление (тогда рассинхронизация видна читателю сразу, а не молча — «все три
  обязаны совпадать», и они названы строкой выше); и свойство домена, а не счёт кода
  (пять разделов задачи, четыре экрана из `CONCEPT.md`, два независимых графа связей).
- **Перечень, который отдаёт код, в прозу не переписывается.** Копия списка отстаёт
  от источника так же тихо, как и число при нём: состав тонов называет `tokens.css`,
  слои — `steiger.config.ts` и структура `src/`, форма записи по типу —
  `EntryFacts` в `src/shared/api/openapi.ts`. Держать перечень в двух местах в согласии
  стоит сверкой множеств, а не вычиткой — так уже делает `theme.test.ts` для границы
  слоёв токенов и `testing/notes.test.ts` для имён в самих заметках.
- Проверки, которая считала бы числительные в тексте, в проекте нет и заводить её
  не стоит: она дороже и ненадёжнее, чем не писать число, и не отличит «сейчас
  типов пятнадцать» от «пять разделов задачи».

## Коммиты

Conventional Commits: `<тип>(<область>): описание в повелительном наклонении`.
Типы: `feat`, `fix`, `docs`, `refactor`, `test`, `build`, `chore`, `style`, `perf`;
`merge` — слияние ветки задачи, и в его описании стоит её ключ.
Область — затронутый слой или узел: `app`, `tasks`, `task`, `case`, `questions`, `live`,
`entry`, `motion`, `api`, `ui`, `notes`, `docker`. Список не закрыт: область называет
место изменения, а не выбирается из перечня.
Один коммит — одно осмысленное изменение; карты `AGENTS.md` и правки заданий идут тем же
коммитом, что и код. В индекс добавляются только свои файлы поимённо. Пуш и ветки
только по явной просьбе.

## Слияние ветки задачи в main

Ветку задачи сливает не её исполнитель, а тот, кто ведёт слияния: в соседних деревьях
`git worktree` в это время идут другие задачи, и `main` двигают в одном месте.

**Зелёные родители слияния не означают зелёного результата.** Каждая ветка прогоняла
проверки на своём дереве, а базой для следующей задачи становится результат. Ветка A
переименовала символ, ветка B в это же время назвала его в соседнем месте — git сведёт
их без конфликта, файлы-то разные, и красным или неполным окажется `main`, которого не
проверял никто. Это не теория: 10 сентября 2026 три ветки (`UI-66`, `UI-79`, `UI-74`),
зелёные каждая у себя, дали после чистого слияния четыре падения и сорок четыре
незапущенных сценария `pnpm e2e` (`UI-93`).

Поэтому слияние делается одной командой, из того дерева, где лежит `main`:

```bash
scripts/merge-task-branch.sh task/UI-93 -m "merge(область): что изменилось (UI-93)"
```

Она сливает без коммита (`--no-ff --no-commit`) и прогоняет на результате два набора по
очереди: сперва `pnpm check` (хост, секунды) — красный на нём останавливает слияние, не
тратя времени на второй; затем, если он зелёный, `pnpm e2e` (Docker, минуты). Оба
обязательны: именно `pnpm e2e` поймал живой случай выше, `pnpm check` его не видит вовсе.
Коммит уезжает только на двойном зелёном, и в сообщение идёт строка `Merge-verified:`
с исходом каждого из двух наборов. Красный любой — `git merge --abort`, и `main` остаётся
прежним: расхождение видно до того, как стало базой. Конфликты скрипт оставляет
человеку: разобрать, добавить в индекс, довести слияние тем же скриптом с `--continue`.

Своя изоляция контура для `pnpm e2e` (свой compose-проект, свой порт, свой
`COMPOSE_FILE` — когда слияние проверяется не в единственном рабочем дереве машины)
задаётся так же, как для прямого запуска: переменными окружения перед вызовом скрипта.
Сам скрипт ничего не подставляет — он лишь наследует окружение вызвавшего.

Исход слияния называется в деле задачи, а не подразумевается:

- зелёное — запись `artifact` с ключом коммита слияния и обеими строками прогона;
- красное — запись `remark` в дело той задачи, чья ветка не сошлась с базой: ветка не
  слита, и разбирается это по правилам замечаний, а не молчаливой правкой `main`.

Слияние руками (`git merge`) не запрещено, но не остаётся незаметным: у такого коммита нет
строки `Merge-verified:`, и следующий запуск скрипта называет его перед тем, как сделать
своё слияние. Вписать строку задним числом нельзя — она означает прогон, а не намерение.

## Протокол выполнения задачи

Обязателен для любого агента, берущего задачу репозитория (очередь `UI` в трекере).
Шаги идут по порядку.

1. **Подготовка.** Прочитать `CONCEPT.md`, этот файл, свою задачу целиком, заметки
   в `notes/` по своей области, `docs/FRONTEND.md`. Проверить по коду, что
   задачи-зависимости выполнены, а не поверить статусу связи.
2. **Реализация** в границах слоёв и правил выше.
3. **Проверка.** `pnpm check` и `pnpm e2e` зелёные целиком, не только свои тесты.
   Затем живая проверка руками: поднять бэкенд с демо, открыть интерфейс в браузере,
   пройти сценарий задачи, посмотреть консоль браузера и сеть на ошибки и лишние
   запросы.
4. **Зафиксировать находки** в `notes/` по формату ниже.
5. **Обновить карты** `AGENTS.md` затронутых папок.
6. **Закрыть задачу** в трекере по дисциплине, которую сервер раздаёт сам —
   `instructions` и метадатой инструментов: сводка, вердикт по каждой обзорной проверке,
   `close_task`. Со стороны репозитория
   остаётся одно: находки — в `notes/`, карты — в `AGENTS.md`, а обнаруженная работа,
   не влезающая в твою задачу, — отдельной задачей, а не строкой в отчёте.
7. **Закоммитить** самостоятельно, если всё зелёно и живая проверка прошла; иначе
   оставить в рабочем дереве и объяснить в отчёте.
8. **Погасить всё, что запускал**: dev-сервер, контуры Docker, Playwright.
9. **Проверить готовность к передаче.** Следующий агент не знает ничего из твоего
   контекста: всё знание в файлах, все «надо не забыть» стали пунктами задач.
10. **Отчитаться**: что сделано, что не сделано и почему, какие решения приняты
    самостоятельно, что дописано в чужие задачи, что осталось запущенным, вердикт
    «можно закрывать сессию» или «закрывать рано, потому что».

## Заметки: находки, грабли, особенности

Папка `notes/`, один файл на область: `api.md`, `live.md`, `ui.md`, `testing.md`,
`tooling.md` и подобные. Заметки только копятся. Формат записи:

```markdown
## Краткое название находки

**Что:** факт одной фразой.
**Почему важно:** что сломается или сколько времени потеряет тот, кто этого не знает.
**Как правильно:** конкретное действие или пример.
**Где:** файл или место в коде, если применимо.
```

Указатель «Где:» обязан вести в существующий файл, а названный в нём символ —
находиться в этом файле. Это стережёт `testing/notes.test.ts` в `pnpm check`, а не
вычитка: переезд файла ломает знание тихо, и глазами это не ловится. Сторож читает
ведущий список мест каждого куска указателя (куски делятся `;`), а прозу после него —
нет: в ней законно поминают снесённый код. Указатели в бэкенд (`../...`) проверяются
так же, как свои, — дерево одно; текст такой записи с кодом интерфейса не сверяется,
потому что говорит о коде бэкенда. Не проверяются только абсолютные пути.

Остальной текст записи сторож читает тоже, но вопрос к нему слабее: названный файл
обязан существовать, а имя, читающееся как имя из кода, — встречаться где-нибудь
в коде репозитория. Так переименование компонента или хука роняет прогон у того, кто
переименовал. Классы Tailwind, переменные CSS, куски команд, числа и обычные слова
в обратных кавычках не проверяются; чужое или снесённое имя, помянутое по делу,
освобождается списком в самом стороже — с причиной словами
(`docs/notes/testing.md`, «Сторож заметок читает всю запись»).

Файл, чья механика снесена целиком, оставляется как история и помечается баннером
в первых строках, до первой записи: `> **Файл-история:** …`. Его указатели сторож
пропускает. Освобождается файл целиком — отдельная запись себя баннером не освободит.

Что писать: поведение библиотеки, выясненное опытным путём; неочевидное решение
и причину, по которой очевидное не подошло; ограничение окружения; место, которое
выглядит странно, но так задумано. Что не писать: пересказ документации библиотек,
описание собственного кода, историю попыток, планы.
