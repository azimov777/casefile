# src/features/connect-agent/ui

## Файлы

- `connection-snippets.tsx` — `ConnectionSnippets`: дорожка клиентов `SegmentedNav` и раздел выбранного клиента с объяснением и блоками копирования, переменная Codex — оба варианта, bash/zsh и PowerShell, а не один по умолчанию, поля формы Codex с ключами файла, объяснение метки у общего токена
