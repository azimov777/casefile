# src/features/manage-installation/api

## Файлы

- `archive.ts` — `exportInstallationArchive` (архив тем же ключом `data`, каким его
  вернул бы бэкенд), `importInstallationArchive` (тело — файл выгрузки как есть)
