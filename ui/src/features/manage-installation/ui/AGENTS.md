# src/features/manage-installation/ui

## Файлы

- `import-dialog.tsx` — подтверждение приёма `role="alertdialog"`: предупреждение,
  имя выбранного файла, отправка
- `import-result.tsx` — итог приёма: ревизии, таблицы по строкам, ключи машины,
  отозванные ключи источника, замененные строки — только то, что вернул `ArchiveImportRead`
