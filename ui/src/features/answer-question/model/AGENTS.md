# src/features/answer-question/model

## Файлы

- `draft.ts` — черновик ответа и ключ повтора в `sessionStorage`, разбор строки ссылок
- `use-answer-question.ts` — отправка ответа с `Idempotency-Key` и перечитывание задетых экранов
