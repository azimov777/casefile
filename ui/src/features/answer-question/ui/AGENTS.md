# src/features/answer-question/ui

## Файлы

- `answer-form.tsx` — форма ответа: поле, предпросмотр, отправка с ключом повтора
- `answer-form.module.css` — стили формы; место под упрёк занято всегда, кнопки не едут
- `answer-receipt.tsx` — подтверждение: ответ подшит, вот его номер ссылкой и текст
- `answer-receipt.module.css` — стили подтверждения
