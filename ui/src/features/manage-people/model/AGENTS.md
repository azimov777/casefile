# src/features/manage-people/model

## Файлы

- `use-people-actions.ts` — `useCreateAccount` и `useResetPassword` (мимо кэша запросов: пароль уезжает вызывающему), `useSetDisabled`; инвалидация списка учётных записей
