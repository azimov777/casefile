# src/features/live-journal/model

## Файлы

- `stream-client.ts` — открытие SSE с заголовком авторизации и переподключением; граница, подменяемая в тестах
- `frames.ts` — кадр журнала и разбор его тела
- `invalidation.ts` — какие запросы устарели от записи этого типа
- `use-live-journal.ts` — поток на вкладку: курсор `seq`, накопление в фоне, статус, вопрос ко мне
- `live-journal.test.tsx` — соединение, кадры, обрыв, фон, `401`, уведомление о вопросе
