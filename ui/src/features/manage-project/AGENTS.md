# src/features/manage-project

Действия человека с проектом (`UI-175`, решение 8 `TRK-150`): завести проект из панели,
править название и описание с видимым остатком до предела, ставить, менять и снимать
атрибуты — изменение и снятие только с причиной, — писать заметки в дело проекта;
архивировать и восстанавливать проект с причиной (`UI-176`).

Что из этого видно, решает набор ключа сеанса (`bootstrap.token.scope`, `useProjectRights`):
создание, карточка, архив и восстановление — `main`, атрибуты и заметки — любой набор. Недоступное не
рисуется вовсе. С задачами проекта срез ничего не делает.

Окна открываются кнопкой, переданной в `Dialog` как `trigger`: фокус после закрытия
возвращается на неё. Заметка — не окно, а форма записи на месте (`Composer`), как
замечание к задаче.

## Папки

- `api/` — запросы: создание, правка, атрибут, снятие атрибута, заметка, архив и восстановление
- `model/` — права по набору ключа, мутации с перечитыванием, ключ черновика заметки
- `ui/` — окна создания, правки карточки, атрибутов, архива и восстановления, поля описания и причины, форма заметки

## Файлы

- `index.ts` — публичный интерфейс среза: `CreateProject`, `EditProject`, `AddAttribute`, `ChangeAttribute`, `RemoveAttribute`, `ProjectArchiving`, `NoteForm`, `useProjectRights`
