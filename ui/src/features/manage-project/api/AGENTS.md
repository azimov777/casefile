# src/features/manage-project/api

## Файлы

- `projects.ts` — `POST /projects` с ключом повтора, `PATCH /projects/{key}`, `PUT …/attributes/{name}` (причина — только при изменении), `POST …/attributes/{name}/remove`, `POST /projects/{key}/entries` типа `note`, `POST /projects/{key}/archive` и `/restore` с причиной
