# src/features/task-filters/model

Отбор как данные: адрес, параметры запроса, разбор отказа.

## Файлы

- `filters.ts` — тип отбора, чтение и запись адреса (включая свёрнутые столбцы доски), перевод в параметры `GET /api/v1/tasks`
- `filters.test.ts` — круг «адрес → отбор → адрес», приоритет поля запроса, отсев чужих значений
- `href.ts` — адрес списка с изменёнными условиями: одно правило на все точки навигации, курсор не переносится
- `href.test.ts` — сохранение условий при смене вида, сброс курсора, отсев чужих параметров
- `query-problem.ts` — позиция и допустимые значения из `details` отказа разбора
- `query-problem.test.ts` — разбор `details` и указатель на позицию
- `summary.ts` — включённые условия словами: что показывает свёрнутый вид отбора
- `summary.test.ts` — полнота перечисления и то, что ни одно условие не свёрнуто в счётчик
- `expanded.ts` — развёрнутость формы в `localStorage`: состояние человека, а не выдачи
- `use-task-filters.ts` — отбор через `useSearchParams`: правка заменяет запись в истории, «ещё» добавляет
