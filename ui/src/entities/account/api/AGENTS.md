# src/entities/account/api

## Файлы

- `accounts.ts` — тип учётной записи из контракта, `isDisabled` (отключение — это заполненный `disabled_at`), ключи запросов и `accountsQueryOptions`: все учётные записи установки, отключённые тоже, страницами по курсору; только администратору
