# src/entities/token/api

## Файлы

- `tokens.ts` — тип токена из контракта, `isRevoked` (отзыв — это заполненный `revoked_at`), ключи запросов и `tokensQueryOptions`: список всех токенов установки, включая отозванные, страницами по курсору
