# src/entities/project/api

## Файлы

- `project.ts` — тип карточки проекта с атрибутами (`ProjectDetailRead`) и атрибута, ключи запросов под префиксом `['project', key]`, `projectQueryOptions`: `GET /api/v1/projects/{project_key}`
