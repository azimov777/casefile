# src/pages/connect/ui

## Файлы

- `connect-page.tsx` — токен агента этой машины командой, фрагменты из `features/connect-agent` с флажком общего токена в адресе (`?shared=true`), скил дисциплины командой из установки — `../model/skill-command.ts`, обе оболочки
- `connect-page.test.tsx` — вход из навигации ключом `task`, адрес из ответа установки, другой адрес — другой текст, нет секрета, флажок и адрес, копирование, отказ с повтором, русский
