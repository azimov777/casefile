# src/pages/connect/ui

## Файлы

- `connect-page.tsx` — три нумерованных шага в одной колонке по центру (`--ui-column-max`): токен агента этой машины командой, фрагменты из `features/connect-agent` с флажком общего токена в адресе (`?shared=true`), скил дисциплины командой из установки — `../model/skill-command.ts`, обе оболочки
- `connect-page.test.tsx` — вход из навигации ключом `task`, адрес из ответа установки, другой адрес — другой текст, нет секрета, флажок и адрес, клиент в адресе и его умолчание, шаги по порядку, копирование, отказ с повтором, русский
