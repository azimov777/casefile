# src/pages/access

Экран «Доступы»: все токены установки, заведение агента-участника, выпуск токена
с показом секрета один раз и отзыв. Список виден любым ключом, запись открыта ключу
набора `main`; ввода ключа на экране нет (`UI-104#7`).

## Папки

- `ui/` — экран доступов

## Файлы

- `index.ts` — публичный интерфейс среза
