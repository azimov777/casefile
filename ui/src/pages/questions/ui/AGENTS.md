# src/pages/questions/ui

## Файлы

- `questions-page.tsx` — две половины входящей: вопросы ко мне и мои замечания без разбора; очередь отбирает обе, «только блокирующие» — вопросы; пустота различает «ничего нет» и «ничего не нашлось»; оформление утилитами, две колонки от точки `wide`
- `questions-page.test.tsx` — ответ с ключом повтора, повтор после обрыва, замечания у полей, черновик, пустые состояния под отбором, ссылки на записи
