# src/pages/moving/ui

## Файлы

- `moving-page.tsx` — экран в одной колонке по центру: выгрузка кнопкой, приём файлом с
  подтверждением в отдельном окне и панелью итога, объяснение неадминистратору
- `moving-page.test.tsx` — пункт панели и обе кнопки администратору, объяснение и ни
  одного запроса архива неадминистратору, выгрузка и её отказ, файл не JSON, тело
  приёма как есть и итог на экране, повторный приём (`installation_not_empty`) в
  открытом окне, отмена без запроса, русский язык
