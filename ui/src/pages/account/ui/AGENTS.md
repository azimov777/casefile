# src/pages/account/ui

## Файлы

- `account-page.tsx` — экран: почта, имя-подпись, роль из первого кадра и форма смены своего пароля
- `account-page.test.tsx` — вход из панели почтой, смена пароля с прежним, расхождение повтора без запроса, отказ неверного прежнего, своя машина без экрана
