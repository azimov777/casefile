# src/pages/account

Экран «Моя учётная запись» (`/account`): почта, имя-подпись, роль и смена своего пароля.
Только в режиме входа (страж `app/routes/require-sign-in-mode.tsx`).

## Папки

- `ui/` — экран своей учётной записи

## Файлы

- `index.ts` — публичный интерфейс среза
