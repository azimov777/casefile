# src/pages/login

Экран входа. Форма и проверка токена лежат в `features/auth`; страница отвечает
за обрамление, объяснение просроченного сеанса и переход на `/tasks` после удачи.

## Папки

- `ui/` — `login-page.tsx` и его стили

## Файлы

- `index.ts` — публичный интерфейс среза: `LoginPage`
