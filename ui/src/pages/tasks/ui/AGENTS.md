# src/pages/tasks/ui

Экран списка задач.

## Файлы

- `tasks-page.tsx` — сборка экрана, состояния (загрузка, пусто, отказ, отказ разбора), листание
- `tasks-page.module.css` — стили экрана
- `tasks-table.tsx` — таблица: заголовки столбцов и строки
- `tasks-table.module.css` — стили таблицы
- `tasks-page.test.tsx` — состояния экрана с MSW: данные, признаки, пусто, отказы, курсор
