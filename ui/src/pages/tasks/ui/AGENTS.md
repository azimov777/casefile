# src/pages/tasks/ui

Экран списка задач в двух режимах: таблицей и доской. Переключатель вида живёт
в верхней полосе оболочки (`features/task-filters/ui/view-switch.tsx`).

## Файлы

- `tasks-page.tsx` — сборка экрана, выбор режима, состояния (загрузка, пусто, страница за концом выдачи, отказ, отказ разбора)
- `tasks-table.tsx` — таблица: липкая шапка, заданные ширины колонок, строки одной высоты
- `tasks-pagination.tsx` — ряд страниц под таблицей: число страниц из `meta.total`, ссылки через `tasksHref`
- `tasks-board.tsx` — доска: столбец на каждое значение статуса из контракта, свёрнутые `done` и `cancelled`
- `tasks-page.test.tsx` — состояния экрана с MSW: данные, признаки, пусто, отказы, страницы
- `tasks-board.test.tsx` — столбцы из перечисления клиента, свёрнутость, отбор, дочитывание
