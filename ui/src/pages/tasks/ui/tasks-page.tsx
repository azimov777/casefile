import { Callout } from '@/shared/ui';

/**
 * Заглушка списка задач. Экран собирается в задаче 02: здесь он есть только затем,
 * чтобы вход было куда завершать, а оболочку — на чём проверять.
 */
export function TasksPage() {
  return (
    <main>
      <h1>Задачи</h1>
      <Callout>Список появится в задаче 02.</Callout>
    </main>
  );
}
