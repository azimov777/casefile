# src/pages/tasks

Список задач. Отбор живёт в `features/task-filters`, строка — в `entities/task`;
страница собирает их, держит один запрос на страницу выдачи и рисует состояния.

## Папки

- `ui/` — экран и таблица

## Файлы

- `index.ts` — публичный интерфейс среза: `TasksPage`
