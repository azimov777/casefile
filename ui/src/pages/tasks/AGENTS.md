# src/pages/tasks

Список задач. Сейчас заглушка: экран собирается в задаче 02, здесь он есть только
затем, чтобы вход было куда завершать.

## Папки

- `ui/` — `tasks-page.tsx`

## Файлы

- `index.ts` — публичный интерфейс среза: `TasksPage`
