# src/pages/project

Экран проекта на чтение (`/projects/:key`, UI-174): карточка, атрибуты с историей, дело
проекта описью. Состояние — в адресе: `?entry=N`, `?attribute=имя`.

## Папки

- `ui/` — страница, блок атрибутов с историей, блок дела проекта

## Файлы

- `index.ts` — публичный интерфейс среза: `ProjectPage`
