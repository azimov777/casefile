# src/pages/project

Экран проекта (`/projects/:key`, UI-174): карточка, атрибуты с историей, дело проекта
описью; действия с проектом из `features/manage-project` (UI-175) по набору ключа.
Состояние — в адресе: `?entry=N`, `?attribute=имя`.

## Папки

- `ui/` — страница, блок атрибутов с историей, блок дела проекта

## Файлы

- `index.ts` — публичный интерфейс среза: `ProjectPage`
