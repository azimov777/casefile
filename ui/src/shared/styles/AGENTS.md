# src/shared/styles

Тема и сброс. Цвета, отступы и шрифты берутся только отсюда — переменными.

## Файлы

- `tokens.css` — значения темы (`--t-*`) и тоны; тёмная выбирается `prefers-color-scheme`
- `theme.css` — `@theme` Tailwind: семантические токены и шрифты, из них рождаются утилиты
- `reset.css` — сброс и базовая типографика; подключается в слой `base`
- `index.css` — единственная точка подключения и порядок слоёв каскада
