/**
 * Источник API. Пуст в браузере — и dev-сервер Vite, и nginx проксируют `/api`
 * на бэкенд, поэтому запрос всегда идёт на свой источник: токен не уезжает в чужой
 * источник, и CORS не нужен.
 *
 * Назван отдельным модулем, потому что им пользуются мимо клиента `openapi-fetch`: живой
 * поток (SSE с заголовком авторизации требует своего `fetch`, `features/live-journal`) и
 * чтение сеанса по куке в `install-config.ts`, который сам нужен клиенту, — импорт из
 * `client.ts` замкнул бы круг.
 */
export const apiBaseUrl: string = import.meta.env.VITE_API_BASE_URL ?? '';
