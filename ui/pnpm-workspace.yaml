# Сборочные скрипты зависимостей pnpm запускает только по явному разрешению.
# Разрешены те, без которых пакет нерабочий: esbuild ставит свой бинарник,
# msw — служебный воркер.
allowBuilds:
  esbuild: true
  msw: true
