# testing/msw

Подмена API для страничных тестов.

## Файлы

- `server.ts` — сервер MSW; общих обработчиков нет намеренно, их задаёт каждый тест
- `responses.ts` — ответы в оболочке контракта и фикстуры `bootstrap`, `task`, `collection`
